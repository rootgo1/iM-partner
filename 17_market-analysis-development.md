# CCTV 중심 상권분석 개편

## 2026-09-18 추가 수정: 숫자 카드·시각 갱신·지도·비교 화면 복원

회복전략은 최신 요청에 따라 **대화·리포트 단일 카드**로 정리했습니다. 별도의 ‘상권분석에서 찾은 회복전략’ 화면은 제거했고, 상권 분석 계산과 조건 변경 시 기존 리포트를 무효화하는 처리는 유지합니다. 사후관리에는 대화에서 확인하고 직접 수행한 행동을 기록합니다.

이 절이 아래 이전 구성보다 우선합니다. 화면은 **상권 요약 → 시간대·요일별 진단 → 방문·결제 진단 → 주변상권 진단**, 총 4개입니다. 매출진단과 같이 ‘요약·진단’ 명칭을 사용하며 주변상권 진단 이동 버튼은 마지막 섹션으로 연결합니다. 월간 요약 화면을 제거하되 회복전략의 기간 분석 근거는 유지합니다.

관측 지점·월간 분석 요일·복수 선택·초기화는 매출진단과 같은 필터 바 디자인으로 표시합니다. 공통 고정 영역을 모든 섹션 앞에 두어 끝까지 접근할 수 있고, PC·모바일 모두 꺾쇠 버튼으로 접기·펼치기와 조건 요약을 제공합니다. 접힌 상태도 고정됩니다. 관측 지점은 현재 하나뿐이므로 읽기 전용이며, 월간 요일 필터가 최근 10분 관측을 바꾸지는 않습니다.

- 첫 화면의 4개 카드는 최근 통행·같은 요일 통행 평균·최근 입장률·같은 요일 입장률 평균입니다. 미니 그래프를 제거하고 숫자와 ▲/▼ 비교로 표시합니다. 입장률 차이는 %p, 통행 증감은 %로 구분합니다.
- 제목의 괄호는 서울 현재 시각이며 매분 갱신합니다. 관측 기준 날짜·완료된 10분 구간은 따로 표시합니다. 시간 경계에서 요약 카드만 갱신하여 지도와 스크롤 상태를 유지합니다. 영업시간 밖에는 마지막 완료 관측임을 표시합니다.
- 주변흐름은 자료가 있는 시간대만 왼쪽에 표시하고, 자료 없는 시간은 0으로 취급하지 않습니다. 오른쪽에는 대구 행사 지도 카드를 배치합니다. 키 발급 전은 OpenStreetMap, 카카오 JavaScript 키 등록 후는 카카오 지도·번호 마커를 사용합니다. 작은 화면에서는 위아래로 배치합니다.
- 복원된 비교 화면은 시간대·요일별 통행과 입장률을 전기와 비교합니다. 1시간을 구성하는 10분 관측 6개가 모두 있는 날만 시간대 평균에 사용합니다. 그래프 높이를 제한하고 상세 근거를 열 수 있습니다.
- 최종 배치에서 좌우 비교 차트와 주변흐름/지도 카드를 같은 폭·높이로 맞췄습니다. 비교 차트 영역은 PC 296px·모바일 260px로 동일하게 맞추고 긴 내용만 스크롤합니다. 분석 기간 청록 `#137C78`과 비교 기간 회청 `#78869B`를 사용하며 숫자 카드의 값·증감 행도 정렬합니다.
- 사용자 제공 **전국문화축제표준데이터.csv**에서 전국 1,305행 중 대구 56행을 추출했고, 같은 일정 중복을 병합한 46개 행사 기록을 연결했습니다. 31개 등록 행사장 좌표를 사용하며 행정기관 참조 13개·좌표 없는 2개는 지도와 거리 계산에서 제외합니다. 진행·예정/전체 기록, 직선거리 범위, 선택 행사 위치·상세 정보가 동작합니다. 원본 연도와 데이터 기준일을 표시하며 관광공사 API는 호출하지 않습니다. 기준 위치는 여전히 대구 중구 예시입니다. [자료·카카오 설정 안내](prototype/README-nearby-events.md)를 참고하세요.

## 2026-09-18 최신 회의 반영: 최근 10분과 완료된 한 달

**이 절이 현재 기준입니다. 아래 같은 날의 이전 기록과 과거 기간·체류·섹션 설명보다 이 절을 우선합니다.**

- 번호 없이 **상권 분석 요약 → 주변상권 흐름 → 통행에서 결제까지 → 월간 상권 분석 요약**의 4개 섹션입니다. 위치는 **관측 CCTV / 매장 앞 CCTV**로 표시합니다. 시간대 선택은 없고 월간 요일 필터 옆에 복수 선택 체크를 배치했습니다.
- `getLiveSnapshot(now, source)`는 한국시각 기준 가장 최근 완료된 10분 관측을 읽습니다. 19:16에는 19:00~19:10, 19:20에는 19:10~19:20 자료를 사용하고 각각 `(19:00)`, `(19:10)`으로 표시합니다. 한 시간에 6번 갱신하며 해당 구간이 없으면 이전 관측으로 바꾸지 않습니다. 첫 영업 구간 완료 전은 대기, 영업시간 밖은 마지막 예정 구간의 실제 날짜와 영업시간 외 상태를 표시합니다.
- 첫 화면 4개 카드는 최근 10분 통행 관측, 요일별 통행 관측, 최근 10분 입장률, 요일별 입장률입니다. 전기 대비 수치를 제거했습니다. 월간 요일 선택은 최근 관측을 바꾸지 않습니다. 첫 화면 버튼은 주변상권 흐름 차트로 이동합니다.
- 많고 적음의 기준은 이전 28일 중 같은 요일·같은 시각의 유효 자료입니다. 3일 이상일 때 +20% 이상은 많은 편, -20% 이하는 적은 편, 나머지는 비슷한 수준입니다. 관측 수는 고유 인원 수가 아닙니다. 누락·부분·중복 기록은 제외하고 유효한 0건은 유지합니다.
- `lastCompleteMonth(now)`와 `monthlySummary(now, filters, source)`는 **직전 완료된 한 달**을 사용합니다. 9월 접속 시 8월 1~31일이며 시간 범위는 전체입니다. 마지막 요약과 회복전략의 월간 근거는 같은 기간·요일을 사용합니다.
- 주변상권 흐름은 주변 통행과 서비스 이용 가게 소비의 시간별 평균을 00~23시로 표시합니다. 시연 자료가 없는 시간은 줄무늬와 `—`로 구분하며 0으로 만들지 않습니다. 매장 앞 CCTV와 주변상권 표본의 범위를 구분합니다. 오른쪽 상권/가게 비교와 하단 행동 인사이트는 제거했습니다.
- 통행에서 결제까지는 별도 섹션으로 복원했습니다. 통행·입장·유효 결제를 연결하되 개인별 경로 추적이 없는 결제 전환은 추정치입니다. 체류→입장 단계는 사용하지 않습니다.
- 월간 요약은 근거에 따라 통행 집중·요일별 통행·입장률 집중·비교 가능한 입장률 하락 등을 생성하며 3개로 고정하지 않습니다. 체류 요약/회복 제안은 제외했고 운영 행동은 회복전략에 표시합니다.

실제 CCTV·영상 분석 엔진은 미연결이며 수치는 생성 자료입니다. 기존 계산 32개 조건, `market-live-monthly.test.cjs`의 월/10분 경계·누락·0건·표본 기준·±20% 경계, `market-meeting-ui.test.cjs`, 실제 브라우저의 차트 이동·시간 갱신·요약 개수 변화·반응형 검사를 통과했습니다.

## 2026-09-18 이전 구현 기록: 이번 달 관측과 간결한 필터

**이 절과 아래 내용은 이전 개발 이력입니다. 현재 기준은 위의 최신 회의 반영 절입니다.**

- 상권분석은 관측 요약·요일/시간대 흐름·체류와 매장 유입·상권 진단 요약, **4개 섹션**에서 관측 사실과 해석 한계를 보여줍니다. 운영 지침은 회복전략으로 이동했으며 진단 화면에는 근거 확인과 회복전략 이동 기능을 남겼습니다.
- 현재 자료는 공유 가상 고깃집의 **매장 전면 1개 지점 · 17~23시 · 10분 단위**입니다. 분석 범위는 **한국시각 기준 이번 달 1일~오늘**로 고정하고 기간 선택과 필터 아래 비교 날짜 안내를 제거합니다. 증감에 필요한 이전 기간 비교는 겹치지 않는 같은 요일 구성으로 유지합니다. 자료가 없는 날짜를 0건으로 간주하거나 다른 달로 대체하지 않습니다. 과거의 3개 카메라 선택·08~20시·2시간 관측 도식 설명은 현재 화면에 적용하지 않습니다.
- 요일은 기본 단일 선택입니다. **복수 선택**을 체크했을 때만 여러 요일을 토글하며, 체크를 끄면 마지막으로 선택한 요일을 유지합니다. 시간대는 **17~19시 / 19~21시 / 21~23시** 중 접속 한국시각의 구간을 기본 선택하고 직접 바꿀 수 있습니다. 영업시간 외에는 전체 시간과 영업시간 외 안내를 보여주며 초기화도 현재 시각에 맞춥니다.
- 모든 상권분석 섹션의 위아래 간격을 매출진단과 일관되게 줄여 핵심 내용을 한 화면에서 확인하기 쉽도록 조정합니다. 기존 글꼴과 디자인은 유지합니다.
- `IM_MARKET_ANALYSIS.getRecoveryContext(period)`는 선택한 요일과 시간대의 분석 결과를 `IM_MARKET_DATA.recoveryContext(analysis)`로 전달합니다. 반환값은 `{ source, title, scope, findings, reason? }`이며 각 finding에 `{ id, title, evidence, action, caveat, status }`를 담습니다.
- 통행 집중·입장률 하락·체류 집중이라는 실제 계산 결과에 대응하는 점검 제안을 회복전략에서 매출 제안 다음에 보여줍니다. 이전 기간 비교가 불가능하면 하락 제안을 만들지 않으며, 현재 관측만으로 가능한 제안에는 비교 한계를 표시합니다. 현재 관측에 누락이 있으면 `insufficient`로 실행 제안을 보류하고, 관측 없음·통행 0건은 빈 findings와 이유를 반환합니다.
- 회복전략 안에 회복전략/사후관리 탭을 두고 제안별 실행 기록을 연결했습니다. 제안의 범위·근거·한계는 PDF의 다음 실행 항목에 포함됩니다. 기존 추가 대화 분석과 PDF 매출·지출 요약은 선택 기간의 가게 전체 기준이므로 개별 상권 필터의 결과와 구분합니다.
- 직접 주소 `#market`은 상권분석, `#recovery`는 회복전략, `#recovery/aftercare`는 사후관리 탭입니다. `#recovery`를 상권분석으로 돌리던 과거 연결은 대체되었습니다.
- 홈은 **접속 시간대에 맞는 행동 지침을 제공하는 예외**이며, 홈 배너는 좌측 메뉴에 맞춘 매출진단·상권분석·지원사업·회복전략 4개입니다. 우측 **iM비서**는 서비스 설명 FAQ 예시 3개와 짧은 답변을 제공합니다. 현재 외부 API 호출은 없으며 향후 백엔드도 FAQ 우선·무료 경량 AI 방향으로 구현하고 유료 고성능 AI는 사용하지 않습니다. 호스팅 비용까지 무료라고 확정한 것은 아닙니다.

원본 영상·카메라·영상 분석 엔진·실제 외부 AI는 계속 미연결입니다. 관측 수는 고유 방문자나 상권 전체 인구가 아니며, 제안은 원인 또는 매출 개선 효과를 확정하지 않습니다. 좌측 생성 자료 안내 박스를 제거했어도 관측 근거와 PDF의 생성 자료 고지는 유지합니다.

이번 통합본에서 다음 계산·UI 계약 검사를 실행해 통과했습니다. 현재 계산 검사는 32개 CCTV 조건 조합, 10분 집계·공유 영수증/입장 자료 정합성, 누락·0건·비교 불가·회복 제안 보류를 포함합니다.

```powershell
node prototype/tests/market-analysis.test.cjs
node prototype/tests/market-meeting-ui.test.cjs
```

최신 브라우저 검사는 `prototype/tests/market-analysis.browser.test.cjs`를 사용합니다. 아래 2026-09-13의 5개 섹션·72개 사례·화면 검증 기록은 당시 결과입니다.

---


기준일: 2026-09-13. 현재 결과는 정적 HTML 프론트엔드와 생성 집계입니다.

## 1. 회의 결정과 이번 구현 범위

- 독립된 **회복전략** 메뉴를 **상권분석**으로 변경합니다.
- 회복전략의 실행 내용은 향후 iM비서에서 구체화합니다.
- iM비서는 이번에 **회복전략 리포트**라는 이름만 반영합니다. 질문 예시·대화 답변·분석 입력·PDF 본문은 변경하지 않습니다.
- 상권분석은 CCTV 영상 분석 결과에서 얻는 통행·체류·매장 유입 인사이트가 핵심입니다.
- 화면은 반드시 필요한 5개 섹션으로 구성합니다. 실행 체크리스트, 실행 전후 효과, 금융상품 추천, 지도 반경 자리 표시용 화면은 새 메뉴에서 제외했습니다.

기존 매출진단 5·6번 섹션의 삭제·축소는 이번에 적용하지 않았습니다. 해당 대화는 설계 검토였으며 이번 실행 요청은 상권분석과 리포트 명칭 변경에 한정했습니다.

## 2. 5개 섹션의 목적과 기능

| 순서 | 섹션 | 사용자가 얻는 답 | 구현 기능 |
|---|---|---|---|
| 01 | 상권 관측 요약 | 선택한 구역의 움직임이 어떠한가? | 기간·지점·요일·비교 기준, 시간당 통행·체류율·입장률·평균 체류시간, 유효 구간과 관측 시간 |
| 02 | CCTV 관측 영역 | 영상의 어떤 움직임을 집계했는가? | 날짜·2시간 구간 선택, 통행선·체류 구역·매장 입구 도식 전환, 선택 구간 집계와 상세 보기 |
| 03 | 요일·시간대 흐름 | 어느 시간·요일에서 변화가 발생했는가? | 통행·체류율·입장률 차트 전환, 현재·이전 기간 비교 막대, 클릭·키보드 상세 탐색 |
| 04 | 체류와 매장 유입 | 통행이 머무름과 입장으로 얼마나 이어지는가? | 통행 기준의 두 행동 비율, 체류시간 분포, 카메라 기준 이동 방향 |
| 05 | 근거 기반 인사이트 | 관측에서 확인되는 사실과 한계는 무엇인가? | 통행 집중 시간·입장률 하락·체류 집중 시간, 해당 시간 관측으로 이동, 접힌 관측 품질·집계 기준, 전체 근거 집계 |

‘iM비서 열기’는 메뉴 이동만 수행합니다. 현재 조건의 자동 전달이나 새 회복전략 리포트 생성으로 오해하지 않도록 후속 개발 항목을 표시했습니다.

## 3. 실제 구현과 목업의 구분

**현재 구현:** 선택 조건에 맞는 생성 CCTV 집계 계산, 화면·그래프·상세 수치 일치, 반응형 화면, 기존 메뉴 통합, 리포트 명칭 변경과 PDF 다운로드.

**현재 미연결:** 실제 CCTV 영상·장비·스트림·영상 분석 엔진, 실제 지도, 카메라 간 동일인 중복 제거, POS 구매 연결, 개인별 이동 이력, 연령·성별·재방문 추정.

관측 도식은 원본 영상 재생 화면이 아닙니다. 한 개의 익명 경로를 통해 통행선 통과·10초 이상 체류·매장 출입선 안쪽 이동의 집계 기준을 설명합니다. 해당 2시간 구간의 모든 움직임을 재생한 결과로 표시하지 않습니다.

생성 자료 범위는 **2026.07.01~2026.09.02, 08~20시, 2시간 간격**입니다.

- CAM-01 매장 전면: 통행·체류·입장 관측 가능.
- CAM-02 인접 골목: 통행·체류 관측 가능, 매장 입구는 관측 범위 밖. 8/26 18~20시 누락, 8/31 16~18시 가림 시나리오 포함.
- CAM-03 시장 입구: 연결 전 상태의 예시. 수치와 인사이트를 생성하지 않습니다.

## 4. 지표와 데이터 품질 기준

1. 관측 수는 **건**으로 표시합니다. 개별 관측 단위이며 기간 전체의 고유 방문자 수나 상권 전체 인구가 아닙니다. 카메라 간 집계를 합산하지 않습니다.
2. 체류율 = 10초 이상 체류 관측 ÷ 통행 관측 × 100.
3. 입장률 = 매장 입장 관측 ÷ 통행 관측 × 100. 입구가 관측되지 않는 카메라에서는 계산하지 않습니다.
4. 체류와 입장은 통행을 기준으로 각각 계산합니다. 두 집계가 겹칠 수 있으므로 체류→입장의 연속 단계로 표현하거나 비율을 합하지 않습니다.
5. 기간 비율은 분자·분모 합계로 계산합니다. 구간별 비율을 단순 평균하지 않습니다.
6. 평균 체류시간 = 체류시간 합계 ÷ 10초 이상 체류한 관측 수. 생성 자료의 체류시간 분포는 10~29초·30~59초·60초 이상입니다.
7. 시간당 통행 = 유효 통행 합계 ÷ 유효 관측 시간. 시간대 막대는 해당 2시간 구간의 유효 관측 일평균, 요일 막대는 08~20시 전체가 관측된 날의 일평균입니다.
8. 누락·가림 구간은 0건으로 넣지 않고 제외합니다. 양쪽 기간의 예정 구간이 모두 유효할 때만 이전 기간 비교를 제공합니다. 따라서 자료 누락이 통행 감소로 오인되지 않습니다.
9. ‘같은 요일 구성’은 기간이 겹치지 않도록 선택 일수 이상의 최소 7일 배수만큼 이동합니다. 8월 전체는 6월 말 비교 자료가 없어 비교를 보류하며 최근 7일은 직전 7일과 비교할 수 있습니다.
10. 기준값이 0이면 증감률을 산정하지 않고, 분모가 없거나 관측 범위 밖이면 `—`로 표시합니다. 입장률 변화는 `%p`로 표시합니다.
11. 통행 집중·입장률 하락·체류 집중은 관측 사실에 대한 규칙 기반 해석입니다. 체류 이유, 구매 의도, 품절·메뉴 노출 등의 원인을 확정하지 않습니다.

## 5. 코드 구조와 추후 연결 계약

- `prototype/market-analysis-data.js`: 생성 CCTV 집계와 DOM에 의존하지 않는 계산 모듈.
- `prototype/market-analysis.js`: 5개 섹션, 필터 상태, 도식, 그래프, 근거 상세 창.
- `prototype/market-analysis.css`: 기존 카드·버튼·글꼴·색상 체계를 따르는 `ma-` 범위의 스타일.
- `prototype/meeting-ui.js`: `market` 메뉴와 공통 분석 기간 연결. `#recovery`는 `#market`으로 이동합니다.
- `prototype/report-pdf.js`: PDF 문서 제목만 변경.

계산 함수는 `IM_MARKET_DATA.analyze(period, filters, sourceRows)`입니다.

```js
period = { start: 'YYYY-MM-DD', end: 'YYYY-MM-DD' };
filters = {
  camera: 'front', // front / alley / gate
  dayType: 'all', // all / weekday / weekend
  comparison: 'previous' // previous / weekday
};
sourceRows = [{
  id, cameraId, date, slotId,
  status, // valid / missing / occluded
  plannedMinutes, analyzedMinutes,
  passers, dwellers, entrants,
  directionA, directionB,
  dwellShort, dwellMedium, dwellLong, dwellSeconds,
  sourceType
}];
```

실제 연동 시 카메라별 관측 가능 항목·영역 좌표·방향 정의, 실제 분석 완료 시각, 누락·가림 판정, 관측 단위의 중복 제거 기준을 확정해야 합니다. 현재 코드는 유효한 구간을 120분 완전 관측으로 정의합니다. 부분 시간 관측을 지원하려면 실제 `analyzedMinutes`를 사용하는 산식과 비교 기준을 추가해야 합니다.

홈과 iM비서의 기존 CCTV·POS 고정 시나리오는 이번 범위에서 새 CCTV 생성 자료로 교체하지 않았습니다. 특히 iM비서는 사용자의 명시적 요청에 따라 명칭만 바꿨으므로 새 상권분석의 필터·결과를 답변이나 PDF에 자동 반영하지 않습니다.

## 6. 검증

```powershell
node prototype/tests/market-analysis.test.cjs
node prototype/tests/meeting-preview.test.cjs
node prototype/tests/meeting-revisions.test.cjs
node prototype/login-preview/login-preview.test.cjs
# Playwright가 설치된 경로를 NODE_PATH에 설정한 뒤
node prototype/tests/market-analysis.browser.test.cjs
```

- 72개 기간·지점·요일·비교 조합의 합계·비율·체류 분포·이동 방향·날짜 경계 검사.
- 누락·가림·관측 범위 밖·연결 전·빈 날짜·0건 상태 검사.
- 메뉴와 구주소 이동, 기간·지점 선택, 도식·지표 전환, 상세·페이지 이동, 인사이트 근거 이동 검사.
- iM비서 생성 버튼·파일명 변경 및 실제 PDF 다운로드 검사. 로컬 변경 전 사본과 비교하여 질문·답변·PDF 본문을 보존했는지도 검사했습니다.
- 360/390/768/1366/1920px에서 5개 섹션의 가로 넘침·세로 접근성 검사. 기본 동작 줄이기와 `IM_TEST_MOTION=normal` 환경 모두 검사했습니다.
- 검증 이미지는 `tmp/market-analysis-qa/`에 저장합니다.
