# iM파트너 데모 MVP 시연 품질 보완 기록

> 개정 안내(2026-09-08): 아래 1~7절은 2026-09-01 당시 `prototype/main-screen.before-meeting.html`과 `prototype/mock-data.js`를 기준으로 수행한 과거 정적 Mock MVP 기록입니다. 해당 절의 ‘현재’는 모두 2026-09-01 당시 상태를 뜻하며 현행 `prototype/main-screen.html`의 구현·수치·시험 결과로 사용하지 않습니다. 2026-09-08 현행 결과는 0절을 기준으로 확인합니다.

- 문서 상태: 2026-09-01 과거 QA 기록 보존 / 2026-09-08 현행 정적 시연 QA 갱신
- 대상 서비스: iM파트너
- 과거 기준 화면: `prototype/main-screen.before-meeting.html`, `prototype/mock-data.js`
- 현행 기준 화면: `prototype/main-screen.html`, `prototype/meeting-data.js`, `prototype/meeting-ui.js`, `prototype/report-pdf.js`
- 공통 원칙: 생성 데이터 기반 시연과 실제 연동을 구분하고, 계산하지 않은 효과·점수·확정 원인을 표시하지 않음

---

## 0. 2026-09-08 현행 QA 결론

현행 `prototype/main-screen.html`에는 생성 자료를 이용한 7개 화면과 CCTV·POS 회복 플랜이 하나의 메인 흐름으로 통합되어 있습니다. 실제 CCTV 영상 분석이나 POS 기기·DB 연동이 아니라, `meeting-data.js`의 결정론적 생성 레코드와 계산 함수를 `meeting-ui.js`가 표시하는 로컬 프론트엔드 시연입니다.

### 0-1. 현행 파일과 역할

| 파일 | 현행 역할 | 구현 한계 |
|---|---|---|
| `prototype/main-screen.html` | 대시보드·상권·매출/지출·회복 플랜·정책·AI 비서·프로필의 공통 진입 화면 | 서버·인증·외부 API 없음 |
| `prototype/meeting-data.js` | POS·지출·매입·상권·CCTV 형식의 생성 레코드와 기간별 계산, CCTV·POS 회복 시나리오 | 실제 매장·장비·카드사·통신사 자료가 아님 |
| `prototype/meeting-ui.js` | 7개 화면, iM챗봇, 회복 퍼널·행동 시안, 정책·프로필 상태를 렌더링 | 실행·프로필 상태는 브라우저 메모리에만 있으며 새로고침 시 초기화 |
| `prototype/report-pdf.js` | 선택 기간의 생성 POS·지출·상권 분석을 브라우저에서 실제 PDF 파일로 생성 | 서버 저장·권한 관리가 없고 CCTV·POS 회복 퍼널 결과와 실행 상태는 PDF에 포함하지 않음 |

### 0-2. 현행 회복 플랜 구현 상태

```text
CCTV 형식의 익명 통행·체류·입장 생성 집계
    ↓
POS 형식의 유효 결제 건수·순매출 생성 집계
    ↓
유입률·추정 구매전환율·객단가 계산
    ↓
고정 생성 시나리오의 요인 후보와 실행 행동
    ↓
브라우저 메모리의 실행 상태 변경
    ↓
7일 후 결과는 집계 대기
```

- 메인 대시보드와 회복 플랜 화면에서 `통행자 → 체류자 → 입장객 → 유효 결제 → 순매출`을 확인할 수 있습니다.
- 생성 시나리오의 기회 시간대는 `18~20시`로 미리 정해져 있으며, 자동 선정 산식과 임계값·규칙 버전은 아직 없습니다.
- `310명` 통행, `21명` 입장, 매장 유입률 `6.8%` 등 시나리오 내부 계산은 일치합니다. 실제 관측값은 아닙니다.
- 회복 행동을 시작하면 같은 브라우저 메모리에서만 상태가 바뀌며 DB에 저장되지 않습니다.
- 7일 후 비교는 `waiting` 상태이고 후속 지표가 없습니다. 성공·개선 효과·인과관계를 표시하지 않습니다.
- 지도는 배치 예시이며 카카오맵·동종업종·행사 API가 연결되지 않았습니다.
- 실제 CCTV 영상 분석, POS 가져오기, DB·인증·권한, 실제 정책·뉴스·날씨·행사, 외부 AI와 상담원 연결은 구현되지 않았습니다.

### 0-3. 2026-09-08 시험 결과

| 시험 | 결과 | 확인 범위 | 완료로 볼 수 없는 범위 |
|---|---|---|---|
| `prototype/tests/meeting-preview.test.cjs` | 논리 검사 13개 통과 | 생성 자료 합계·기간·7개 화면·iM챗봇·CCTV/POS 계산·회복 상태·PDF 다운로드 주소·프로필 이스케이프 | 실제 DB·API·장비·외부 서비스 |
| `prototype/tests/visual-preview.test.cjs` | 브라우저 검사 통과 | 설치된 Chrome/Edge 기반 폰트·메뉴·화면 이동·PDF·정책 모달·1920×1080·1366×768·1024×768·390×844 반응형과 가로 넘침 | 실제 데이터 정확도·보안·운영 환경 |

이 통과 기록은 로컬 생성 자료 기반 정적 시안의 회귀 결과입니다. 실제 CCTV·POS 연동이나 전체 T-01~T-52 인수 완료를 뜻하지 않습니다.

### 0-4. 현행 발표 직전 수동 QA 체크리스트

- [ ] 첫 화면과 회복 플랜에 `생성 데이터 기반 시연`이 식별되는가
- [ ] 좌측 메뉴에서 7개 화면으로 이동하고 대시보드로 돌아올 수 있는가
- [ ] 월·최근 7일·직접 기간 선택 시 표시 기간과 KPI·그래프·일반 매출·상권 질문의 iM챗봇 답변이 함께 바뀌고, CCTV·POS 회복 질문은 고정 시나리오를 유지하는가
- [ ] 회복 플랜에 `18~20시`, 통행자 `310명`, 입장객 `21명`, 유입률 `6.8%`가 같은 생성 시나리오로 표시되는가
- [ ] 지표 설명에서 유입률·추정 구매전환율·객단가의 분자와 분모를 구분하는가
- [ ] `실행 기록 시안 시작하기` 후 상태가 바뀌고 DB 미저장 안내가 보이는가
- [ ] 7일 후 결과가 `데이터 집계 대기`로 남고 개선 수치를 미리 표시하지 않는가
- [ ] 지도 영역에 `실제 지도 아님`과 카카오맵 미연결 상태가 보이는가
- [ ] iM챗봇의 CCTV 질문 답변이 회복 플랜과 같은 수치를 사용하고, 실제 CCTV·POS가 아닌 생성 시나리오라고 설명하는가
- [ ] AI 비서에서 분석 후 PDF를 만들 때 실제 다운로드 링크가 생기는가
- [ ] PDF에 선택 기간과 생성 자료·외부 미연결 한계가 표시되는가
- [ ] PDF를 CCTV 회복 퍼널·실행 상태까지 포함한 종합 보고서라고 설명하지 않는가
- [ ] 새로고침 후 실행·프로필 상태가 초기화되는가
- [ ] 1920×1080, 1366×768, 1024×768, 390×844에서 가로 스크롤·잘림·콘솔 오류가 없는가

---

## 1. 2026-09-01 당시 QA 결론

2026-09-01 당시 P0 MVP는 다음 흐름을 하나의 정적 화면에서 시연할 수 있었습니다.

```text
사용자·매장 확인
    ↓
KPI 확인
    ↓
매출·유동인구·카드소비 비교
    ↓
매출 변화 요인 후보 확인
    ↓
골목상권 회복 플랜 확인
    ↓
AI 비서 질문·답변
```

발표에 직접 영향을 주는 문제를 우선 수정했으며, 신규 P1 화면은 추가하지 않았습니다. 당시에는 기능 수보다 핵심 흐름의 안정성과 설명 가능성을 우선했습니다.

---

## 2. 2026-09-01 당시 QA에서 확인한 주요 문제와 조치

| 확인 항목 | 확인 결과 | 조치 |
|---|---|---|
| 좌측 메뉴 | 메뉴 접기·펼치기는 동작했으나 이동 대상 확인이 필요함 | 대시보드·자금 흐름·금융 체온·회복 플랜·정책 영역으로 연결 |
| 상세 버튼 | 일부 버튼이 화면상 버튼이지만 동작하지 않음 | 차트·인사이트·자금 흐름·정책 버튼을 AI 비서 질문으로 연결 |
| 기간 선택 | 선택은 가능하지만 데이터가 실제로 바뀌지 않음 | 최근 2년 고정 안내를 표시하고 선택값을 원래 상태로 복원 |
| 작은 노트북 | 우측 AI 패널이 본문 폭을 줄일 가능성이 있음 | 960px 이하에서 AI 패널을 오버레이 방식으로 표시 |
| AI 질문 | 일부 질문은 기본 답변으로만 처리됨 | 매출·시간대·고객층·실행·자금 흐름 질문별 답변 추가 |
| 수치 일관성 | 화면 일부 수치가 HTML에 직접 입력되어 교체 시 누락 가능 | 주요 시연 수치를 `prototype/mock-data.js`에서 관리 |
| 발표 오해 가능성 | 요인 후보와 금융 효과가 실제 보장값으로 오해될 수 있음 | 더미 데이터·참고용 시나리오 안내 유지 |

---

## 3. 2026-09-01 당시 반영한 UI 보완

### 3-1. KPI 우선순위

- 매출·카드소비·유동인구 KPI의 테두리와 강조를 강화했습니다.
- 금융 체온은 보조 지표로 구분했습니다.
- 카드에 마우스를 올렸을 때 가벼운 상승 피드백을 추가했습니다.

### 3-2. 버튼 피드백과 이동

- 상세 버튼에 실제 동작을 연결했습니다.
- 좌측 메뉴는 관련 영역으로 부드럽게 이동합니다.
- AI 리포트 메뉴는 우측 AI 비서 패널을 엽니다.
- 키보드 포커스가 보이도록 `focus-visible` 스타일을 추가했습니다.

### 3-3. 반응형

- 1920×1080과 일반 노트북에서는 기존 3열·2열 구조를 유지합니다.
- 1120px 이하에서는 주요 카드가 1열로 전환됩니다.
- 960px 이하에서는 AI 비서가 본문을 압축하지 않고 우측 오버레이로 열립니다.
- 760px 이하에서는 기존 모바일 메뉴·채팅 패널 동작을 유지합니다.

### 3-4. 기간 선택

2026-09-01 당시 Mock Data는 `최근 2년` 기준으로 고정되어 있었습니다. 다른 기간을 선택하면 화면 하단에 시연 데이터 고정 안내가 나타나고, 선택값은 `최근 2년`으로 돌아갔습니다. 이 동작은 현행 기간 선택 기능의 설명이 아닙니다.

---

## 4. 2026-09-01 당시 AI 비서 데모 품질

2026-09-01 당시 우측 ‘AI 비서’는 외부 AI가 아니라 Mock Data 기반의 규칙형 답변이었습니다. 현행 화면에서는 이 역할을 모든 탭의 `iM챗봇`과 별도 `AI 비서` 화면으로 구분합니다. 당시 대표 질문은 다음 5가지로 고정했습니다.

1. `현재 매출이 왜 떨어졌나요?`
2. `어느 시간대를 공략해야 하나요?`
3. `어떤 고객층을 노려야 하나요?`
4. `당장 실행할 수 있는 방법은 무엇인가요?`
5. `자금 흐름에서 무엇을 확인해야 하나요?`

답변은 다음 구조를 따릅니다.

```text
근거 또는 요인 후보
    ↓
핵심 인사이트
    ↓
추천 행동
```

당시 매출 질문에는 카드소비 `-8.1%`, 유동인구 `+3.2%`, `12~14시` 소비 전환 공백을 함께 보여주고, 대표 메뉴·홍보 노출 점검으로 연결했습니다. 이 수치는 현행 `meeting-data.js`의 표시값이 아닙니다.

---

## 5. 2026-09-01 당시 신규 P1 구현 판단

이번 QA에서는 신규 P1 화면을 추가하지 않았습니다.

- 매출·지출 상세: 당시 요약 카드만으로도 P0 설명을 방해하지 않음
- 금융 체온계 상세: 당시 KPI와 주의 상태 표시로 최소 설명 가능
- 정책 상세: 당시 요약 카드와 AI 질문 연결로 범위 유지
- AI 리포트: 회복 플랜과 AI 비서가 P0의 핵심 메시지를 이미 전달함

P1을 추가하면 화면 이동과 설명 시간이 늘어나므로, 실제 발표 리허설에서 P0 흐름이 안정된 뒤 필요성을 다시 판단합니다.

---

## 6. 2026-09-01 당시 발표 직전 수동 QA 체크리스트

아래 체크리스트는 `main-screen.before-meeting.html` 보존본에만 적용합니다. 현행 발표에는 0-4절 체크리스트를 사용합니다.

- [ ] 새로고침 후 사용자명 `이소현`과 매장 조건이 표시되는가
- [ ] `최근 매출 -12.4%`, `카드소비 -8.1%`, `유동인구 +3.2%`가 표시되는가
- [ ] 그래프와 `12~14시 소비 전환 공백`이 보이는가
- [ ] `회복 플랜 확인` 클릭 시 회복 플랜으로 이동하는가
- [ ] 좌측 메뉴 접기 후 아이콘만 남는가
- [ ] 좌측 메뉴를 눌렀을 때 관련 영역으로 이동하는가
- [ ] 우측 AI 비서가 열리고 닫히는가
- [ ] 대표 질문 5개가 모두 일관된 답변을 반환하는가
- [ ] `플랜 자세히 보기`가 AI 비서 질문·답변으로 연결되는가
- [ ] 하단에 더미 데이터와 참고용 금융 효과 안내가 보이는가
- [ ] 1920×1080, 일반 노트북, 태블릿 폭에서 가로 스크롤이 생기지 않는가

---

## 7. 2026-09-01 당시 잔여 위험

- 브라우저별 폰트 렌더링 차이는 발표 PC에서 최종 확인해야 합니다.
- 당시 기간 선택은 실제 데이터 전환이 아니라 고정 데이터 안내였습니다.
- 당시 우측 AI 비서는 실제 자연어 이해가 아닌 키워드 규칙형 답변이었습니다.
- 본선 시연 전 실제 허용 집계 결과로 Mock Data를 교체해야 합니다.

## 8. 2026-09-03 목표 변경 당시 재검증 대상

이전 HTML은 `prototype/main-screen.before-meeting.html`, 이전 데이터는 `prototype/mock-data.js`에 보존했습니다. 현행 `prototype/main-screen.html`은 개정 화면입니다. 아래 표는 2026-09-03 당시 정리한 재검증 요구이며, 2026-09-08의 실제 정적 시안 결과는 0절을 따릅니다.

| 변경 요구 | 재시험 |
|---|---|
| 생성/실제 데이터 구분 | POS 포함 생성 시연 표시, 실자료와 혼동·자동 대체 방지, KPI·그래프·요인·플랜·답변 일치 |
| 기간·POS | 기간 변경, 시간 경계, 취소·환불·중복, 품목·방문 지표 구분 |
| CCTV·POS 퍼널 | 익명 통행·체류·입장 집계, 결제·매출 시간 정렬, 유입률·추정 구매전환율·객단가 계산 |
| 진단·실행 비교 | 임계값·매출 기회 시간대 규칙, 요인 후보→행동 연결, 7일 후 동일 조건 비교 |
| 체온계·이슈 | 대시보드 배치, 근거 없는 점수 없음, 배너·날씨·캘린더 장애 |
| iM챗봇 | 모든 탭 접근, 크기 조절, 근거 질문, 답변 불가·상담 안내 |
| AI 비서 | 대화형 분석, 파일 생성 상태, 실제 PDF·다운로드·권한 |
| 지도·상권 보조 | 반경·업종·주소, 경쟁 정보 출처, 행사·대목 근거, 핵심 퍼널 대체 금지 |
| 정책·프로필 | 원문 자격·미상 처리·추천 개수·점수 근거·접근권한 |

기존 ‘인터넷 없이 시연 가능’은 정적 MVP에 해당합니다. 지도·정책·날씨 등 연동 후에는 외부 장애 처리까지 검증합니다.

신규 목표 테스트는 [09_test-plan.md](09_test-plan.md)의 T-01~T-52에 정의했습니다. 2026-09-08에는 정적 시안의 논리 검사 13개와 브라우저 검사가 통과했지만, 실제 연동과 권한·데이터 품질을 포함한 전체 T-01~T-52 인수가 끝난 것은 아닙니다.

## 9. 2026-09-03 당시 혼합형 회복 플랜 구현 후 데모 QA 기준

- 생성 데이터라는 표시가 첫 진입과 회복 플랜 화면에서 식별되어야 합니다.
- `통행자 → 체류자 → 입장객 → 결제 → 매출`의 순서와 단위가 한 번에 이해되어야 합니다.
- 추천 시간대의 수치와 퍼널·차트·진단·iM챗봇 답변이 같은 분석 실행을 사용해야 합니다.
- 요인 후보와 행동을 구분하고, 행동의 기대 효과를 보장값처럼 표시하지 않아야 합니다.
- 지도·상권 영역은 ‘보조 근거’임을 시각적으로 구분해야 합니다.
- 원본 영상·얼굴·개인 식별 정보가 화면과 네트워크 응답에 없어야 합니다.
- 실행 행동을 기록한 뒤 7일이 지나지 않았거나 비교 자료가 부족하면 완료 수치를 보여주지 않아야 합니다.
- 2026-09-08에는 생성 CCTV·POS 결과 화면이 메인 화면에 통합되었지만, 이는 로컬 생성 시나리오의 프론트엔드 통과 근거일 뿐 실제 CCTV·POS 분석·연동 완료 근거가 아닙니다.
