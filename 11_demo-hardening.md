# iM 파트너 데모 MVP 시연 품질 보완 기록

> 개정 안내: 아래 1~7절은 기존 정적 Mock MVP의 시연 품질 보완 기록입니다. 2026-09-03에는 문서만 개정했으며 회의 추가 기능을 구현하거나 새 UI 시험을 통과시킨 기록이 아닙니다.

- 문서 상태: 1차 데모 QA 및 보완 완료
- 대상 서비스: iM 파트너
- 기준 화면: `prototype/main-screen.html`
- 원칙: 기존 P0 흐름 유지, 외부 연결 없음, 대규모 기능 추가 없음

---

## 1. QA 결론

현재 P0 MVP는 다음 흐름을 하나의 정적 화면에서 시연할 수 있습니다.

```text
사용자·매장 확인
    ↓
KPI 확인
    ↓
매출·유동인구·카드소비 비교
    ↓
매출 변화 요인 후보 확인
    ↓
골목상권 회복 플랜 확인
    ↓
AI 비서 질문·답변
```

발표에 직접 영향을 주는 문제를 우선 수정했으며, 신규 P1 화면은 추가하지 않았습니다. 현재는 기능 수보다 핵심 흐름의 안정성과 설명 가능성이 더 중요합니다.

---

## 2. QA에서 확인한 주요 문제와 조치

| 확인 항목 | 확인 결과 | 조치 |
|---|---|---|
| 좌측 메뉴 | 메뉴 접기·펼치기는 동작했으나 이동 대상 확인이 필요함 | 대시보드·자금 흐름·금융 체온·회복 플랜·정책 영역으로 연결 |
| 상세 버튼 | 일부 버튼이 화면상 버튼이지만 동작하지 않음 | 차트·인사이트·자금 흐름·정책 버튼을 AI 비서 질문으로 연결 |
| 기간 선택 | 선택은 가능하지만 데이터가 실제로 바뀌지 않음 | 최근 2년 고정 안내를 표시하고 선택값을 원래 상태로 복원 |
| 작은 노트북 | 우측 AI 패널이 본문 폭을 줄일 가능성이 있음 | 960px 이하에서 AI 패널을 오버레이 방식으로 표시 |
| AI 질문 | 일부 질문은 기본 답변으로만 처리됨 | 매출·시간대·고객층·실행·자금 흐름 질문별 답변 추가 |
| 수치 일관성 | 화면 일부 수치가 HTML에 직접 입력되어 교체 시 누락 가능 | 주요 시연 수치를 `prototype/mock-data.js`에서 관리 |
| 발표 오해 가능성 | 요인 후보와 금융 효과가 실제 보장값으로 오해될 수 있음 | 더미 데이터·참고용 시나리오 안내 유지 |

---

## 3. 실제 반영한 UI 보완

### 3-1. KPI 우선순위

- 매출·카드소비·유동인구 KPI의 테두리와 강조를 강화했습니다.
- 금융 체온은 보조 지표로 구분했습니다.
- 카드에 마우스를 올렸을 때 가벼운 상승 피드백을 추가했습니다.

### 3-2. 버튼 피드백과 이동

- 상세 버튼에 실제 동작을 연결했습니다.
- 좌측 메뉴는 관련 영역으로 부드럽게 이동합니다.
- AI 리포트 메뉴는 우측 AI 비서 패널을 엽니다.
- 키보드 포커스가 보이도록 `focus-visible` 스타일을 추가했습니다.

### 3-3. 반응형

- 1920×1080과 일반 노트북에서는 기존 3열·2열 구조를 유지합니다.
- 1120px 이하에서는 주요 카드가 1열로 전환됩니다.
- 960px 이하에서는 AI 비서가 본문을 압축하지 않고 우측 오버레이로 열립니다.
- 760px 이하에서는 기존 모바일 메뉴·채팅 패널 동작을 유지합니다.

### 3-4. 기간 선택

현재 Mock Data는 `최근 2년` 기준으로 고정되어 있습니다. 다른 기간을 선택하면 화면 하단에 시연 데이터 고정 안내가 나타나고, 선택값은 `최근 2년`으로 돌아옵니다. 실제 기간별 분석은 백엔드와 데이터 연결 이후 구현합니다.

---

## 4. AI 비서 데모 품질

현재 AI 비서는 외부 AI가 아니라 Mock Data 기반의 규칙형 답변입니다. 대표 질문은 다음 5가지로 고정했습니다.

1. `현재 매출이 왜 떨어졌나요?`
2. `어느 시간대를 공략해야 하나요?`
3. `어떤 고객층을 노려야 하나요?`
4. `당장 실행할 수 있는 방법은 무엇인가요?`
5. `자금 흐름에서 무엇을 확인해야 하나요?`

답변은 다음 구조를 따릅니다.

```text
근거 또는 요인 후보
    ↓
핵심 인사이트
    ↓
추천 행동
```

예를 들어 매출 질문에는 카드소비 `-8.1%`, 유동인구 `+3.2%`, `12~14시` 소비 전환 공백을 함께 보여주고, 대표 메뉴·홍보 노출 점검으로 연결합니다.

---

## 5. 신규 P1 구현 판단

이번 QA에서는 신규 P1 화면을 추가하지 않았습니다.

- 매출·지출 상세: 현재 요약 카드만으로도 P0 설명을 방해하지 않음
- 금융 체온계 상세: 현재 KPI와 주의 상태 표시로 최소 설명 가능
- 정책 상세: 현재 요약 카드와 AI 질문 연결로 범위 유지
- AI 리포트: 회복 플랜과 AI 비서가 P0의 핵심 메시지를 이미 전달함

P1을 추가하면 화면 이동과 설명 시간이 늘어나므로, 실제 발표 리허설에서 P0 흐름이 안정된 뒤 필요성을 다시 판단합니다.

---

## 6. 발표 직전 수동 QA 체크리스트

- [ ] 새로고침 후 사용자명 `이소현`과 매장 조건이 표시되는가
- [ ] `최근 매출 -12.4%`, `카드소비 -8.1%`, `유동인구 +3.2%`가 표시되는가
- [ ] 그래프와 `12~14시 소비 전환 공백`이 보이는가
- [ ] `회복 플랜 확인` 클릭 시 회복 플랜으로 이동하는가
- [ ] 좌측 메뉴 접기 후 아이콘만 남는가
- [ ] 좌측 메뉴를 눌렀을 때 관련 영역으로 이동하는가
- [ ] 우측 AI 비서가 열리고 닫히는가
- [ ] 대표 질문 5개가 모두 일관된 답변을 반환하는가
- [ ] `플랜 자세히 보기`가 AI 비서 질문·답변으로 연결되는가
- [ ] 하단에 더미 데이터와 참고용 금융 효과 안내가 보이는가
- [ ] 1920×1080, 일반 노트북, 태블릿 폭에서 가로 스크롤이 생기지 않는가

---

## 7. 잔여 위험

- 브라우저별 폰트 렌더링 차이는 발표 PC에서 최종 확인해야 합니다.
- 현재 기간 선택은 실제 데이터 전환이 아니라 고정 데이터 안내입니다.
- AI 비서는 실제 자연어 이해가 아닌 키워드 규칙형 답변입니다.
- 본선 시연 전 실제 허용 집계 결과로 Mock Data를 교체해야 합니다.

## 8. 2026-09-03 목표 변경 후 재검증 대상

기존 HTML·디자인·데이터·영상은 그대로 보존했습니다. 아래 항목은 구현 후 재시험합니다.

| 변경 요구 | 재시험 |
|---|---|
| 생성/실제 데이터 구분 | POS 포함 생성 시연 표시, 실자료와 혼동·자동 대체 방지, KPI·그래프·요인·플랜·답변 일치 |
| 기간·POS | 기간 변경, 시간 경계, 취소·환불·중복, 품목·방문 지표 구분 |
| 체온계·이슈 | 대시보드 배치, 근거 없는 점수 없음, 배너·날씨·캘린더 장애 |
| 챗봇 | 모든 탭 접근, 크기 조절, 근거 질문, 답변 불가·상담 안내 |
| AI 비서 | 대화형 분석, 파일 생성 상태, 실제 PDF·다운로드·권한 |
| 회복 지도 | 반경·업종·주소, 경쟁 정보 출처, 행사·대목 근거 |
| 정책·프로필 | 원문 자격·미상 처리·추천 개수·점수 근거·접근권한 |

기존 ‘인터넷 없이 시연 가능’은 정적 MVP에 해당합니다. 지도·정책·날씨 등 연동 후에는 외부 장애 처리까지 검증합니다.

신규 목표 테스트는 [09_test-plan.md](09_test-plan.md)의 T-01~T-45에 정의했으며 현재 미실행입니다.
