# iM파트너 MVP 점검 및 구현 결과

> 개정 안내: 이 문서의 기존 1~12절은 정적 P0 MVP를 점검·구현했던 당시 기록입니다. 최신 목표는 01~09번 문서를 따르며, 2026-09-04 확정한 회복 플랜은 CCTV·POS 핵심 + 지도·상권 보조 근거의 혼합형입니다. 아래 과거 우선순위·구현 완료·테스트 결과를 새 기능의 완료 근거로 사용하지 않습니다.

- 문서 상태: 1차 MVP 점검 완료
- 기준일: 2026-09-01
- 대상 서비스: iM파트너
- 목적: 공모전 시연을 위한 최소 기능과 구현 상태 확정

---

## 1. 점검 결론

현재 저장소에서 실제로 실행할 수 있는 결과물은 `prototype/main-screen.html` 기반의 정적 웹 프로토타입입니다.

현재 프로토타입은 다음의 핵심 흐름을 시연할 수 있습니다.

```text
이소현의 가게·상권 확인
        ↓
핵심 KPI 확인
        ↓
유동인구·카드소비 비교
        ↓
매출 변화 요인 후보 확인
        ↓
골목상권 회복 플랜 확인
        ↓
AI 비서에게 추가 질문
```

대회 시연을 위해서는 여러 기능을 동시에 확장하기보다, 위 흐름이 빠르고 안정적으로 동작하는 상태를 먼저 완성하는 것이 적절합니다.

---

## 2. 현재 프로젝트 상태

### 2-1. 확인된 파일

- `01_prd.md` ~ `09_test-plan.md`: 기획·기능·화면·데이터·API·테스트 명세
- `README.md`: 프로젝트 개요와 실행 방법
- `prototype/main-screen.html`: 현재 실행 가능한 웹 화면
- `prototype/mock-data.js`: 화면에서 사용하는 시연용 Mock Data

### 2-2. 현재 구현된 내용

- iM파트너 서비스명과 이소현 사용자 표시
- 대구 서문시장·음식점·최근 2년 분석 조건 표시
- 매출·카드소비·유동인구·금융 체온 KPI 표시
- 유동인구와 카드소비 비교 그래프 표시
- 매출 변화 요인 후보와 소비 전환 공백 표시
- 골목상권 회복 플랜 표시
- 추천 시간대·고객층·운영 행동·참고용 금융 효과 표시
- 좌측 파트너 메뉴 접기·펼치기
- 메뉴 선택 시 현재 화면의 관련 영역으로 이동
- 우측 AI 비서 채팅창 접기·펼치기
- 정해진 질문에 대한 로컬 더미 답변
- 모바일 화면 대응
- 화면 내 시연용 데이터와 참고용 금융 시나리오 안내

### 2-3. 아직 구현되지 않은 내용

- Django 백엔드 서버
- MySQL 데이터베이스
- 실제 카드사·통신사 데이터 연동
- 실제 CSV 분석 기능
- 실제 API 호출
- 외부 AI 모델 또는 AI API 연동
- 로그인·권한 관리
- 여러 URL을 사용하는 별도 화면 라우팅
- 영수증 OCR
- n8n 자동화와 알림 발송
- 실제 금융상품 신청·심사·대출 실행

---

## 3. MVP 우선순위

### P0 — 공모전 시연에 반드시 필요한 기능

| 기능 | 현재 상태 | 판단 기준 |
|---|---|---|
| 시연용 사용자·가게·분석 조건 표시 | 구현 완료 | 발표 시작 시 서비스 맥락을 즉시 설명할 수 있어야 함 |
| 핵심 KPI 카드 | 구현 완료 | 매출·카드소비·유동인구 변화를 한눈에 보여줌 |
| 유동인구·카드소비 비교 그래프 | 구현 완료 | 핵심 분석 근거를 시각적으로 설명함 |
| 매출 변화 요인 후보 | 구현 완료 | 확정적 원인이 아닌 근거 기반 후보로 표시함 |
| 골목상권 회복 플랜 | 구현 완료 | 인사이트를 실제 운영 행동으로 연결하는 차별화 기능 |
| 로컬 AI 비서 더미 채팅 | 구현 완료 | 발표 중 질문·답변 장면을 안정적으로 시연함 |
| 좌측 메뉴·우측 AI 패널 제어 | 구현 완료 | 화면 구조와 주요 기능 접근성을 보여줌 |
| 메뉴 선택에 따른 관련 영역 이동 | 구현 완료 | 단일 HTML 안에서 기본 사용자 흐름을 끊김 없이 진행함 |
| 시연용 데이터·참고용 결과 표시 | 구현 완료 | 실제 데이터·금융 결과로 오해하는 것을 방지함 |

### P1 — 있으면 경쟁력이 높아지는 기능

| 기능 | 현재 상태 | 구현 방향 |
|---|---|---|
| 매출·지출 상세 분석 | 요약 카드 부분 구현 | 거래내역과 비용 구조 화면 추가 |
| 월간 자금 흐름 | 요약 카드 부분 구현 | 입력값 기반 예상 잔액과 비용 표시 |
| 골목금융 체온계 상세 | KPI 부분 구현 | 업종 평균 대비 비교 화면 추가 |
| 정책·지원사업 상세 추천 | 요약 카드 부분 구현 | 조건·신청기간·출처를 상세 표시 |
| AI 리포트 별도 화면 | 미구현 | P0 분석 결과를 발표용 리포트로 정리 |
| 허용된 실제 집계·분석 결과 교체 | 미구현 | 원천파일을 저장하지 않고 결과값만 교체 |

### P2 — 시간이 남을 때 검토할 기능

- 사용자 로그인과 권한 관리
- Django·DRF·MySQL 실제 연결
- 실제 AI 모델 또는 외부 AI API 연결
- 영수증 업로드 및 OCR
- n8n 기반 정책·날씨·행사 자동 수집
- 이메일·문자·카카오 알림
- 실제 금융상품 신청·심사·대출 실행

---

## 4. 최종 발표 사용자 시나리오

인증과 매장 선택은 현재 범위에서 제외하고, 시연용 사용자와 매장이 이미 선택된 상태로 시작합니다. 이렇게 해야 로그인 오류나 데이터 선택 오류가 발표를 방해하지 않습니다.

### 대표 시나리오

1. `iM파트너` 화면에서 사용자 `이소현`과 `대구 서문시장·음식점` 조건을 확인합니다.
2. 핵심 KPI에서 최근 매출 `-12.4%`, 카드소비 `-8.1%`, 유동인구 `+3.2%`를 확인합니다.
3. 비교 그래프에서 유동인구는 유지·증가했지만 카드소비가 상대적으로 낮은 구간을 설명합니다.
4. `12~14시 소비 전환 공백`을 매출 변화의 **요인 후보**로 설명합니다.
5. `회복 플랜 확인`을 눌러 해당 공백을 개선하기 위한 실행 행동을 확인합니다.
6. 집중 시간대, 추천 고객층, 추천 운영 행동을 설명합니다.
7. `+650,000원`은 확정 수익이 아니라 비교지표 기반 참고용 자금 흐름 시나리오임을 설명합니다.
8. 우측 AI 비서를 열고 `회복 플랜 알려줘` 또는 `매출 감소 요인 후보`를 입력합니다.
9. 화면에 표시된 더미 답변으로 분석 근거와 대응 방향을 다시 확인합니다.

### 발표 핵심 문장

> iM파트너는 사람이 줄었다고 단정하지 않고, 유동인구와 카드소비가 어긋나는 시간대를 찾아 소상공인이 바로 실행할 수 있는 회복 플랜으로 연결합니다.

---

## 5. 화면 구조

```text
iM파트너
├── 좌측 파트너 메뉴
│   ├── 대시보드
│   ├── 매출·지출 분석
│   ├── 골목금융 체온계
│   ├── 골목상권 회복 플랜
│   ├── 정책·지원사업
│   └── AI 리포트
├── 상단 헤더
│   ├── 서비스명
│   ├── 분석 상태
│   ├── AI 비서 열기
│   └── 사용자명
├── 메인 대시보드
│   ├── 분석 조건
│   ├── 핵심 KPI
│   ├── 핵심 인사이트
│   ├── 유동인구·카드소비 그래프
│   ├── 골목상권 회복 플랜
│   ├── 자금 흐름 요약
│   └── 정책·지원사업 요약
└── 우측 AI 비서 패널
    ├── 분석 조건
    ├── 대화 내용
    ├── 추천 질문
    └── 더미 답변 입력창
```

현재 좌측 메뉴는 별도 페이지로 이동하지 않고 같은 대시보드의 관련 영역으로 이동합니다. 별도 화면이 필요한 기능은 P1 이후에 라우팅을 추가합니다.

---

## 6. 시스템 구조

### 현재 시연 구조

```text
브라우저
    ↓
main-screen.html
    ├── HTML: 화면 구조
    ├── CSS: iM뱅크 톤의 반응형 UI
    ├── mock-data.js: 시연용 데이터
    └── JavaScript: 메뉴·채팅·영역 이동
```

### 향후 실제 서비스 구조

```text
브라우저 화면
    ↓
Django Template / JavaScript
    ↓
Django REST Framework API
    ├── 분석 실행
    ├── 카드소비·유동인구 분석
    ├── 매출·지출·자금 흐름
    ├── 회복 플랜
    ├── 정책 추천
    └── AI 리포트·AI 비서
    ↓
MySQL
```

현재는 서버를 추가하지 않아도 발표할 수 있는 구조를 유지하고, 실제 데이터와 백엔드는 P1 이후에 연결합니다.

---

## 7. Mock Data·API 구조

### 7-1. 현재 Mock Data

`prototype/mock-data.js`에 다음 영역을 분리해 저장합니다.

- `user`: 사용자명·아바타
- `store`: 지역·업종·분석 기간
- `metrics`: 매출·카드소비·유동인구·금융 체온
- `insight`: 핵심 인사이트
- `recoveryPlan`: 근거·시간대·고객층·추천 행동·참고용 금융 효과
- `cashflow`: 현재 현금·예상 매출·예상 잔액
- `policy`: 정책·지원사업 요약

본선에서 허용된 집계·분석 결과를 확보하면 원천데이터 파일을 저장하지 않고 이 구조의 값만 교체할 수 있습니다.

### 7-2. 향후 API 계약

기존 `08_api-spec.md`의 API 계약을 유지합니다.

- `POST /api/v1/analysis-runs`: 분석 실행
- `GET /api/v1/analysis-runs/{id}/dashboard`: 대시보드
- `GET /api/v1/analysis-runs/{id}/insights`: 인사이트
- `GET /api/v1/analysis-runs/{id}/recovery-plan`: 회복 플랜
- `GET /api/v1/analysis-runs/{id}/report`: AI 리포트
- `POST /api/v1/assistant/messages`: AI 비서

현재 HTML은 위 API를 호출하지 않으며, 로컬 Mock Data와 사전 정의된 답변으로 동작합니다.

---

## 8. 이번 점검에서 실제 구현한 내용

- 좌측 메뉴 항목에 분석 대상 영역을 연결했습니다.
- 대시보드·자금 흐름·금융 체온·정책 영역으로 이동할 수 있게 했습니다.
- AI 리포트 메뉴는 우측 AI 비서 패널을 열도록 연결했습니다.
- 화면의 주요 시연 수치를 `prototype/mock-data.js`로 분리했습니다.
- Mock Data를 HTML에 렌더링하는 로컬 JavaScript를 추가했습니다.
- 회복 플랜과 AI 비서 응답이 동일한 Mock Data를 사용하도록 연결했습니다.
- 전체 변경은 기존 정적 화면 구조를 유지한 채 최소 범위로 적용했습니다.

---

## 9. 실행 방법

별도 설치나 서버 실행이 필요하지 않습니다.

1. `prototype/main-screen.html`을 Chrome 또는 Edge에서 엽니다.
2. 좌측 메뉴를 눌러 관련 영역으로 이동합니다.
3. `회복 플랜 확인`을 눌러 핵심 차별화 기능을 확인합니다.
4. `AI 비서`를 열고 추천 질문을 선택하거나 직접 입력합니다.
5. 화면 상단의 `시연용 데이터` 표시와 하단 안내 문구를 확인합니다.

---

## 10. 검증 결과

- Git 변경사항 형식 검사: 통과
- API 명세 JSON 예시 18개: 모두 유효
- HTML 닫힘 태그 및 회복 플랜 주요 요소: 확인
- 외부 AI API 호출: 없음
- 현재 실행 방식: 로컬 HTML 직접 실행
- 브라우저·서버 의존성: 없음

---

## 11. 남은 문제와 주의사항

- 현재 수치는 모두 AI 생성 더미 데이터이므로 실제 분석 결과로 설명하면 안 됩니다.
- 카드소비·유동인구 데이터는 특정 가게의 확정적인 매출 원인을 증명하지 않습니다.
- 회복 플랜의 금융 효과는 참고용 시나리오이며 보장값이 아닙니다.
- 현재 메뉴 이동은 같은 페이지 내 영역 이동이며, 별도 URL 화면은 아닙니다.
- AI 비서는 정해진 규칙 기반 더미 답변이며 실제 AI가 아닙니다.
- 본선 데이터 교체 전에는 기간·지역·업종·출처 표기를 반드시 확인해야 합니다.

---

## 12. 다음으로 개발할 가장 중요한 5개 작업

1. **대구 빅데이터활용센터에서 허용된 분석 결과 확보**
   - 카드소비·통신사 유동인구의 지역·업종·기간 기준을 확인합니다.
   - 반출 가능한 결과와 촬영만 가능한 결과를 구분합니다.

2. **실제 발표용 분석 결과를 Mock Data 구조에 교체**
   - `metrics`, `insight`, `recoveryPlan` 값을 교체합니다.
   - 모든 수치에 기간·지역·업종 기준을 붙입니다.

3. **회복 플랜의 근거와 발표 문장 확정**
   - 소비 전환 공백이 실제 결과에 존재하는지 확인합니다.
   - 추천 행동이 데이터에서 자연스럽게 이어지는지 검토합니다.

4. **P0 발표 리허설과 반응형 점검**
   - 1분 이내 대표 흐름을 반복 시연합니다.
   - 발표 노트북 화면과 모바일 화면에서 버튼·채팅·스크롤을 확인합니다.

5. **시간이 확보될 때 P1 기능 확장**
   - 매출·지출 상세, 정책 상세, AI 리포트 화면 순서로 추가합니다.
   - P0 시연 안정성을 해치면 P1 구현을 중단합니다.

## 13. 2026-09-03 회의 개정 영향 — 문서만 반영

현재 정적 MVP 코드는 이번 개정에서 변경하지 않았습니다. Mock 데이터·고정 그래프·규칙 기반 AI 비서는 기존 상태입니다.

목표 변경:

- POS 포함 생성 데이터로 공모전 시연을 하되 생성 사실을 표시하고, 실제 자료는 추후 확보·검증 후 교체.
- DB POS·지출, 기간·요일·시간 분석, 현재 시간 안내.
- 체온계는 대시보드 영역으로 정의.
- 우측 AI 비서→iM챗봇, AI 리포트→별도 AI 비서/PDF.
- 회복 플랜의 데이터→행동 핵심 유지, 지도·경쟁·행사·대목 요구 확장.
- 정책 수집·자격 구조화·추천 목록 및 프로필·권한 추가.

기존 회복 플랜의 P0 지위는 유지합니다. 신규 요구 전체의 우선순위·일정을 확정한 것은 아닙니다. DB·API·인증은 목표 데이터 전환의 선행 작업이 됩니다.

실제 구현 순서와 미정 항목은 [01_prd.md](01_prd.md), 현재 코드와 목표 차이는 [README](README.md)를 따릅니다. 본 문서의 과거 검증 결과는 새 기능 구현 후 다시 확인해야 합니다.

## 14. 2026-09-04 혼합형 회복 플랜 결정 영향

회복 플랜의 목표 구조를 다음과 같이 확정했습니다.

```text
CCTV 익명 집계: 통행자 → 체류자 → 입장객
                         ↓
POS 집계:             유효 결제 건수 → 순매출액
                         ↓
유입률·추정 구매전환율·객단가·매출 기회 시간대
                         ↓
요인 후보 → 실행 행동 → 7일 후 같은 조건 전후 비교
                         ↑
지도·상권·경쟁점·행사 자료는 보조 근거
```

- 핵심 진단은 CCTV와 POS의 같은 매장·시간대 집계에서 출발합니다.
- 지도·상권 자료는 고객·경쟁·행사 맥락을 보완하지만, 통행·입장·결제 값을 대신하지 않습니다.
- 구매전환율은 결제 건수÷입장객이므로 고객 개인 수를 직접 확인한 값이 아닌 **추정 구매전환율**입니다.
- 결과는 확정 원인이 아니라 요인 후보이며, 7일 후 비교도 다른 변수를 통제하지 않으면 인과 효과로 단정하지 않습니다.
- CCTV는 익명 집계만 목표로 하며 얼굴·개인 식별·원본 영상을 일반 화면이나 분석 결과에 표시하지 않습니다.
- `18~20시`, 통행자 `310명`, 입장객 `21명`, 유입률 `6.8%`는 생성 데이터 화면 예시입니다.

현재 `prototype/main-screen.html`에는 이 혼합형 기능이 연결되지 않았습니다. 별도 `prototype/cctv-recovery-plan-preview.html`은 결과 화면 방향을 확인하기 위한 시각 시안일 뿐 실제 CCTV·POS 분석 구현 완료 근거가 아닙니다.
