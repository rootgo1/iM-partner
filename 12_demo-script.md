# iM파트너 공모전 데모 시연 스크립트

> 현재 실행되는 `prototype/main-screen.html`, `meeting-data.js`, `meeting-ui.js`, `report-pdf.js`를 기준으로 작성한 현행 대본입니다. 이전 시안과 고정 Mock 수치는 발표 근거로 사용하지 않습니다.

- 개정일: 2026-09-09 / 블록체인 P2 조건부 답변 보강, 시연 흐름 변경 없음
- 권장 시연 시간: 약 3분
- 시작 화면: `prototype/main-screen.html#dashboard`
- 시연 자료: 출처가 명시된 생성 POS·지출·상권 자료와 고정 CCTV·POS 회복 시나리오
- 핵심 메시지: **데이터를 보여주는 데서 끝나지 않고, 점검할 구간을 실행 행동과 사후 비교로 연결합니다.**

## 1. 발표 전에 확인할 사항

1. **현재 실행 파일을 엽니다.**

   프로젝트 루트의 `index.html`을 열어 비로그인 시작 화면부터 시연합니다. 빠른 화면 복구가 필요할 때만 `prototype/main-screen.html#dashboard`를 직접 엽니다.

   `prototype/main-screen.before-meeting.html`은 이전 시안 보존본이므로 발표에서 열지 않습니다.

2. **생성 자료 표시를 확인합니다.**

   좌측과 화면 하단에 `생성 데이터 기반 시연` 및 실제 POS·DB·외부 API·외부 AI 미연결 안내가 보여야 합니다.

3. **두 분석 범위를 구분합니다.**

   대시보드의 일반 KPI·상권·매출·지출은 선택 기간에 따라 다시 계산됩니다. CCTV·POS 회복 플랜은 `2026-09-03`의 고정 생성 시나리오이며 선택 기간·프로필과 아직 연결되지 않았습니다.

4. **PDF 범위를 확인합니다.**

   현재 PDF는 선택 기간의 매출·지출, 상권 기반 요인 후보, 비용 점검과 일반 실행 제안을 브라우저에서 만듭니다. CCTV·POS 회복 퍼널과 7일 비교 내용은 PDF에 아직 포함되지 않습니다.

## 2. 전체 시연 흐름

```text
비로그인 시작 화면에서 생성 데이터 시연 범위 확인
    ↓
로그인 없이 데모 시작 또는 임의 입력 확인
    ↓
생성 자료·분석 범위 확인
    ↓
대시보드의 CCTV·POS 회복 신호 확인
    ↓
18~20시 통행 → 체류 → 입장 → 결제 → 매출 퍼널 확인
    ↓
요인 후보 → 실행 행동 → 7일 후 비교 대기 확인
    ↓
우측 iM챗봇으로 짧은 데이터 질문
    ↓
선택: 별도 AI 비서에서 매출·지출 질문 후 로컬 PDF 생성
    ↓
로그아웃으로 비로그인 시작 화면 복귀
```

## 3. 발표 멘트와 조작 순서

| 시간 | 화면 조작 | 발표 멘트 | 반드시 보여줄 내용 |
|---|---|---|---|
| 0:00~0:20 | 비로그인 시작 화면의 `생성 데이터 기반 시연`을 확인하고 `로그인 없이 데모 시작` 클릭 | “iM파트너는 소상공인이 데이터를 해석하는 데서 끝나지 않고, 오늘 점검할 행동까지 정하도록 돕는 경영·금융 도우미입니다. 현재는 실제 계정 인증 없이 생성 자료로 서비스 흐름을 시연합니다.” | 실제 계정 입력 금지, 입력 미저장·미전송, 생성 데이터 기반 시연 |
| 0:20~0:40 | 대시보드의 `CCTV·POS 생성 분석` 카드로 이동 | “대시보드에서 바로 회복 신호를 확인할 수 있습니다. 이 카드는 실제 CCTV 분석이 아니라 2026년 9월 3일 기준의 고정 생성 시나리오입니다.” | 회복 기능이 메인 화면에 통합되어 있음 |
| 0:40~1:10 | `전환 흐름과 회복 플랜 보기` 클릭 | “18시에서 20시 사이 통행자는 310명, 체류자는 74명, 입장객은 21명, 유효 결제는 14건입니다. 매장 유입률은 6.8%, 추정 구매전환율은 66.7%, 객단가는 약 2만 429원입니다.” | 통행 310명, 체류 74명, 입장 21명, 결제 14건, 순매출 286,000원 |
| 1:10~1:35 | 진단 카드와 회복 플랜 제안 확인 | “통행 대비 입장이 낮은 구간을 먼저 점검할 **요인 후보**로 제시합니다. 자동 진단의 임계값과 규칙 버전은 아직 확정되지 않았고, 18시에서 20시는 기능 검토를 위해 선택한 시나리오입니다.” | 확정 원인이 아님, 자동 선정 산식 미정 |
| 1:35~1:55 | `실행 기록 시안 시작하기` 클릭 후 전후 비교로 이동 | “17시 30분 대표 메뉴 입간판 노출과 18시부터 20시까지 2인 세트 혜택 안내를 제안합니다. 실행 버튼은 현재 브라우저 상태만 바꾸며 DB에 저장하지 않습니다.” | 실제 할인율은 운영자가 결정, 저장 미구현 |
| 1:55~2:10 | `실행 전·후 비교`의 마지막 단계 확인 | “7일 뒤 같은 요일과 시간대의 유입률, 결제 건수, 순매출을 비교하도록 설계했습니다. 현재 후속 자료는 없으므로 결과는 ‘데이터 집계 대기’이며 개선 효과를 표시하지 않습니다.” | 2026.09.10, 집계 대기, 효과 미확인 |
| 2:10~2:35 | 우측 상단 `iM챗봇` 열기 → `CCTV 유입률과 구매전환율을 알려주세요` 입력 | “iM챗봇은 모든 탭에서 짧은 데이터 질문에 답합니다. 현재는 외부 AI가 아니라 키워드 규칙으로 같은 생성 시나리오를 읽습니다.” | 310명·21명·14건, 6.8%·66.7% 답변 |
| 2:35~2:55 | 선택: 좌측 `AI 비서` → `매출과 지출을 함께 분석해 주세요.` → PDF 생성 | “별도 AI 비서는 선택 기간의 매출·지출 분석을 대화와 문서로 남기는 화면입니다. 현재는 규칙 기반이며 PDF는 서버가 아니라 이 브라우저에서 실제 파일로 생성됩니다.” | 파란 PDF 다운로드 링크, 로컬 생성 |
| 2:55~3:05 | 상단 `로그아웃`으로 비로그인 시작 화면에 돌아가기 | “현재 구현은 생성 자료로 진입부터 실행까지의 핵심 흐름을 검증한 단계입니다. 실제 인증·CCTV·POS·지도·정책·뉴스·날씨·외부 AI 연동과 7일 후 효과 검증은 완료되지 않았습니다.” | 비로그인 화면 복귀, 현재 구현과 향후 계획 구분 |

## 4. iM챗봇 시연 질문

### 기본 질문

`CCTV 유입률과 구매전환율을 알려주세요`

답변에서 다음 항목을 확인합니다.

- 분석일 `2026-09-03`
- `18~20시` 통행자 `310명`, 입장객 `21명`, 유효 결제 `14건`
- 매장 유입률 `6.8%`, 추정 구매전환율 `66.7%`
- 통행→입장 구간을 먼저 점검한다는 요인 후보
- 실제 CCTV·POS가 아닌 생성 시나리오라는 안내

### 시간이 남을 때 사용할 질문

`당장 실행할 수 있는 방법은 무엇인가요?`

대표 메뉴 입간판, 2인 세트 **혜택 안내**, 7일 후 같은 조건 비교로 연결되는지 확인합니다. 할인율이나 기대 매출은 정해진 값이 없다는 답변도 함께 보여줍니다.

## 5. AI 비서와 PDF 시연 범위

PDF 시연에는 `매출과 지출을 함께 분석해 주세요.`를 사용합니다. 현재 PDF가 실제로 포함하는 범위와 일치하기 때문입니다.

- AI 비서는 서비스 내 화면 명칭이며 현재 외부 AI·LLM을 사용하지 않습니다.
- 질문 답변은 `meeting-ui.js`의 키워드 규칙으로 생성됩니다.
- PDF는 브라우저에서 만들어지는 로컬 파일이며 서버 저장·사용자 권한 기능은 없습니다.
- 현재 PDF에는 CCTV·POS 회복 퍼널, 고정 회복 시나리오의 수치, 정책, 7일 후 결과가 포함되지 않습니다.
- 따라서 `회복 플랜을 요약해 주세요.`를 질문한 뒤 PDF에 회복 분석이 모두 들어갔다고 설명하지 않습니다.

## 6. 반드시 사용할 표현

- “현재 화면은 **생성 데이터 기반 시연**입니다.”
- “18~20시는 자동 산식으로 선정한 결과가 아니라 **기능 검토용 고정 시나리오**입니다.”
- “매출 변화의 확정 원인이 아니라 **우선 점검할 요인 후보**입니다.”
- “구매전환율은 결제 건수와 구매자 수가 다를 수 있어 **추정 지표**입니다.”
- “실행 후 7일 결과는 현재 **데이터 집계 대기** 상태입니다.”
- “iM챗봇과 AI 비서는 현재 **외부 AI 없는 규칙 기반 시안**입니다.”
- “PDF는 현재 브라우저에서 생성되는 로컬 파일입니다.”

## 7. 피해야 할 표현

- “실제 CCTV가 실시간으로 고객을 분석하고 있습니다.”
- “AI가 18~20시를 자동으로 최적 시간대로 예측했습니다.”
- “이 플랜을 실행하면 매출이 확실히 증가합니다.”
- “결제 14건은 구매자 14명을 뜻합니다.”
- “선택 기간을 바꾸면 CCTV·POS 회복 데이터도 함께 바뀝니다.”
- “PDF에 모든 분석과 회복 플랜이 저장됩니다.”
- “실제 카드사·통신사·정책·지도 자료가 연결되어 있습니다.”
- “현재 금융상품 신청, 대출 심사 또는 상담원 연결이 가능합니다.”
- “AI가 실제 원천데이터를 학습했습니다.”
- “현재 블록체인을 적용했거나 적용이 확정됐습니다.”

## 8. 발표 중 질문이 들어왔을 때 짧은 답변

| 질문 | 짧은 답변 |
|---|---|
| 실제 데이터인가요? | “아닙니다. 현재는 구조와 계산을 검증하기 위한 생성 자료이며 실제 관측 자료로 설명하지 않습니다.” |
| 18~20시는 자동 추천인가요? | “아닙니다. 현재는 규칙과 임계값을 확정하기 전의 기능 검토용 선택 시나리오입니다.” |
| 실행 효과가 나왔나요? | “아직 아닙니다. 7일 후 결과는 집계 대기이며 성공이나 매출 증가를 표시하지 않습니다.” |
| AI를 쓰고 있나요? | “현재 두 대화 화면 모두 키워드 규칙 기반이며 외부 AI 제공자는 미정입니다.” |
| PDF도 시연용인가요? | “실제로 열리는 PDF 파일을 브라우저에서 만들지만 서버 저장·권한과 CCTV 회복 내용 연결은 아직 없습니다.” |
| AI·블록체인 대회인데 왜 블록체인을 넣지 않았나요? | “현재는 단일 운영 구조라 DB의 출처·버전·감사 기록을 먼저 구현하는 편이 적합합니다. 여러 기관의 독립 검증 수요와 중앙 방식 대비 필요성이 확인되면 월간 브리핑의 발급본 일치 여부만 확인하는 P2 PoC를 별도 검토합니다.” |
