# iM 파트너 기능명세서

- 문서 상태: 2026-09-04 정적 HTML 부분 구현 반영 / 전체 구현 목표
- 기준: [PRD](01_prd.md), [데이터 명세](04_data-spec.md)
- 현재 코드는 생성 자료로 동작하는 정적 HTML MVP입니다. 구현되지 않은 서버·실자료 기능의 완료 기준은 향후 검증할 기준입니다.

## 1. 기능 ID와 우선순위

기존 ID를 유지합니다. F-07의 챗봇과 AI 비서는 하위 구분으로 분리하고, 프로필만 F-10으로 추가합니다.

| ID | 기능 | 변경 방향 | 기존 기준 |
|---|---|---|---|
| F-01 | 가게·분석 조건 | 주·월·직접 기간 조건 확장 | P0 |
| F-02 | 대시보드 | 뉴스·날씨·체온계·캘린더 통합 | P0 |
| F-03 | 상권·요일·시간 분석 | 기간 집계 및 현재 시간 행동 안내 | P0 |
| F-04 | 매출·지출 분석 | DB POS와 지출·매입자료 기반 | P1 |
| F-05 | 금융 체온계 | 대시보드 내부에 시각화 | P1 |
| F-06 | 정책·지원사업 | 수집·자격 매칭·추천 목록 | P1 |
| F-07-C | 챗봇 | 기존 우측 AI 비서의 명칭·범위 변경 | 간단한 채팅 P0 |
| F-07-R | AI 비서 | 기존 AI 리포트의 대화형 분석·PDF | 리포트 P1 |
| F-08 | 서비스 데이터 처리 | 생성 시연 자료와 실자료의 출처·구조 검증 | P0 |
| F-09 | 골목상권 회복 플랜 | 기존 행동 제안 유지, 지도·경쟁·행사 확장 | P0 유지 |
| F-10 | 프로필 | 사용자·가게 정보 분리 관리 | 신규 일정 미정 |

기존 우선순위와 신규 기능의 개발 일정은 다릅니다. 이번 표는 회의의 모든 추가 요구를 P0로 올리거나 기존 P0를 내리는 결정이 아닙니다.

## 2. 공통 처리

- 모든 화면은 동일한 가게·기간·데이터 버전의 분석 결과를 사용합니다.
- 금액·인원·건수·지수·비율, 출처·기준일·비교 기간을 구분합니다.
- 데이터 없음, 일부 없음, 비교 불가, 처리 중, 실패 상태를 제공합니다.
- 공모전에서는 POS 포함 생성 데이터 사용을 허용하되 명확히 표시합니다. 선택한 자료에 없는 항목을 고정 답변으로 채우거나 실자료인 것처럼 설명하지 않습니다.
- 내 가게 자료는 인증된 소유자만 조회하며, 타 사용자 자료를 공개하지 않습니다.
- PC 우선 반응형, 기존 민트·그린 톤과 좌측 접기·펼치기를 유지합니다.

## 3. F-01 가게·분석 조건

입력: 가게, 사업 지역, 업종, 분석 시작·종료일. 정책 조건에 필요하면 프로필 정보를 추가 확인합니다.

처리: 주·월 등 기간 선택을 실제 조회 조건에 반영합니다. 최근 2년 분석은 해당 기간 자료가 있을 때 제공합니다. 요일·시간 집계에는 거래 시각과 시간대 기준이 필요합니다.

출력: 선택 가게·기간·비교 기준·자료 갱신일.

정적 HTML 상태: 월·최근 7일·직접 기간 변경 시 KPI·그래프·요인·플랜·챗봇·AI 비서가 `meeting-data.js`의 동일 계산 결과를 사용합니다. 실제 DB 조회와 매장 선택은 미구현입니다.

완료 기준: 서버 연동 후에도 조건 변경 시 모든 표시 영역이 같은 가게·기간·자료 버전을 사용합니다.

## 4. F-02 대시보드

표시:

- 매출·카드소비·유동인구 핵심 지표와 변화
- 대표 인사이트 및 회복 플랜 진입
- F-05 금융 체온계
- 소상공인 뉴스·이슈가 자동 순환하는 배너
- 날씨, 캘린더, 현재 시간대 행동 안내
- 자금 흐름·정책 요약

배너는 자동 전환과 수동 탐색·일시정지를 제공하는 방향으로 설계합니다. 날씨·체온계·캘린더를 모두 회전 배너 속에 숨기도록 확정하지 않습니다. 배치와 자료 공급원은 화면·데이터 명세에서 구분합니다.

완료 기준: 출처와 갱신 시점이 있으며, 누락 자료는 별도 안내합니다. 외부 금융거래나 뉴스 원문 무단 재게시를 기능으로 포함하지 않습니다.

## 5. F-03 상권·요일·시간 분석

입력: 비교 가능한 카드소비·유동인구, DB POS 매출·주문 건수, 확보된 경우 별도의 매장 방문자 수.

처리:

1. 주·월 등 선택 기간을 요일·시간대로 집계합니다.
2. 지역·기간·단위를 맞출 수 있는 카드소비와 유동인구만 비교합니다.
3. 높은 유동인구에 비해 소비가 낮은 구간을 소비 전환 공백의 요인 후보로 제시합니다.
4. 연령·성별, 방문층과 소비층, 경기일·비경기일, 행사·날씨 차이는 해당 자료가 있을 때만 분석합니다.
5. 지난 일주일의 기록을 현재 시간대에 맞춰 운영 행동과 연결합니다.

회의 예시: 14~15시 한가한 기록이 있다면 식사·청소·장보기, 16~17시 수요가 확인된다면 미리 재료 준비를 제안합니다. 고정 시간표·실시간 방문 예측으로 제공하지 않습니다.

완료 기준:

- 주문 건수를 방문자 수로 부르지 않습니다.
- 지난 7일에서 같은 요일·시간은 한 번뿐일 수 있음을 표시합니다.
- 분석창·완료된 날짜·관측 수·기준 시간대와 충분성 기준이 정의되어야 합니다. 부족하면 일반화하지 않습니다.
- 기존 12~14시, 20~30대·직장인을 실제 자료와 무관하게 고정 추천하지 않습니다.

## 6. F-04 매출·지출 분석

기본 전제: POS 데이터를 DB에 보유한다고 가정하여 조회·분석하는 구조입니다. DB 구축·자료 확보 완료를 뜻하지 않습니다.

입력:

- POS 거래 시각, 영수증/거래 ID, 매출액, 취소·환불, 품목·판매수량(품목 분석 시)
- 월세, 관리비, 매입비, 인건비, 기타 지출
- 별도 매입 품목·단가·수량·재고 자료(발주 분석 시)
- 현금잔액, 예상 입금·지출, 추정의 근거(현금흐름 시)

처리:

- 최근 매출과 지출의 금액·증감·구성비 비교
- 매출이 늘어도 돈이 남지 않는 지출 요인 점검
- 선택 기간 매입금액 합계가 높은 품목 TOP 3
- 판매·매입 단위와 품목 대응, 재고가 확인된 범위의 과다 매입 점검
- 확인된 재고·판매 추세를 근거로 다음 달 발주량 조절 제안
- 기존 월간 자금 흐름, 대구로 취소율·조리시간·할인율 분석은 자료 확보 조건으로 유지

주의:

- “매출 +10%, 매입비 +22%”는 회의의 설명 예시입니다. 금액·다른 지출 없이 이익 감소의 확정 근거로 사용하지 않습니다.
- 매입 지출은 판매된 상품의 원가와 같지 않습니다. 단순 매출−현금 지출을 영업이익으로 표기하지 않습니다.
- 자료가 없으면 TOP 3·초과 수량·발주량을 만들어내지 않습니다.
- 직접 입력·거래내역 보완 경로는 유지하되 POS 가져오기가 기본입니다. 영수증 OCR은 향후 확장입니다.

완료 기준: 취소·환불·중복을 처리하고 합계가 원자료와 대조됩니다. 매출·지출 차이, 회계상 이익, 예상 현금흐름이 서로 구분됩니다.

## 7. F-05 골목금융 체온계

위치: S-02 대시보드의 시각화 구성요소. 별도 필수 이동 화면을 추가하지 않습니다.

입력: 매출액, 비용 구조, 현금흐름, 확보된 영업이익률·부채비율 및 비교 가능한 업종 평균.

출력: 비교 가능한 항목의 상대적 상태와 근거. 점수·단위·상태 경계는 정의 후 사용합니다.

완료 기준: 타 사용자 원자료 비공개, 신용점수·대출 심사 결과가 아니라는 안내. 현재 62°는 산식 미정의 Mock이므로 목표 점수로 재사용하지 않습니다.

## 8. F-06 정책·지원사업

수집: 금융·창업 분류의 공고 중 제목에 ‘소상공인’, ‘골목’, ‘전통시장’ 중 하나 이상 포함된 공고를 1차 수집합니다. 제공처의 실제 분류명·범위는 연동 전에 확인합니다.

조건 처리:

- 공고 갱신 시 원문과 원문 링크를 보존합니다.
- 나이, 지역, 업종, 창업일자, 사업장 규모 조건을 구조화해 저장합니다.
- 기존 매출 조건은 해당 공고에 필요한 경우 유지합니다.
- 범위·기준일·복수 지역·제외 조건·AND/OR를 보존합니다.
- 미상은 ‘제한 없음’과 다릅니다. 추출이 불확실하면 확인 필요로 처리합니다.

화면:

- 오늘의 추천공고(맞춤형) / 전체 리스트
- 검색과 옵션 필터
- 추천 최대 10개, PC 기준 3개 표시, 한 번에 1개씩 이동
- 후보가 10개보다 적으면 실제 개수만 표시하고 채워 넣지 않음
- 적합도 높은 순서와 적합도 %; 산식 확정 전에는 임의 점수 없이 일치·불일치·확인 필요 조건 표시

완료 기준: 프로필과 조건의 연결 근거를 확인할 수 있으며, 적합도는 선정 확률·지원 자격 확정이 아닙니다. 만료·중복·갱신을 처리합니다.

## 9. F-07-C 챗봇

- 기존 우측 ‘AI 비서’를 ‘챗봇’으로 변경합니다.
- 모든 탭에서 접기·펼치기와 패널 크기 조절이 가능합니다.
- 권한 있는 각 카테고리의 분석 결과를 참조합니다. 현재 열린 탭의 데이터만 아는 구조로 제한하지 않습니다.
- 간단한 질문에 데이터 근거 → 짧은 해석 → 행동 제안으로 답합니다.
- 범위를 벗어나거나 모르는 내용은 모른다고 설명하고 상담원 연결을 안내합니다.
- 실제 응대 주체·채널 확정 전에는 ‘연결 완료’나 가짜 상담창을 제공하지 않습니다.

대표 질문: 매출 변화, 공략 시간대, 고객층, 당장 할 행동, 자금 흐름 점검.

완료 기준: 숫자 변경이 답변에도 반영됩니다. 고정 문구를 실분석처럼 출력하지 않으며, 대출 승인·미확보 자료·일반 잡담을 아는 척 답하지 않습니다. 외부 AI API는 선택 미정입니다.

## 10. F-07-R AI 비서

별도 S-07 화면이며 챗봇과 목적을 구분합니다.

1. 첫 화면에서 대화창과 예시 질문을 보여줍니다.
2. 질문에 필요한 가게·기간·분석 범위를 확인합니다.
3. 승인된 데이터 조회·분석 결과로 대화합니다.
4. 최종 요약과 근거·제외된 자료를 정리합니다.
5. PDF 생성 성공 후 파란색 파일 링크와 완료 문구를 표시합니다.
6. 실제 파일 다운로드·열기·인쇄가 가능합니다.

완료 문구 예: “최종 요약본 분석이 완료되었습니다. 분석요약.pdf”

파일 내용: 상황 요약, 요인 후보, 시간대·고객층, 매출·지출·현금흐름, 체온계, 정책, 회복 플랜 중 실제 분석한 항목. 모든 항목을 데이터 없이 채우지 않습니다.

완료 기준: 단순 가짜 링크가 아닌 열리는 PDF, 한글·수치·기간 일치, 다운로드 권한 확인, 실패 시 재시도 안내.

## 11. F-08 서비스 데이터 처리

- 기존 ‘시연용 데이터 처리’ ID를 유지하고 역할을 변경합니다.
- 자료의 출처·권한·기간·단위·검증 상태 확인 후 저장·분석합니다.
- 센터 제한 원자료와 사용자 제공 POS를 구분합니다.
- POS·매입·재고·방문 등을 생성한 시연 데이터와 실자료의 사용 범위를 기록합니다. 생성값을 DB에 넣어도 생성 데이터 표시는 유지합니다.
- 자료가 없거나 근거가 부족하면 상태를 반환하고 숫자를 대신 생성하지 않습니다.
- 테스트용 fixture는 서비스 DB·화면과 분리합니다.

현재 HTML은 `meeting-data.js`의 검증 가능한 생성 거래·지출·상권 집계와 공통 계산 결과를 연결했습니다. 브라우저 안에서만 동작하며 DB에서 읽어온 자료는 아닙니다. 실제 자료 전환 시에는 해당 자료·출처·표시를 함께 바꾸며, HTML 구현만으로 실데이터 전환 완료라고 표시하지 않습니다.

## 12. F-09 골목상권 회복 플랜

기존 핵심: 데이터 → 요인 후보 → 시간대·고객층 → 실행 행동 → 근거가 있을 때 금융 참고정보.

회의 확장:

- 카카오맵으로 내 가게 주소의 핀과 반경 n미터 원 표시
- 상권분석 API의 좌표·업종으로 반경 안 동종업종 조회
- 확인된 메뉴·영업시간·주변 직장인 근거와 우리 가게 정보 비교
- 주변 디저트 카페 대비 식사 메뉴, 주변 10시 개점 대비 7시 영업, 직장인 대상 사원증 할인 등 조건부 제안
- 과거 분석의 방문 연령층을 출처·기간과 함께 반영
- 근처 행사 알림, 과거 축제의 판매자료가 있을 때 상품 추천
- 절기·기념일 문구와 업종별 대목 행사 안내
- 업종 중심 AI 프롬프트를 활용하는 대목 1주 전 나우브리프형 안내

제약:

- 지도에서 점포 위치를 안다고 메뉴·영업시간·직장인 비중까지 아는 것은 아닙니다.
- ‘서문시장 방문객은 50대가 많음’은 검증 전 사실로 쓰지 않습니다.
- 업종만으로 만든 계절 문구와 지역 행사·실판매 데이터 기반 추천을 구분합니다.
- 반경, 할인율, 알림 채널·주기, 산출 기준은 임의 확정하지 않습니다.
- +650,000원은 근거 미정의 Mock이며 데이터 기반 효과로 설명하지 않습니다.

완료 기준: 지도 결과와 경쟁 근거를 추적할 수 있고, 조건이 없는 추천은 가설·확인 필요로 구분합니다. 금융 신청·수익 보장으로 연결하지 않습니다.

## 13. F-10 프로필

사용자: 이름, 전화번호, 나이, 아이디·비밀번호, 이메일.

사업장: 지역(사업장 소재지), 업종, 가게 주소, 사업자번호, 창업일자, 사업장 규모(직원 수).

- 계정 비밀번호는 인증 시스템에서 해시로 저장하고 조회 응답에 포함하지 않습니다.
- 지역은 사업장 주소지로 사용자 확인을 받았습니다. 사업장 규모는 직원 수 기준으로 확정했습니다. 입력 직원 수와 공고의 상시근로자 수 산정 기준이 다를 수 있으므로 기준·확인일을 함께 관리합니다. 공고가 거주지를 요구하면 사업장 주소로 대신 판정하지 않습니다.
- 정책 추천에 필요한 조건만 요구하며 누락은 확인 필요로 표시합니다.
- 나이를 받기로 한 회의 요구를 임의로 생년월일 수집으로 바꾸지 않습니다.
- 사용자 정보 변경 시 추천을 다시 평가하되 과거 분석에는 당시 조건을 보존합니다.

## 14. 확인·검증

미정 항목은 [PRD 8절](01_prd.md#8-구현-전-확인할-사항), 기능별 시험은 [09_test-plan.md](09_test-plan.md)에 연결합니다. 현재 구현 사실은 [README](README.md)와 [화면명세](06_screen-spec.md)를 기준으로 구분합니다.
