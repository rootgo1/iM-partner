# iM 파트너 데모 주장-데이터-코드 추적성 감사

> 개정 안내: 이 문서의 기존 감사 본문은 정적 Mock MVP의 수치·코드·문구를 추적한 기록입니다. 2026-09-03 문서 개정에서 코드와 Mock을 변경하지 않았습니다. 기존 코드 위치·수치 감사는 보존하며, 문서 사용처의 향후 변경과 신규 근거 조건은 마지막 개정 절에 추가합니다.

- 문서 상태: 감사 완료
- 감사 목적: 실제 데이터 교체 전에 발표·심사 Q&A의 주장과 현재 Mock Data·화면 코드의 일치 여부 확인
- 감사 기준: `prototype/main-screen.html`, `prototype/mock-data.js`, `06_screen-spec.md`, `11_demo-hardening.md`, `12_demo-script.md`, `13_judge-qa.md`, `README.md`
- 범위: 기존 P0 동작 유지, 신규 기능·화면·P1 기능 추가 없음

---

## 0. 감사 결론

현재 P0 프로토타입은 주요 KPI, 인사이트 문구, 회복 플랜 카드, 자금 흐름 카드, 정책 카드 및 대부분의 AI 비서 답변을 `prototype/mock-data.js`에서 읽어 화면에 표시합니다.

다만 다음 항목은 현재 `mock-data.js`와 완전히 연결되어 있지 않습니다.

- 시간대 그래프의 선·점·축 라벨은 HTML 안의 고정 SVG입니다.
- 핵심 인사이트 목록의 일부 설명은 정적 HTML 문구입니다.
- AI 코드에는 Mock 필드가 없을 때 사용할 숫자형 기본값이 남아 있습니다. 정상 로딩 시에는 사용되지 않지만, Mock 필드와 따로 바뀌면 stale 값이 될 수 있습니다.
- 발표 스크립트와 심사 Q&A의 숫자는 문서에 직접 입력된 값이므로 `mock-data.js`를 수정해도 자동으로 바뀌지 않습니다.
- `+650,000원`은 계산식·입력값·원천 데이터가 없는 고정 Mock 값입니다. 현재 산출 근거는 **미정의**입니다.

따라서 현재 발표에서는 다음과 같이 설명해야 합니다.

> 현재 화면은 AI 생성 Mock Data를 사용한 분석 흐름 시연이며, 화면의 수치는 실제 특정 매장의 결과나 금융 효과를 보장하지 않습니다. `+650,000원`은 계산 결과가 아닌 고정 Mock 참고값입니다.

---

## 1. 핵심 데이터 목록

| ID | 핵심 수치·주장 | 현재 Mock 값 | 현재 상태 |
|---|---|---:|---|
| D-01 | 분석 사용자 | 이소현 | Mock 고정값 |
| D-02 | 분석 대상 | 대구 서문시장 · 음식점 | Mock 고정값 |
| D-03 | 분석 기간 | 최근 2년 | Mock 고정값. 시작·종료일은 없음 |
| D-04 | 최근 매출 변화 | -12.4% | Mock 고정값. 계산식·원천 자료 없음 |
| D-05 | 카드소비 변화 | -8.1% | Mock 고정값. 계산식·원천 자료 없음 |
| D-06 | 유동인구 변화 | +3.2% | Mock 고정값. 계산식·원천 자료 없음 |
| D-07 | 금융 체온 | 62° | Mock 고정값. 체온 산식·비교 집단 없음 |
| D-08 | 소비 전환 공백 | 12~14시 | Mock 문구·고정 그래프를 통한 시연값 |
| D-09 | 추천 고객층 | 20~30대 · 인근 직장인 | Mock 고정값. 고객층 산출 필드·규칙 없음 |
| D-10 | 현재 보유 현금 | 3,240,000원 | Mock 고정값. 입력 내역·계산식 없음 |
| D-11 | 예상 매출 | 8,600,000원 | Mock 고정값. 예측식·기간·입력값 없음 |
| D-12 | 예상 잔액 | 1,180,000원 | Mock 고정값. 현금흐름 계산식 없음 |
| D-13 | 참고용 금융 효과 | +650,000원 | 고정 Mock 값. **산출 근거 미정의** |
| D-14 | 정책 추천 상태 | 신청 조건 일치 | Mock 고정값. 실제 자격 검증 없음 |
| D-15 | 인사이트 목록 개수 | 세 가지 변화 | 분석 데이터가 아니라 화면에 직접 입력한 UI 라벨 |
| D-16 | 대표 AI 질문 개수 | 5개 | 분석 데이터가 아니라 화면에 배치한 시연 질문 개수 |
| D-17 | 권장 발표 시간 | 약 2~3분 | `12_demo-script.md`의 발표 운영 상수. 서비스 데이터 아님 |

`D-04`부터 `D-13`까지의 숫자는 현재 저장소의 실제 카드사·통신사 원천 자료에서 계산된 값이 아닙니다. 현재 확인 가능한 근거는 `prototype/mock-data.js`와 이를 설명하는 프로토타입 문서뿐입니다.

---

## 2. 데이터 필드와 의미

### 2-1. `prototype/mock-data.js` 필드 매핑

| 데이터 영역 | 필드명 | 현재 값 | 화면에서의 의미 |
|---|---|---|---|
| `user` | `name` | 이소현 | 사용자명 및 AI 인사말 |
| `user` | `initials` | 이소 | 좌측 프로필 아이콘 |
| `store` | `region` | 대구 서문시장 | 분석 지역 |
| `store` | `industry` | 음식점 | 분석 업종 |
| `store` | `periodLabel` | 최근 2년 | 분석 기간 표시 및 기간 선택 고정 안내 |
| `store` | `context` | 대구 서문시장 · 음식점 · 최근 2년 | 대시보드·AI 분석 조건 |
| `metrics` | `salesChange` / `salesDelta` | -12.4% / ▼ 12.4% | 매출 KPI와 하단 변화 표시 |
| `metrics` | `cardChange` / `cardDelta` | -8.1% / ▼ 8.1% | 카드소비 KPI와 하단 변화 표시 |
| `metrics` | `trafficChange` / `trafficDelta` | +3.2% / ▲ 3.2% | 유동인구 KPI와 하단 변화 표시 |
| `metrics` | `financeTemperature` / `financeStatus` | 62° / 주의 | 금융 체온 KPI |
| `insight` | `headline` | 사람은 늘었지만, 지갑은 덜 열렸습니다. | 대표 인사이트 제목 |
| `insight` | `description` | 유동인구 증가·카드소비 감소·12~14시 공백 | 대표 인사이트 설명 |
| `insight` | `chartCalloutTitle` / `chartCalloutDescription` | 12~14시 공백 / 시간대 설명 | 그래프 아래 호출 문구 |
| `recoveryPlan` | `focusTime` | 12~14시 | 회복 플랜 집중 시간대, AI 답변 |
| `recoveryPlan` | `customer` | 20~30대\n인근 직장인 | 회복 플랜 추천 고객층, AI 답변 |
| `recoveryPlan` | `action` | 점심 대표 메뉴·홍보·배달 운영 점검 | 회복 플랜 추천 실행, AI 답변 |
| `recoveryPlan` | `referenceCashflow` | +650,000원 | 참고용 금융 효과 표시 및 플랜 상세 답변 |
| `cashflow` | `currentCash` | 3,240,000원 | 현재 보유 현금 |
| `cashflow` | `expectedSales` | 8,600,000원 | 예상 매출 |
| `cashflow` | `expectedBalance` | 1,180,000원 | 예상 잔액 |
| `policy` | `tag` / `name` / `description` | 신청 조건 일치 / 소상공인 경영안정 지원사업 / 운영자금 및 경영 지원 | 정책 요약 카드와 AI 답변 |

### 2-2. 화면 렌더링 연결

`main-screen.html`은 `renderMockData()`에서 `setMockText()`를 호출해 다음 `data-mock` 요소를 채웁니다.

| 화면 영역 | HTML 위치·식별자 | 연결 데이터 |
|---|---|---|
| 사용자·분석 조건 | `[data-mock="user-name"]`, `[data-mock="store-context-short"]`, `[data-mock="analysis-context-description"]` | `user`, `store` |
| KPI | `sales-change`, `card-change`, `traffic-change`, `finance-temperature` 등 | `metrics` |
| 대표 인사이트 | `insight-headline`, `insight-description` | `insight` |
| 그래프 호출 문구 | `chart-callout-title`, `chart-callout-description` | `insight` |
| 핵심 인사이트 01 시간대 | `insight-focus-time` | `recoveryPlan.focusTime` |
| 회복 플랜 | `recovery-description`, `recovery-evidence`, `recovery-focus-time`, `recovery-customer`, `recovery-cashflow`, `recovery-action` | `recoveryPlan` |
| 자금 흐름 | `current-cash`, `expected-sales`, `expected-balance` | `cashflow` |
| 정책 | `policy-tag`, `policy-name`, `policy-description` | `policy` |
| AI 초기 상태 | `ai-context`, `ai-greeting`, `ai-initial-insight` | `user`, `store`, `metrics`, `recoveryPlan` |

핵심 인사이트 목록의 첫 번째 시간대 문구는 이번 감사에서 `recoveryPlan.focusTime`을 참조하도록 최소 수정했습니다. 화면 디자인이나 사용자 흐름은 변경하지 않았습니다.

---

## 3. 핵심 수치별 전체 사용처 추적

| ID | `mock-data.js` 필드 | HTML 사용 위치 | AI 비서 | 회복 플랜 | 발표 스크립트 | 심사 Q&A | 하드코딩·판정 |
|---|---|---|---|---|---|---|---|
| D-04 | `metrics.salesChange` | 매출 KPI `sales-change` | 매출 질문의 원인 답변에서는 직접 표시하지 않고 카드소비·유동인구를 근거로 사용 | 직접 표시하지 않음 | `12.4% 감소` | 매출 변화와 요인 후보 설명 | 화면 KPI는 동적. 발표·Q&A 문장은 문서 하드코딩 |
| D-05 | `metrics.cardChange` | 카드소비 KPI `card-change` | 매출·시간대·초기 인사이트·차트 상세 답변 | `recoveryPlan.evidence`에 포함된 고정 문구 | `-8.1%` | 카드소비 비교 근거 | 화면 일부는 동적. SVG 선은 값과 무관한 고정 경로 |
| D-06 | `metrics.trafficChange` | 유동인구 KPI `traffic-change` | 매출·초기 인사이트·차트 상세 답변 | `recoveryPlan.evidence`에 포함된 고정 문구 | `+3.2%` | 유동인구 비교 근거 | 화면 일부는 동적. SVG 선은 값과 무관한 고정 경로 |
| D-07 | `metrics.financeTemperature` | 금융 체온 KPI `finance-temperature` | 5개 대표 질문의 본문에는 직접 사용하지 않음 | 직접 사용하지 않음 | 직접 언급 없음 | 금융 관점 설명 | 화면 KPI 동적. 산식·비교 기준은 미정의 |
| D-08 | `recoveryPlan.focusTime` 및 관련 인사이트 문구 | 그래프 호출 문구, 핵심 인사이트 01, 회복 플랜 | 5개 질문 중 매출·시간·고객·실행·회복 답변 | 집중 시간대 | `12~14시` | 소비 전환 공백 설명 | 첫 번째 목록은 동적. 그래프 축·선과 일부 문구는 정적 |
| D-09 | `recoveryPlan.customer` | 회복 플랜 추천 고객층 | 고객 질문 답변 | 추천 고객층 | `20~30대·인근 직장인` | 소상공인 가치 설명의 일반 주장 | 화면·AI는 동적. 고객층 산출 규칙은 없음 |
| D-10 | `cashflow.currentCash` | `current-cash` | 자금 흐름 질문·자금 상세 버튼 | 직접 사용하지 않음 | 직접 언급 없음 | 자금 흐름 설명 | 화면·AI는 동적. 입력·계산식 없음 |
| D-11 | `cashflow.expectedSales` | `expected-sales` | 현재 대표 답변에서는 직접 사용하지 않음 | 직접 사용하지 않음 | 직접 언급 없음 | 직접 언급 없음 | 화면 동적이지만 산출 근거 없음 |
| D-12 | `cashflow.expectedBalance` | `expected-balance` | 자금 흐름 질문·자금 상세 버튼 | 직접 사용하지 않음 | 직접 언급 없음 | 직접 언급 없음 | 화면·AI는 동적. 계산식 없음 |
| D-13 | `recoveryPlan.referenceCashflow` | `recovery-cashflow` | 회복 플랜 상세 답변에서 사용 | 참고용 금융 효과 | `+650,000원` | 금융 효과 Q&A | 값은 Mock에서 읽지만 계산·근거는 없음 |

---

## 4. AI 비서 5개 대표 질문 추적

현재 AI 비서는 외부 AI API가 아니라 `main-screen.html`의 `demoAnswer()` 키워드 규칙으로 답변합니다. 각 질문은 클릭 시 입력창을 거쳐 동일한 함수로 처리됩니다.

| 질문 | 사용 데이터 | 판단 규칙 | 답변 구조 | 행동 제안 | 일관성 판정 |
|---|---|---|---|---|---|
| 현재 매출이 왜 떨어졌나요? | `metrics.trafficChange`, `metrics.cardChange`, `recoveryPlan.focusTime`, `recoveryPlan.action` | `왜`, `떨어`, `줄`, `매출` 중 하나가 포함되면 매출 분기 | 원인 확정이 아닌 소비 전환 공백 후보 → 유동인구·카드소비 수치 → 집중 시간대 | 대표 메뉴·홍보 노출 점검 | Mock 값 변경 시 답변 수치는 변경됨. 필드가 없을 때 기본값은 하드코딩 |
| 어느 시간대를 공략해야 하나요? | `recoveryPlan.focusTime`, `recoveryPlan.action` | `시간`, `운영`, `언제` 중 하나가 포함되면 시간 분기 | 방문객은 많고 소비가 낮은 공백 → 시간대 설명 | 해당 시간대 대표 메뉴·홍보 집중 | 시간대는 Mock 연결. 카드·유동인구 수치를 직접 표시하지 않음 |
| 어떤 고객층을 노려야 하나요? | `recoveryPlan.customer`, `recoveryPlan.focusTime` | `고객`, `타겟`, `누구` 중 하나가 포함되면 고객 분기 | 추천 고객층 → 해당 고객층이 많은 시간대 | 대표 메뉴·노출 집중 | Mock 연결. 고객층의 실제 산출 규칙은 없음 |
| 당장 실행할 수 있는 방법은 무엇인가요? | `recoveryPlan.action`, `recoveryPlan.focusTime` | `당장`, `실행`, `방법` 중 하나가 포함되면 실행 분기 | 가장 빠른 실행 → 적용 시간대 → 재확인 | Mock에 정의된 운영·홍보·배달 점검 행동 | Mock 연결. 행동 자체는 고정 문구 |
| 자금 흐름에서 무엇을 확인해야 하나요? | `cashflow.currentCash`, `cashflow.expectedBalance` | `자금`, `현금`, `비용`, `지출` 중 하나가 포함되면 자금 분기 | 현재 현금 → 예상 잔액 → 금융 효과의 참고용 성격 | 자금 흐름을 먼저 확인 | 현금·잔액은 Mock 연결. 금융 효과 산출 근거는 없음 |

### AI 연결상 주의점

- 정상적으로 `mock-data.js`가 로드되면 위 질문의 주요 숫자는 Mock 필드에서 읽습니다.
- `mock-data.js`의 숫자를 수정하면 정상 로딩 상태의 AI 답변 수치도 함께 바뀝니다.
- 다만 필드가 누락되면 `demoAnswer()`와 상세 버튼 응답의 `||` 기본값이 대신 표시됩니다. 이 기본값은 `+3.2%`, `-8.1%`, `12~14시`, `3,240,000원`, `1,180,000원`, `+650,000원` 등으로 하드코딩되어 있습니다.
- 따라서 실제 데이터 교체 시 필드 누락을 허용하지 않거나, 향후 기본값을 `데이터 없음` 상태로 바꾸는 검증이 필요합니다.
- 응답 문장 자체는 `mock-data.js`의 `description`을 조합하는 것이 아니라 `demoAnswer()`에 고정되어 있습니다. 데이터 구조가 바뀌면 답변 규칙도 함께 점검해야 합니다.

---

## 5. 회복 플랜 연결 구조

현재 회복 플랜은 계산 엔진이 아니라 `mock-data.js`의 고정 필드와 고정 문구를 화면에 조합하는 시연 구조입니다.

| 단계 | 현재 연결 | 실제 규칙 여부 |
|---|---|---|
| 데이터 | 유동인구 `+3.2%`, 카드소비 `-8.1%`, `focusTime` `12~14시` | 숫자 필드·시간대가 Mock으로 고정 |
| 발견한 요인 후보 | 유동인구는 증가했지만 카드소비가 감소한 소비 전환 공백 | `recoveryPlan.description`, `evidence`에 작성된 고정 문구 |
| 추천 시간대 | `recoveryPlan.focusTime` | 현재는 고정값. 그래프 분석으로 자동 산출하지 않음 |
| 추천 고객층 | `recoveryPlan.customer` | 현재는 고정값. 고객 데이터 필드·분류 규칙 없음 |
| 실행 행동 | `recoveryPlan.action` | 현재는 고정 문구. 최적화·효과 검증 없음 |
| 참고용 금융 효과 | `recoveryPlan.referenceCashflow` `+650,000원` | 계산식 없음. 고정 Mock 값 |

즉, 현재 화면에서 설명 가능한 연결은 다음 수준입니다.

```text
Mock으로 정의한 유동인구·카드소비·시간대
    ↓
Mock으로 정의한 소비 전환 공백 요인 후보 문구
    ↓
Mock으로 정의한 시간대·고객층·운영 행동
    ↓
계산되지 않은 고정 참고용 금융 효과
```

`+650,000원`을 실제 데이터 분석 결과나 실행 후 예상 수익으로 표현하면 현재 코드 근거를 벗어납니다.

---

## 6. 발표 스크립트·심사 Q&A 연결

### 6-1. `12_demo-script.md`

| 발표 주장·수치 | 코드 연결 | 감사 결과 |
|---|---|---|
| 이소현·대구 서문시장·음식점·최근 2년 | 화면은 `user`, `store`를 Mock에서 렌더링 | 화면과 스크립트가 일치하지만 문서 자체는 자동 연동되지 않음 |
| 매출 -12.4% | `metrics.salesChange` | 화면 KPI와 일치. 스크립트 수치는 하드코딩 |
| 카드소비 -8.1% | `metrics.cardChange` | 화면 KPI·AI 답변과 일치. 스크립트 수치는 하드코딩 |
| 유동인구 +3.2% | `metrics.trafficChange` | 화면 KPI·AI 답변과 일치. 스크립트 수치는 하드코딩 |
| 12~14시 소비 전환 공백 | `recoveryPlan.focusTime` 및 인사이트 문구 | 현재 Mock과 일치. 그래프 선 자체는 고정 |
| 20~30대·인근 직장인 | `recoveryPlan.customer` | 회복 플랜·AI와 일치. 근거 데이터·분류 규칙은 없음 |
| +650,000원 | `recoveryPlan.referenceCashflow` | 화면과 문서 표기는 일치하지만 계산 근거는 없음 |

`12_demo-script.md`의 금융 효과 멘트는 감사 후 다음처럼 수정했습니다.

> `+650,000원`은 현재 프로토타입의 고정 Mock 참고값이며, 계산식이 연결된 실제 예측값이 아닙니다.

### 6-2. `13_judge-qa.md`

| Q&A 주제 | 현재 문서 주장 | 코드·문서 근거 판정 |
|---|---|---|
| 기존 상권분석과 차이 | 데이터 조회 → 해석 → 실행 행동 연결 | 현재 P0 화면의 인사이트·회복 플랜·AI 흐름으로 시연 가능 |
| iM뱅크가 해야 하는 이유 | 자금 흐름·금융 체온·운영 정보 연결 | 현재는 금융 관점 UI와 기획 수준. 금융상품 연결은 미래 계획 |
| 소상공인 가치 | 여러 데이터를 한 번에 확인하고 행동으로 연결 | 현재 Mock 화면에서 시연 가능. 실제 운영 효과는 검증 전 |
| 데이터 확보 | 대구 빅데이터활용센터의 허용 범위에서 삼성카드·통신사 집계 결과를 활용할 계획 | 계획 표현으로 작성되어 있어 사실 단정은 아님 |
| AI 필요성 | 계산은 규칙 기반 가능, 설명·대화형 안내에 AI 활용 | 현재 AI는 실제 AI가 아닌 규칙 기반 더미 |
| 금융 연결 | 자금 흐름·금융 체온·참고용 효과 → 실제 서비스에서 상담 연결 계획 | 현재 신청·심사·상품 연결은 없음 |
| 원인 정확성 | 확정 원인이 아니라 요인 후보 | 현재의 데이터·코드 수준과 일치 |
| +650,000원 | 감사 전에는 비교지표·입력값 활용이라고 표현 | 현재 코드에 계산식·입력값이 없어 과장 가능성이 있었음. 감사 후 고정 Mock·산출 근거 미정의로 수정 |

---

## 7. 하드코딩 목록

### 7-1. 현재 Mock과 연결된 항목

- KPI 숫자 표시 영역
- 사용자·매장·기간 표시
- 대표 인사이트 제목·설명
- 그래프 아래 호출 문구
- 회복 플랜의 시간대·고객층·금융 효과·행동
- 자금 흐름 숫자
- 정책 카드
- AI 초기 인사이트 및 5개 대표 질문의 주요 Mock 숫자

이 항목들은 정상적으로 `mock-data.js`가 로드될 때 공통 데이터 원천을 사용합니다.

### 7-2. 화면에 남아 있는 하드코딩 또는 부분 연결

| 위치 | 하드코딩 내용 | 영향 |
|---|---|---|
| 그래프 SVG | 선 경로, 점 위치, 면적 경로 | Mock 숫자를 바꿔도 그래프 모양은 바뀌지 않음 |
| 그래프 SVG 축 | 10시·12시·14시·16시·18시, 높음·낮음 | 실제 시간대 데이터 교체 시 축·시리즈를 함께 교체해야 함 |
| 금융 효과 보조 라벨 | 고정 Mock 참고값 | 계산된 예상 자금 흐름으로 오해하지 않도록 감사 후 수정 |
| 핵심 인사이트 02·03 | 피크 시간 차이, 고객층 차이 설명 | 해당 판단을 만드는 데이터 필드가 없음 |
| HTML 초기 표시값 | 페이지 원문 안의 KPI·자금·플랜 숫자 | JS 로딩 전 또는 Mock 로딩 실패 시 보일 수 있는 fallback |
| `demoAnswer()` 기본값 | 주요 숫자·시간대의 `||` fallback | Mock 필드 누락 시 오래된 숫자가 남을 수 있음 |
| 상세 버튼 응답 기본값 | 같은 숫자의 `||` fallback | Mock 필드 누락 시 오래된 숫자가 남을 수 있음 |
| 발표 스크립트·Q&A | 수치·문장 직접 기입 | `mock-data.js` 수정 시 수동 동기화 필요 |

그래프의 정적 SVG를 이번 감사에서 동적 데이터 구조로 바꾸지 않은 이유는, 차트 시계열 데이터 필드·단위·정규화 기준을 현재 프로젝트에서 확정하지 않았기 때문입니다. 임의의 시계열이나 계산식을 추가하면 실제 데이터 교체 전 근거 없는 구조가 생깁니다.

---

## 8. `+650,000원` 집중 감사

| 점검 항목 | 확인 결과 |
|---|---|
| 값의 정의 위치 | `prototype/mock-data.js`의 `recoveryPlan.referenceCashflow` |
| 화면 사용 위치 | `prototype/main-screen.html`의 `[data-mock="recovery-cashflow"]` |
| AI 사용 위치 | `플랜 자세히 보기` 버튼이 여는 AI 답변에서 Mock 필드로 사용 |
| 발표 사용 위치 | `12_demo-script.md`의 회복 플랜 설명·핵심 수치·주의 표현 |
| Q&A 사용 위치 | `13_judge-qa.md`의 금융 효과 질문 |
| 입력 데이터 | 현재 코드에서 확인되지 않음 |
| 계산식 | 현재 코드에서 확인되지 않음 |
| 특정 가정 | 현재 코드에서 확인되지 않음 |
| 자동 계산 | 되지 않음 |
| 실제 데이터 기반 여부 | 아님. 현재는 AI 생성 Mock Data의 고정 문자열 |
| 화면 표시 방식 | `mock-data.js` 값을 읽어 표시하지만 값 자체는 하드코딩된 Mock 값 |
| 최종 판정 | **산출 근거 미정의** |

따라서 현재 단계에서 `+650,000원`에 대해 설명할 수 있는 정확한 문장은 다음과 같습니다.

> `+650,000원`은 회복 플랜 카드에 보여주기 위한 고정 Mock 참고값입니다. 현재 구현에는 이 값을 계산하는 산식이나 실제 입력 데이터가 없습니다.

반대로 다음 표현은 현재 코드 근거가 없어 사용하면 안 됩니다.

- 비교지표를 실제로 계산해 도출한 값입니다.
- 입력값을 기반으로 산출한 예상 수익입니다.
- 플랜 실행 시 증가할 금액입니다.
- 실제 금융 결과 또는 대출 가능 금액입니다.

---

## 9. 데이터 출처 관련 주장 감사

| 표현 | 현재 저장소 기준 상태 | 발표 시 표현 |
|---|---|---|
| 대구 빅데이터활용센터 | 실제 데이터·반출 결과·접근 권한은 저장소에서 확인되지 않음. 향후 활용 계획으로만 기록됨 | “허용 범위와 제공 방식 확인 후 활용할 계획” |
| 삼성카드 카드소비 | 현재 Mock Data에 실제 카드사 데이터가 없음. 향후 분석 대상으로 기록됨 | “실제 데이터 확보 후 허용된 집계 결과를 반영할 계획” |
| 통신사 유동인구 | 현재 Mock Data에 실제 통신사 데이터가 없음. 향후 분석 대상으로 기록됨 | “실제 데이터 확보 후 허용된 집계 결과를 반영할 계획” |
| AI 생성 Mock Data | 현재 프로토타입에서 실제로 확인되는 데이터 방식 | “현재 화면은 AI 생성 Mock Data 기반” |
| 원천파일 미저장 | README와 화면 안내에 기록된 데이터 원칙 | “원천데이터가 아닌 허용된 집계·분석 결과만 반영” |

`13_judge-qa.md`의 “활용할 계획입니다”라는 표현은 계획형이라 현재 사실로 단정하지 않습니다. 다만 “허용하는 범위”는 제공기관의 실제 허가가 확인되었다는 뜻이 아니므로, 방문·제공 조건·반출 가능 여부를 확인하기 전까지는 확정 사실로 설명하지 않습니다.

---

## 10. Single Source of Truth 적용 상태

### 현재 적용된 부분

- `mock-data.js`가 HTML이 읽는 시연 데이터의 중심 파일입니다.
- KPI·카드·인사이트·회복 플랜·자금 흐름·정책 카드의 주요 표시값은 `renderMockData()`로 연결됩니다.
- AI 답변의 주요 KPI·시간대·고객층·행동·현금·잔액은 Mock 객체를 참조합니다.
- 핵심 인사이트 01의 시간대는 `recoveryPlan.focusTime`을 사용하도록 보완했습니다.

### 아직 완전한 SSoT가 아닌 부분

- 그래프의 시계열 값과 축 라벨은 Mock 객체에 없습니다.
- 회복 플랜의 `evidence`·`description`은 수치 필드를 계산해 만드는 값이 아니라 고정 문자열입니다. Mock 숫자만 바꾸면 이 문장 속 숫자는 함께 바뀌지 않습니다.
- 발표 스크립트와 Q&A는 코드와 자동 연동되지 않습니다.
- HTML·AI 코드의 fallback 숫자는 Mock 필드가 누락될 때 표시되는 별도 값입니다.
- 금융 체온, 예상 매출, 예상 잔액, 참고용 금융 효과는 표시값은 있으나 계산 모델이 없습니다.

현재 상태를 한 문장으로 요약하면 다음과 같습니다.

> 표시 컴포넌트의 SSoT는 부분적으로 적용되어 있지만, 분석·그래프·문서까지 포함한 전체 SSoT는 아직 완성되지 않았습니다.

---

## 11. 실제 데이터 교체 시 필요한 항목

실제 데이터를 확보한 뒤 다음 항목을 확인해야 합니다.

1. 제공기관·데이터 종류·지역·업종·기간·집계 단위·단위·제공 조건을 기록합니다.
2. 실제 카드소비와 유동인구 결과를 현재 `metrics` 및 향후 시계열 데이터 구조에 매핑합니다.
3. `-12.4%`, `-8.1%`, `+3.2%`가 어떤 기간 비교와 분모로 계산되는지 확정합니다.
4. 시간대 그래프의 원자료·단위·정규화 기준을 확정한 뒤 고정 SVG를 교체합니다.
5. 소비 전환 공백의 판단 기준과 임계값을 정의합니다.
6. 추천 고객층의 근거 필드와 분류 기준을 정의합니다.
7. `currentCash`, `expectedSales`, `expectedBalance`의 입력 항목과 계산식을 정의합니다.
8. `financeTemperature`의 비교 집단·산식·주의 기준을 정의합니다.
9. `referenceCashflow`를 계속 사용할지 결정하고, 사용할 경우 계산식·가정·기간·표현을 확정합니다. 기준이 없으면 해당 값을 제거하거나 “산출 전” 상태로 표시해야 합니다.
10. 실제 데이터에 맞춰 `recoveryPlan.description`, `evidence`, `action`과 AI 답변 규칙을 함께 갱신합니다.
11. 발표 스크립트·Q&A·화면 캡처의 모든 숫자를 수동 대조합니다.
12. 원천파일 반출·저장 가능 여부를 다시 확인하고 저장소에는 허용되지 않은 원천데이터를 넣지 않습니다.

---

## 12. 현재 근거가 부족하거나 정의되지 않은 항목

| 항목 | 부족한 근거 | 현재 발표 가능 수준 |
|---|---|---|
| `+650,000원` | 산식·입력값·기간·원천 데이터 없음 | 고정 Mock 참고값이라고만 설명 |
| 금융 체온 `62°` | 체온 변환 산식·업종 평균·비교군 없음 | 금융상태 참고 지표 UI 시연 |
| 예상 매출·예상 잔액 | 매출·지출 입력과 계산식 없음 | Mock 자금 흐름 카드 시연 |
| 12~14시 공백 | 그래프 원자료·전환율 계산식 없음 | Mock이 설정한 요인 후보 시연 |
| 20~30대·인근 직장인 | 연령·직업·소비 고객 데이터와 선정 규칙 없음 | Mock 추천 고객층 시연 |
| 피크 시간 불일치 | 피크를 계산하는 시계열 필드 없음 | 고정 인사이트 문구로만 표시 |
| 정책 “신청 조건 일치” | 실제 공고 API·자격 요건 검증 없음 | Mock 정책 카드. 실제 신청 가능 의미 아님 |
| 삼성카드·통신사 데이터 활용 | 실제 제공·접근·반출 조건 확인 자료 없음 | 향후 계획으로만 설명 |
| 최근 2년 | 정확한 시작·종료일 없음 | 화면 표시용 기간 라벨 |

---

## 13. 실제 데이터 확보 후 교체 순서

```text
제공 조건·반출 범위 확인
    ↓
현장에서 집계 결과와 메타데이터 기록
    ↓
지역·업종·기간·단위 정리
    ↓
현재 Mock 필드와 실제 필드 매핑
    ↓
변화율·시간대·고객층 산식 확정
    ↓
그래프 데이터 구조와 화면 교체
    ↓
인사이트·회복 플랜 규칙 교체
    ↓
AI 답변 규칙과 근거 문장 교체
    ↓
금융 효과 산출 근거 확정 또는 표시 제거
    ↓
발표 스크립트·Q&A·캡처 대조
    ↓
원천데이터 비포함 상태로 최종 테스트
```

실제 데이터 교체의 최소 안전 기준은 다음과 같습니다.

- 하나의 핵심 수치는 가능한 한 데이터 모델의 한 필드에서 읽습니다.
- 계산되지 않은 값은 “참고용 Mock” 또는 “산출 기준 미정의”라고 표시합니다.
- 실제 데이터가 없는 부분을 실제 결과처럼 채우지 않습니다.
- 발표 문서의 숫자는 화면과 수동 대조한 뒤 사용합니다.
- 실제 데이터 교체 후에도 매출 감소를 확정 원인이 아닌 요인 후보로 표현합니다.

---

## 감사 후 변경 사항

- `prototype/main-screen.html`: 핵심 인사이트 01의 시간대 문구를 `recoveryPlan.focusTime`과 연결했습니다. 시각 디자인과 P0 흐름은 변경하지 않았습니다.
- `12_demo-script.md`: `+650,000원`을 고정 Mock 참고값으로 설명하도록 수정했습니다.
- `13_judge-qa.md`: `+650,000원`에 계산식·입력값이 없다는 사실을 반영했습니다.
- `README.md`: 회복 플랜 금융 효과를 현재 고정 Mock 값으로 명확히 표시했습니다.
- `14_data-traceability.md`: 본 감사 문서 신규 작성

변경하지 않은 파일도 모두 감사했습니다.

- `prototype/mock-data.js`: 현재 구조가 이미 시연 데이터의 중심으로 사용되고 있어 값과 구조를 변경하지 않았습니다.
- `06_screen-spec.md`: 금융 효과가 현재 고정 Mock이라는 점과 실제 데이터 연계 후 산출 기준을 정의해야 한다는 점을 명확히 했습니다.
- `11_demo-hardening.md`: 더미 데이터·참고용 시나리오 한계를 이미 명시하고 있어 수정하지 않았습니다.

## 14. 2026-09-03 회의 개정에 따른 추적성·전환 계획

### 14-1. 기존 감사 결과 보존

기존 본문의 코드·필드·하드코딩·산식 미정의 판정은 변경하지 않습니다. 12번 스크립트와 13번 Q&A에 현재 시연과 목표 계획을 구분하는 안내를 추가했습니다. 신규 목표 기능은 코드 검증 전이며 본 감사의 통과 항목으로 간주하지 않습니다.

### 14-2. 기존 Mock에서 목표 데이터로 연결할 항목

| 기존 값·필드 | 목표 원천/결과 | 교체 전 필수 확인 |
|---|---|---|
| user.name / store.* | user_profiles / store_profiles | 예시 사용자와 실제 계정·가게 구분, 사업장 소재지 확정·규모 기준 미정 |
| metrics.salesChange | POS sales_expenses → analysis_insights | 취소·환불·중복·기간·증감 분모 |
| metrics.cardChange | card_consumptions → 공통 분석 | 제공처·기간·업종·모집단·단위 |
| metrics.trafficChange | foot_traffic → 공통 분석 | 실제 매장 방문과 구분, 카드자료와 비교 가능성 |
| metrics.financeTemperature / financeStatus | 금융 비교 분석 | 62°의 승계 금지, 산식·경계·단위 정의 |
| insight.*와 고정 SVG | 실제 시계열·요인 후보 | 정규화·표본·시간 기준, 데이터와 그래프 동시 교체 |
| recoveryPlan.focusTime / customer | 검증된 시간·고객 분석 | 12~14시·20~30대·직장인 고정값 제거 |
| recoveryPlan.action | 분석 근거에 연결된 실행 행동 | 고정 예시와 실제 규칙 결과 구분 |
| recoveryPlan.referenceCashflow | 정의된 금융 참고 결과 또는 null | +650,000원 산출 근거 미정의, 임의 산식 금지 |
| cashflow.* | cashflow_inputs와 검증된 입출금 | 현금 기준일·정산 시점·예상 매출/입금 구분 |
| policy.* | policy_support_programs + 프로필 매칭 | 실제 공고·원문 자격·상태·조건 근거 |

KPI·그래프·요인·플랜·챗봇·AI 비서·PDF가 동일 analysis_run_id와 source_ids·rule_version을 사용하도록 설계했습니다. 최신 사용자 확인에 따라 POS를 포함한 생성 데이터 시연을 허용하고 synthetic_demo와 dataset_mode로 구분합니다. 아직 실제 코드의 공통 데이터 원천 전환을 구현한 것은 아닙니다.

### 14-3. 회의의 새 예시·주장 처리

| 예시·주장 | 현재 근거 상태 | 목표 처리 |
|---|---|---|
| 매출 +10%, 매입비 +22% | 회의 설명 예시 | 실제 금액·기간·지출·재고 확인 전 이익 감소 단정 금지 |
| 14~15시 식사·청소 / 16~17시 준비 | 운영 예시 | 지난 7일 실제 관측·표본·규칙 확인 후 안내 |
| 서문시장 50대 방문객 다수 | 원자료·결과·기간 검증 필요 | 확인 전 사실로 표시하지 않음 |
| 주변은 디저트, 우리는 식사 메뉴 | 경쟁 인사이트 예시 | 양측 메뉴 출처 필요 |
| 주변 10시, 우리는 7시 개점 | 경쟁 인사이트 예시 | 양측 영업시간·기준일 필요 |
| 사원증 n% 할인 | 조건부 행동 예시, 할인율 미정 | 직장인 근거·비용 영향 확인, 수치 임의 생성 금지 |
| 축제 추천 상품 | 과거 품목 실적 미확인 | 행사-품목 판매 연결 없으면 인기 상품 주장 금지 |
| 정책 적합도 % | 산식·가중치 미정 | 조건별 근거 표시, 점수는 null |
| 금융·창업 + 제목 3키워드 | 승인된 1차 수집 규칙 | 공급처 분류명·실제 목록 검증 필요 |
| 나우브리프 1주 전 | 승인된 안내 요구 | 일정·업종 근거와 일반 문구 구분 |

### 14-4. 데이터 연결 후 재감사

1. 생성 시연 자료는 생성 사실·구조·계산 일관성 확인. 실자료는 권한·출처·컬럼·기간 별도 확인.
2. 분석 정의·표본·단위·개별 산식 확정.
3. DB→분석→화면→대화→PDF의 필드별 추적표 갱신.
4. Mock 숫자 하나 변경뿐 아니라 실제 입력 변화가 모든 소비 영역에 반영되는지 검사.
5. 자료 없음에서 고정 숫자·고정 고객층·가짜 답변이 나오는지 검사. 명시된 생성 자료 사용과 실자료 실패의 자동 Mock 대체를 구분.
6. +650,000원·62° 등 미정의 값을 실제 효과·점수로 주장하지 않는지 검사.
7. 실제 구현에 맞춰 12·13번 문서와 최종 영상을 갱신.

미정 사항은 [PRD C-01~C-11](01_prd.md#8-구현-전-확인할-사항), 데이터 계약은 [04_data-spec.md](04_data-spec.md), 재시험은 [09_test-plan.md](09_test-plan.md)를 따릅니다. 이번 개정은 새 데이터를 확보하거나 새로운 산식을 만든 작업이 아닙니다.

### 14-5. 추가 확인 사항

- 프로필 지역: 사업장 주소지로 확정. 거주지와 구분합니다.
- AI 리포트의 새 명칭: AI 비서. 우측 단순 질의응답은 챗봇입니다.
- 생성 데이터: POS 포함 공모전 시연에 사용 가능, 생성 자료임을 표시합니다.
- 위젯 이동: 사용자 요청으로 보류하고 AI 비서 화면 설계·구현 시 재확인합니다.
