# 매출진단 개발 방향과 구현 안내

## 2026-09-18 최종 수정: 월간 요약 고정·표기 정리

이 절이 아래 이전 화면 설명보다 우선합니다. 매출 요약 4개 카드와 요약 진단은 **직전 완료월의 가게 전체 집계**입니다. 필터용 `result`와 별도의 `monthlyResult`를 만들어 품목·요일·시간·현재 시각에 영향을 받지 않게 했습니다. 상세 차트·CSV·회복전략의 필터 근거는 기존처럼 선택 조건을 따릅니다.

‘순이익(추정)’은 요청에 따라 ‘순이익’으로 표기합니다. 계산은 동일 월 매출에서 입력 지출을 뺀 값이며, 미입력 비용·세금·재고 조정이 없는 시연값이라는 설명을 계산 기준에 유지합니다. 기존에는 필터 적용 매출 옆에 월 전체 이익을 표시해 범위가 달랐습니다.

합성 비용 설정도 공유 원자료에서 조정했습니다. 돼지고기 일 기준 매입량 20kg·단가 20,000원, 월 인건비 12,000,000원·관리비 1,100,000원·기타 2,400,000원이며 월세는 2,300,000원입니다. 기존 주말·날짜별 매입량 변화와 8월 이후 단가 계수를 유지합니다. 8월 총매출은 43,085,000원, 지출은 33,790,900원, 차감 후 표시는 9,294,100원입니다. 이 값은 가상의 고깃집 설정으로 업종 평균을 뜻하지 않습니다. 비용 원자료를 읽는 지출 진단·보고서·홈 참고 지수도 함께 일치합니다.

분석기간은 요약 카드 아래 공통 줄로 옮겼습니다. 접힌 필터는 `품목 / 시간대 / 요일`로 나누고, 시간 옵션은 ‘전체’ 및 ‘12시~13시’ 형식으로 단순화했습니다. 현재 시간은 ` - 현재 시간대`를 붙입니다. 매출·지출 기준은 제목/설명 목록으로 정리하며 `disclosure-polish.css`를 차트·행사·정책의 접이식 상세와 공유합니다.

## 2026-09-18 추가 수정: 정오~자정·일별과 요일별 통합

회복전략의 최신 화면은 **대화·리포트 단일 카드**입니다. 이 문서 아래의 진단별 제안 카드와 시간대 운영 참고 UI는 제거되었으며, 진단 계산 모듈과 조건 변경 시 기존 리포트를 무효화하는 처리는 유지합니다. PDF·실행 기록에는 대화에 실제 표시된 행동만 포함합니다.

이 절이 아래 이전 구성보다 우선합니다. 시간 필터는 **12:00~24:00의 12개 1시간 구간**입니다. ‘전체’는 해당 범위 전체를 사용하며 POS 원기록의 10분 구분은 CSV에 보존합니다. 17~23시 외 시간의 매출을 생성하지 않고, 총매출·품목별 합계와 시간별 합계를 일치시킵니다.

화면은 매출 요약 → **일별 매출 진단·요일별 진단을 함께 배치한 섹션** → 품목별 진단 → **지출 진단**, 총 4개입니다. 일별과 요일별은 넓은 화면에서 좌우, 좁은 화면에서 위아래로 배치합니다. 품목별 매입금액의 증감액은 기존 매출 증감과 같이 **▲ 증가(빨강) / ▼ 감소(초록)**로 표시합니다.

최종 배치에서는 일별·요일별과 지출의 두 카드를 **1:1 폭·같은 높이**로 맞췄습니다. 일별·요일별 제목·범례·344px 차트 영역·상세 안내 기준선을 공유하며, 상세 내용을 펼쳐도 카드 하단을 맞춥니다. 긴 일별 원자료만 내부 스크롤하고 일반 품목표는 전체 높이를 표시합니다. 차트는 청록 `#137C78`과 회청 `#78869B`를 사용합니다. `final-polish.css`가 홈을 제외한 여백과 자연스러운 연속 스크롤을 적용합니다.

검사: `sales-diagnosis.test.cjs`, `sales-ui-approved.test.cjs`, `sales-recovery-context.test.cjs`, `sales-diagnosis.visual.test.cjs` 및 통합 화면 검사. 실제 데이터나 회계상 이익을 새로 산출한 변경은 아닙니다.

상단 필터는 `meeting-ui.js`에서 첫 섹션 밖의 공통 고정 영역으로 옮깁니다. `diagnosis-filters.css`를 상권분석과 공유하며, 매출·상권의 모든 섹션에서 보입니다. 작은 화면에서는 접힌 조건 요약을 기본으로 표시하고 펼쳐서 수정합니다. 필터 높이를 관찰해 섹션 이동 위치를 보정하며 홈에는 적용하지 않습니다.

## 2026-09-18 최신 회의 반영: 완료된 한 달·요일 진단·지출분석

**이 절이 현재 기준입니다. 아래 같은 날의 이전 기록과 과거 구성보다 이 절을 우선합니다.**

- 공통 `diagnosisPeriod()`가 한국시각 기준 **직전 완료된 한 달**을 전달합니다. 9월 18일 접속 시 8월 1~31일이며 월이 바뀌면 갱신합니다. 증감 계산의 겹치지 않는 같은 요일 구성 비교는 유지하되 기간 선택과 별도 비교 날짜 행은 표시하지 않습니다.
- **매출 요약 → 매출 변화 → 요일별 진단 → 품목별 진단 → 지출분석**, 5개 섹션입니다. 번호를 없애고 일별 매출 비교를 전체 너비로 확장했습니다. 오른쪽 매출 변화 분해 카드와 요일×2시간대 표는 삭제했으며 요일별 일평균 매출 그래프와 상세 집계는 유지합니다.
- **순이익(추정)**은 같은 달 가게 전체 매출에서 입력 지출을 뺀 값입니다. 미입력 비용을 제외한 참고치라는 설명을 바로 아래 표시하며 회계상 확정 이익으로 해석하지 않습니다. 품목·요일·시간 필터는 이 값과 지출분석에 적용하지 않습니다.
- 지출분석은 `D.expenses`와 `D.purchases`에서 총지출·매출 대비 비율·지출 분류와 비중·비교·매입 품목별 수량/금액을 계산합니다. 일별 매출·CSV·원자료 상세 기능은 유지합니다.
- 요일은 기본 단일 선택이며 복수 선택 체크를 켜야 중복 선택됩니다. 시간대는 현재 한국시각이 포함되는 2시간 구간을 기본으로 하고 수동 변경할 수 있습니다. 영업시간 밖에는 전체 시간을 사용합니다. 회복전략은 진단 조건을 이어받고 추가 보고서의 별도 기간과 구분합니다.
- 글꼴·색상은 유지하며 모든 섹션 간격을 일관되게 줄였습니다. PC 기본 화면에서는 핵심 카드·그래프·하단 설명이 한 화면에 들어오며 작은 화면은 세로로 이어집니다.

검증: 매출 계산 44개 조건 조합, 시간대 경계, 필터/CSV/원자료 일치, 지출 필터 독립성, 회복전략 연결을 확인했습니다. `sales-diagnosis.visual.test.cjs`는 현재 5개 섹션에 맞췄고 `diagnosis-layout.browser.test.cjs`와 `meeting-structure.browser.test.cjs`로 구성·반응형 배치를 확인합니다.

## 2026-09-18 이전 구현 기록: 이번 달 진단과 간결한 필터

**이 절과 아래 내용은 이전 개발 이력입니다. 현재 기준은 위의 최신 회의 반영 절입니다.**

- 매출진단에는 매출 요약·매출 변화·품목별 진단·요일/시간대 진단, 4개 섹션을 유지합니다. 수치 해석과 자료 한계는 남기고 품절·메뉴 노출·가격·준비 수량 등의 운영 점검 제안은 **회복전략**으로 모았습니다. 요약에서 회복전략으로 이동할 수 있습니다.
- 분석 범위는 **한국시각 기준 이번 달 1일~오늘**로 고정합니다. 기간 선택과 필터 아래 비교 날짜 안내를 제거했으며, 증감·변화 차트에 필요한 이전 기간 계산은 유지합니다. 자료가 없는 날짜를 0원으로 간주하거나 다른 달 결과로 대체하지 않습니다.
- 대분류·중분류·요일·시간 필터, 증감액/증감률 정렬, 날짜·품목·요일별 원자료 확인, 전체 선택 조건 CSV 기능을 유지합니다. 요일은 기본 단일 선택으로 월요일 다음 화요일을 누르면 화요일만 남습니다. **복수 선택**을 체크하면 여러 요일을 토글하며, 체크를 끄면 마지막으로 선택한 요일을 유지합니다.
- 현재 영업시간은 **17~23시**, 원기록은 **10분**, 선택 시간대는 **17~19시 / 19~21시 / 21~23시**입니다. 접속 한국시각의 구간을 기본 선택하고 직접 변경할 수 있습니다. 영업시간 외에는 **모든 시간 · 현재 영업시간 외**를 표시합니다. 초기화도 현재 시각을 기준으로 다시 선택하며 화면·CSV·회복전략 근거는 같은 시간/요일 필터를 사용합니다.
- 모든 매출진단 섹션의 위아래 간격을 일관되게 줄여 핵심 내용이 한 화면에 들어오도록 조정합니다. 필터에서는 기간·비교 문구로 차지하던 행을 제거하며 기존 글꼴과 디자인은 유지합니다.
- 현재 요약 지표는 매출·일평균 매출·매출−지출 차이·결제 객단가입니다. 결제 객단가는 선택 품목이 포함된 고유한 유효 결제 건수를 사용합니다. 정상 영업일과 누락 자료를 구분하며, 지출과 매출−지출 차이는 품목·요일·시간 필터와 별도의 가게 전체 기준입니다.
- `IM_SALES_DIAGNOSIS.getRecoveryContext(period)`는 현재 매출 필터와 비교 기준으로 다시 계산하여 `{ source, title, scope, findings, reason? }`을 반환합니다. 각 finding은 `{ id, title, evidence, action, caveat, status }`이며 정상 제안은 `ready`, 자료 부족은 `insufficient`와 빈 action으로 구분합니다. 자료가 없으면 빈 findings와 reason을 반환합니다. 이 호출은 매출 화면의 표시 상태를 바꾸지 않습니다.
- 회복전략은 매출 근거와 해당 행동을 먼저 보여주고 상권 근거와 행동을 이어서 표시합니다. 제안의 근거·범위·한계는 실행 기록과 PDF의 다음 실행 항목에도 전달합니다. 특정 품목에 고정된 재고 안내 대신 현재 선택 결과의 상위 품목을 사용합니다.
- 회복전략 안의 추가 대화와 PDF 매출·지출 요약은 선택 기간의 가게 전체 기준입니다. 사후관리는 실행일 전후 각 7일의 같은 영업시간에 대한 매장 전체 매출 비교이며, 매출진단의 품목 필터와 구분합니다. 실행 기록은 브라우저에 보관하고 실제 서버 저장·인증·실데이터 연결은 구현하지 않았습니다.
- 홈은 진단 화면과 달리 **접속 시간대의 행동 안내를 제공하는 예외**입니다. 홈 배너는 매출진단·상권분석·지원사업·회복전략의 4개입니다. 우측 iM비서는 서비스 설명 FAQ 3개를 제시하며 현재 외부 API를 호출하지 않습니다. 향후 백엔드도 FAQ와 무료 경량 AI를 우선하고 유료 고성능 AI는 사용하지 않는 방향이며, 호스팅까지 무료라고 확정한 것은 아닙니다.

매출 계산 검사는 현재 44개 기간·필터 조합과 한국시각 시간대 경계 9개를 확인합니다. UI 검사는 단일/복수 요일 전환, 현재 시간대 초기화, 선택 조건과 CSV 금액·10분 원기록 일치를 포함합니다. 회복전략 검사는 현재 필터/기간, 화면 미변경, 누락·비교 불가·0 기준값에서의 제안 보류를 포함합니다. 아래 세 매출 검사는 이번 변경 후 통과했으며 사후관리 검사는 이전 통합 시 통과한 회귀 검사입니다.

```powershell
node prototype/tests/sales-diagnosis.test.cjs
node prototype/tests/sales-ui-approved.test.cjs
node prototype/tests/sales-recovery-context.test.cjs
node prototype/tests/aftercare.test.cjs
```

아래의 과거 6개 섹션용 `sales-diagnosis.visual.test.cjs`와 당시 사례 수는 이번 4개 섹션 검증 결과로 간주하지 않습니다.

---


기준일: 2026-09-12. 정적 HTML 프로토타입의 프론트엔드 구현이며 실제 POS·DB·외부 AI 연동은 포함하지 않습니다.

## 1. 현재 화면 진단과 개발 방향

개편 전 화면은 매출·지출 요약, 상권과 매출 비교, 요일별 매출, 지출 구조, 매입금액 순위, 일부 판매 집계로 구성되었습니다. 기간 선택은 있었지만 품목·시간대별 필터, 일별 이전 기간 비교, 매출 증감의 수치 분해, 전체 집계 확인과 상세 탐색이 부족했습니다.

사용자가 답을 얻어야 하는 질문은 다음과 같습니다.

1. 순매출과 일평균 매출이 얼마나 변했는가?
2. 판매 수량과 판매 1개당 평균 금액 중 무엇이 달라졌는가?
3. 어떤 품목·요일·시간대에서 변화가 컸는가?
4. 가게 전체 지출까지 고려하면 운영 상태는 어떠한가?
5. 오늘 무엇을 점검하고 어떤 지표로 결과를 확인해야 하는가?

**현재 상태 → 변화가 발생한 지점 → 실행할 일 → 근거 확인** 순서로 구성했습니다. 기존 `card`, `v-card`, `metric-card`, `v-button`, `v-select`, `v-table`, 화면 단위 스크롤 구조와 Noto Sans KR·민트색 디자인을 재사용했습니다.

## 2. 화면 순서·지표·데이터

| 순서 | 섹션 | 지표와 시각화 | 필요한 데이터 |
|---|---|---|---|
| 1 | 진단 요약 | 규칙 기반 요약, 순매출·일평균·판매 수량·판매 1개당 금액 카드 | 일자, 판매·취소·순판매 수량, 순매출 |
| 2 | 매출 변화 | 두 기간의 일별 선 그래프, 수량·개당 금액의 영향 막대, 일별 비교 표 | 동일 기준의 두 기간 판매 집계 |
| 3 | 품목별 진단 | 순매출·비중·증감액·증감률·순판매·취소 수량 표 | 품목 ID·이름, 수량, 금액 |
| 4 | 요일·시간대 | 요일별 일평균 비교 막대, 요일×시간대 색상표 | 날짜, 시간, 관측 일수, 순매출 |
| 5 | 지출과 실행 | 지출 분류별 현재·이전 비교, 점검 과제·근거·확인 지표·체크리스트 | 지출 분류·금액, 선택 조건의 분석 결과 |
| 6 | 집계 근거 | 전체 필터 결과, 합계, 페이지 이동, CSV 다운로드 | 집계 ID·날짜·시간·품목·판매·취소·순판매·순매출 |

## 3. 우선순위와 구현 범위

**필수 / 구현:** 월·최근 7일·직접 기간 선택, 직전 동일 일수·같은 요일 구성 비교, 품목·시간대·평일/주말 필터, 초기화, 모든 매출 분석 재계산, 품목 표 정렬, 날짜·품목·요일·시간대 상세 대화상자, 계산 기준, 비교 자료 부족·빈 결과 처리, 모바일 표 가로 스크롤, 키보드 조작.

**추가 / 구현:** 현재 조건의 전체 판매 집계 CSV 다운로드, 조건별 점검 완료 상태. 체크리스트는 페이지 메모리에만 유지되며 새로고침하면 초기화됩니다.

**후속 / 미구현:** 주문 건수·주문당 객단가, 판매 채널, 신규·재방문 고객, 목표 매출, 재고·레시피 기반 품목별 이익, 실제 상권·CCTV 연계, 실행 전후 성과의 자동 저장·추적.

## 4. 계산·해석 기준

- 순매출은 기존 `netAmount`, 순판매 수량은 기존 `netQuantity` 합계입니다. 행은 날짜·시간·품목별 집계이며 주문이나 영수증이 아닙니다.
- 일평균은 선택한 요일의 관측 일수로 나눕니다. 휴무·미수집 상태를 구분하는 자료는 없습니다.
- 판매 1개당 평균 금액 = 순매출 ÷ 순판매 수량. 주문당 객단가로 표시하지 않습니다.
- 수량 영향 = (현재 수량 − 이전 수량) × 이전 개당 평균 금액.
- 개당 금액 영향 = 현재 순매출 − 현재 수량 × 이전 개당 평균 금액.
- 두 영향의 합은 실제 매출 증감액과 일치합니다. 개당 금액 변화에는 메뉴 구성 변화도 포함되므로 가격 변화로 단정하지 않습니다.
- 기준값이 0이면 증감률을 산출하지 않습니다. 판매 수량이 0이면 개당 평균 금액은 표시하지 않습니다.
- 비교 기간에 필요한 날짜의 집계가 모두 존재하는지 확인합니다. 일부만 있으면 이전 값·증감률·요인 분해·점검 후보를 만들지 않습니다. 현재 목업의 완전성 검사는 날짜 단위이며 실연동 시 시간·품목별 누락 여부도 별도로 받아야 합니다.
- 같은 요일 구성은 선택 일수 이상인 최소 7일 배수만큼 이전으로 이동합니다. 31일 기간이면 35일 이전으로 이동하므로 두 기간은 겹치지 않습니다. 8월 전체와 같은 요일 구성의 비교는 6월 말 자료가 없어 비교 불가로 표시됩니다.
- 시간 필터는 시작 시각을 포함하고 끝 시각은 제외합니다. 생성 자료는 08~20시입니다.
- 품목·시간·요일 필터는 매출진단 내부에 적용됩니다. 홈·iM챗봇·기존 PDF는 전체 품목 기준이므로 이 범위를 화면에 명시합니다.
- 가게 전체 지출은 같은 분석 기간을 따르지만 품목·시간 필터에 따라 나누지 않습니다. 비용 배분 자료가 없기 때문입니다. 매출−지출 차이는 영업이익·현금보유량이 아닙니다.
- 실행 과제는 감소한 품목·시간대에서 생성하는 점검 후보입니다. 원인이나 매출 증가 효과를 확정하지 않습니다.

## 5. 코드 구조와 데이터 교체

- `prototype/meeting-data.js`: 기존 생성 자료. 이번 매출진단은 같은 원자료를 재사용합니다.
- `prototype/sales-diagnosis-data.js`: DOM과 분리한 계산 모듈. `analyze(source, period, filters)`가 화면에 필요한 결과를 반환합니다.
- `prototype/sales-diagnosis.js`: 6개 섹션 렌더링, 필터 상태, 상세 보기, CSV와 체크리스트.
- `prototype/sales-diagnosis.css`: 기존 디자인을 확장하는 `sd-` 범위의 스타일.
- `prototype/meeting-ui.js`: 기존 분석 기간을 전달하고 6개 화면 그룹과 연결합니다. 다른 메뉴의 계산은 기존 모듈을 사용합니다.

현재 계산 모듈의 입력 계약:

```js
source = {
  records: [{ id, date, hour, itemId, quantity, cancellations, netQuantity, netAmount }],
  menu: [{ id, name }],
  analyze(period) { /* 가게 전체 지출 및 분류별 비교 결과 */ }
};
period = { start: 'YYYY-MM-DD', end: 'YYYY-MM-DD' };
filters = {
  comparison: 'previous' /* 또는 weekday */,
  item: 'all' /* 또는 품목 ID */,
  hour: 'all' /* morning / lunch / afternoon / evening */,
  dayType: 'all' /* weekday / weekend */
};
```

실데이터 전환 시 서버 응답을 위 계약에 맞추는 변환 계층을 추가하고, 날짜 범위·출처·갱신일의 고정 문구도 응답 메타데이터로 교체해야 합니다. 현재 인증·로딩·통신 오류·서버 저장은 구현하지 않았습니다. 현재 코드가 실자료 연동까지 완료되었다는 의미는 아닙니다.

## 6. 실행과 검증

`prototype/main-screen.html`을 열고 **매출진단** 메뉴를 선택합니다. `#analysis` 주소 조각으로 직접 진입할 수도 있습니다.

```powershell
node prototype/tests/sales-diagnosis.test.cjs
node prototype/tests/meeting-preview.test.cjs
# Playwright가 설치된 경로를 NODE_PATH에 설정한 뒤 실행
node prototype/tests/sales-diagnosis.visual.test.cjs
```

계산 검사는 240개 기간·필터 조합의 합계, 증감 기여도, 누락·부분·빈 기간·0 분모, 실행 근거, 가게 전체 지출 범위를 확인합니다. 브라우저 검사는 필터·기간·정렬·상세 보기·색상표·대화상자·체크리스트·페이지·CSV와 360/390/768/1366/1920px 배치를 확인합니다. 기본은 동작 줄이기 설정이며 `IM_TEST_MOTION=normal`로 일반 스크롤 환경도 검사할 수 있습니다.

검증 이미지 위치: `tmp/sales-diagnosis-qa/`. 이 폴더는 로컬 검증용이며 배포에 필요한 파일이 아닙니다.
