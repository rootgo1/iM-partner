# iM파트너 시스템 아키텍처

- 개정일: 2026-09-09 / 정적 HTML 생성 시나리오 구현·목표 설계 및 블록체인 P2 조건부 참조 구조
- 현재 실행 구조와 목표 구조를 구분합니다.
- 관련: [데이터](04_data-spec.md), [DB](07_db-schema.md), [API](08_api-spec.md)

## 1. 현재 실행 구조

브라우저 → index.html → prototype/login-preview/index.html → 로그인 없는 데모 또는 임의 입력 확인 → 선택한 prototype/main-screen.html 해시 화면 → meeting-data.js·report-pdf.js 로드 → meeting-ui.js 실행 → 로그아웃 시 비로그인 화면 복귀.

현재는 DB·백엔드·외부 API·실제 CCTV 영상 분석·지도·실제 사용자 인증이 없는 정적 시안입니다. 비로그인 화면은 계정 없이 데모로 들어가거나 비어 있지 않은 임의 입력만 확인해 메인으로 이동하며, 값을 전송·저장하지 않습니다. 로그아웃은 메인 문서를 비로그인 화면으로 교체해 브라우저 메모리 상태를 종료할 뿐 실제 세션을 폐기하지 않습니다. `meeting-data.js`가 기간별 POS·지출·상권 생성 자료와 고정 CCTV·POS 회복 시나리오를 제공하고, `meeting-ui.js`가 7개 화면·iM챗봇·AI 비서를 연결합니다. 회복 시나리오는 대시보드·회복 탭·iM챗봇이 공유하지만 선택 기간·프로필과는 분리되어 있습니다. 실행 상태는 브라우저 메모리에만 반영되고 후속 비교값은 생성하지 않습니다. `report-pdf.js`는 서버 없이 생성 POS·지출·상권 요약 PDF를 만들며 CCTV·POS 회복 내용은 아직 포함하지 않습니다. iM챗봇과 AI 비서는 외부 AI가 아닌 키워드 규칙으로 동작합니다.

## 2. 목표 구조

```text
반응형 화면: 공통 메뉴 / 대시보드 / 상권 / 매출·지출 / 회복 / 정책 / AI 비서 / 프로필
                    ↕ 모든 탭의 iM챗봇
Django 웹 화면 또는 DRF API
                    ↓ 입력·권한 확인
공통 서비스 계층
    ├─ 가게·프로필·분석 조건
    ├─ 데이터 조회·집계·요인 후보
    ├─ CCTV·POS 전환 퍼널·회복 실험
    ├─ 지도·상권 보조 근거·현재 시간 안내
    ├─ 정책 조건 매칭
    ├─ iM챗봇: 간단한 조회·설명
    └─ AI 비서: 대화형 분석·PDF 생성
                    ↓
Django ORM / MySQL
                    ↑
생성 시연 CCTV 집계·POS·지출 등(표시 필수) / 권한 있는 실자료 / 허용된 카드·유동인구 결과 / 지도·공공 자료
```

지도 SDK는 화면에 위치를 표시하고, 상권·점포 및 주소 좌표 변환의 실제 연동 방식은 제공처 확인 후 결정합니다. 지도 표시 기능과 경쟁 근거 수집은 별도 역할입니다.

## 3. 기술과 역할

| 영역 | 방향 |
|---|---|
| 화면 | 현재 HTML/CSS/JavaScript 톤 유지, Django Template 중심으로 확장 |
| 요청 | Django Views와 DRF, 같은 공통 서비스를 호출 |
| 분석 | Python, pandas·numpy; 계산은 서버 한 곳에서 수행 |
| 저장 | MySQL, Django ORM, 기능별 소유권·출처 연결 |
| 수집·입력 | httpx, openpyxl 등 기존 도구; 제공처 계약 검증 후 연결 |
| 인증 | Django 인증 기반 비밀번호 해시·세션·가게 권한 |
| 지도 | 카카오맵 활용 요구; 키·도메인·이용 조건 확인 필요 |
| CCTV 집계 | 기존 CCTV 활용 가능성을 검토하며 영상 대신 익명 시간대 집계값을 서비스에 연결; 공급 방식·모델·정확도·보관 기준 미정 |
| iM챗봇·AI 비서 | 구조화 결과를 우선 사용; 외부 LLM 선택 미정 |
| PDF | 브라우저 로컬 생성·다운로드는 구현, 서버 생성·저장·소유권·별도 인쇄 흐름과 도구는 미정 |
| 품질 | pytest·pytest-django, ruff |
| 영수증 | Pillow 업로드 준비, OCR은 후순위 |
| 블록체인 | 현재 구현·확정 구조에 미적용. 다기관 독립 검증 수요와 중앙 감사 기록·전자서명 대비 실익이 확인되고 별도 PoC 승인을 받은 경우에만 P2 조건부 참조 구조를 구체화 |

새 프론트 프레임워크, n8n, 특정 유료 AI, 새 학습모델, 블록체인을 이번 구현에 도입하지 않습니다. 블록체인은 [10절의 도입 게이트](#10-p2-조건부-블록체인-참조-구조현재-미적용)를 모두 충족하고 별도 승인한 경우에만 PoC 구조에 추가합니다. 그전에는 현재 목표 구조·기능 모듈·구현 순서의 의존 기술이나 보안 수단으로 전제하지 않습니다.

## 4. 목표 공통 결과와 현재 예외

가게·기간·자료 버전·계산 규칙 → analysis_runs → 정량 지표·근거·요인 후보 → 모든 표시 영역.

- 목표 서비스에서는 KPI, 그래프, 회복 플랜, iM챗봇, AI 비서와 PDF가 같은 결과를 사용합니다.
- 정량 계산을 화면과 AI가 각각 수행하지 않습니다.
- 소스 ID·단위·표본·누락과 규칙 버전을 기록합니다.
- 가게·기간 변경 시 오래된 결과를 새 결과처럼 보여주지 않습니다.
- 처리 상태와 자료 충분성 상태를 분리합니다.
- 자동 예측·체온계·정책 % 등 미정 산식은 완료 결과를 만들지 않습니다.

현재 시안에서는 기간별 POS·지출·상권 분석이 KPI·그래프·일부 iM챗봇·AI 비서·PDF에 연결됩니다. 고정 CCTV·POS 회복 시나리오는 대시보드·회복 탭·iM챗봇에 별도로 연결되어 있고, 기간별 분석·프로필·PDF와 동일한 analysis_run으로 묶이지 않았습니다.

## 5. 기능 모듈

| 모듈 | 책임 |
|---|---|
| 가게·프로필 | 사용자 정보, 사업장 정보, 계정·소유권, 분석 조건 |
| 상권 | 카드소비·유동인구, 요일·시간·고객층·경기일 비교 |
| 매출·지출 | DB POS, 비용 5분류, 품목 매입·재고 조건 확인, 현금흐름 |
| 금융상태 | 정의된 업종 비교 결과를 대시보드 체온계로 제공 |
| 회복 플랜 | CCTV 통행·체류·입장 + POS 결제·매출 퍼널, 요인 후보→운영 행동→7일 비교 |
| 회복 보조 근거 | 지도·동종업종·상권 고객층·행사·대목을 핵심 퍼널과 출처별로 구분해 연결 |
| 정책 | 1차 수집, 원문·자격 구조화, 프로필 매칭·검색 |
| 대시보드 정보 | 뉴스·이슈 배너, 날씨·캘린더, 현재 시간 행동 안내 |
| iM챗봇 | 권한 있는 모든 카테고리의 간단한 질의응답 |
| AI 비서 | 질문별 분석 실행·종합 요약·실제 PDF |
| 데이터 처리 | 출처·권한·검증·정규화, 서비스와 테스트 자료 분리 |

대구로 운영지표, 직접 입력·거래내역 보완, 기존 금융 비교는 제거하지 않고 자료 확보 조건으로 유지합니다.

## 6. 주요 처리 흐름

### 데이터 수집

자료 이용 권한 확인 → 공급원 식별·검증 → 중복·취소·갱신 처리 → DB 저장 → 집계 → 출처가 연결된 결과.

POS 가져오기는 DB 조회를 기본으로 설계합니다. 실제 POS 장비와 실시간 연결하는 기능을 이미 확보했다고 가정하지 않습니다.

CCTV는 영상 원본을 일반 서비스로 전달하는 구조를 기본으로 하지 않습니다. 카메라 또는 별도 분석 계층에서 익명 집계한 통행·체류·입장 수와 품질 메타데이터를 제공하는 방향이며, 실제 공급·연결 방식은 아직 미정입니다.

정책은 금융·창업 분류 및 제목 키워드 필터 후 조건을 구조화합니다. 수집 실패·원문 변화·불확실한 추출을 기록합니다. 정기 갱신 요구는 반영하지만 실행 주기·스케줄러는 미정이며 n8n으로 확정하지 않습니다.

### 혼합형 회복 플랜

```text
CCTV 익명 집계: 통행 → 체류 → 입장
                         +
POS: 유효 결제 건수 → 순매출
                         ↓
시간 정렬·품질 확인 → 유입률·추정 구매전환율·객단가
                         ↓
이탈 구간·요인 후보 → 지도·상권 보조 근거
                         ↓
실행 행동 기록 → 7일 후 동일 조건 전후 비교
```

목표 서비스에서는 정량 계산과 규칙 판정을 서버 공통 분석 계층에서 수행하고 화면·iM챗봇·AI 비서·PDF가 같은 analysis_run과 recovery_experiment를 조회합니다. 지도·상권 보조 자료가 실패해도 핵심 CCTV·POS 결과는 유지하며, 핵심 자료가 없으면 보조 자료로 전환율을 채우지 않습니다.

### iM챗봇

질문·가게·분석 실행 확인 → 허용된 데이터 조회 → 규칙/선택된 문장 생성기 → 근거 검증 → 짧은 답변 또는 답변 불가·상담 안내.

모든 탭의 데이터 범위는 유지하되 타인 가게 자료와 계정 비밀은 제외합니다. 상담 연계는 채널·응대 주체가 결정되어야 구현할 수 있습니다.

### AI 비서

대화·예시 질문 → 분석 범위 확인 → 허용된 분석 서비스 호출 → 결과 확인 → 최종 요약 → PDF 생성 → 권한 확인 후 다운로드.

iM챗봇의 간단한 답변과 AI 비서의 분석 작업은 별도 경로입니다. 생성 문장이 DB 쿼리나 임의 코드를 직접 실행하도록 하지 않습니다. 파일이 존재하지 않으면 완료 링크를 반환하지 않습니다.

### 행사·현재 시간 안내

확인된 시간 집계 또는 일정 → 정의된 규칙 → 화면 안내.

현재 시간 안내는 과거 기록의 참고 제안이며 실시간 수요 예측이 아닙니다. 업종만으로 만든 대목 문구는 출처 있는 행사 사실과 구분합니다. 알림 전송 채널은 미정입니다.

## 7. 금융 경계

실제 계좌 연동, 신용평가, 대출 심사·실행, 상품 가입, 금융거래 승인은 범위 밖입니다.

현금흐름·상대 비교·정책 정보는 근거 있는 참고자료로 제공하며, 고정 Mock 62°·+650,000원은 목표 분석에 승계하지 않습니다. 예상 입출금 가정과 점수 정의가 먼저 필요합니다.

## 8. 목표 보안·장애 처리 요구사항

현재 정적 프로토타입의 로그인 화면은 시연용일 뿐 인증·권한·서버 비밀관리·보안감사 기능이 없습니다. 아래 내용은 목표 통제사항이며 개인정보·CCTV·금융 관련 법률 검토나 보안 인증 완료를 뜻하지 않습니다.

- 개인·가게 데이터와 PDF에 로그인·소유권 검증.
- 비밀번호는 해시로 관리, 분석·API 응답에서 제외.
- DB 자격정보·서버용 API 키는 환경변수로 관리, Git에 포함하지 않음.
- 브라우저용 지도 키와 서버 비밀키는 노출 방식이 다르므로 제공처의 제한 설정을 확인.
- 외부 AI를 선택해도 원자료·개인정보를 무조건 전송하지 않음.
- CCTV 원본 영상·얼굴·개인 식별정보를 대시보드·iM챗봇·AI 비서·일반 분석 API에 제공하지 않음. 실제 영상 보관·접근·삭제 기준은 도입 전에 검토·확정.
- 외부 호출 실패 시 다른 분석 화면은 유지하고 누락·재시도 안내.
- 공모전 생성 자료는 synthetic_demo로 명시하여 사용. 실자료 요청 실패 시 Mock으로 자동 대체하지 않음.
- 반출 제한 자료는 승인된 범위만 처리하고, 허용된 POS는 별도 보호 저장.
- 테스트 자료는 서비스 데이터와 분리.

## 9. 구현 순서와 기존 구조 보존

1. 현재 메인에 연결한 고정 CCTV·POS 생성 시나리오를 보존하면서 집계 계약, 익명·중복 기준, 시간 정합성과 기회 시간·7일 비교 규칙 확정.
2. Django·MySQL·인증·소유권 기본 구조.
3. 생성 CCTV·POS 데이터의 일관성·산식·시간 정합성 검증과 공통 분석 결과. 실자료는 권한·출처·익명 처리 별도 검증.
4. 현재 분리된 CCTV·POS 전환 퍼널과 기존 P0·POS 상세·기간 분석·PDF를 같은 데이터셋과 분석 실행으로 연결. 실자료 확보 시 권한·출처를 검증해 교체.
5. 실행 기록·7일 비교를 연결한 뒤 지도·상권 보조 기능을 데이터 준비 순서에 따라 구현.
6. API·화면·iM챗봇·AI 비서·PDF 일치 검증 후 발표 자료 갱신.

순서는 기술적 의존관계이며 신규 기능의 일괄 P0 확정이 아닙니다. 회복 플랜의 기존 P0 지위는 유지합니다.

예정 구조는 기존 prototype/, Django config/, apps/, templates/, static/, tests/ 방향을 유지합니다. 내부 테스트 fixture는 tests/ 아래에서 격리하고 생성 시연 데이터는 출처가 명시된 별도 데이터셋으로 관리합니다. 실제 사용자 자료는 저장소 밖의 접근 통제된 저장소로 분리합니다. 아직 폴더·DB·서버를 생성한 것은 아닙니다.

## 10. P2 조건부 블록체인 참조 구조(현재 미적용)

이 절은 현재 목표 아키텍처가 아니라, 월간 경영 브리핑의 발급 후 변경 여부를 여러 기관이 독립적으로 확인해야 하는 상황에 대비한 **조건부 참조 구조**입니다. 원장 유형, 참여기관, 노드, 기록 주체, 기술 제품, 스마트계약과 일정은 확정되지 않았으며 현재 프로토타입·DB·API·PDF에 구현되지 않았습니다.

### 10-1. 적용 조건과 역할 경계

실제 중앙 운영 구조로 전환할 때는 MySQL의 출처·버전·감사 기록을 먼저 구현·검증하고, 필요한 발급자 전자서명도 별도 검토합니다. 현재 정적 프로토타입에는 두 기능 모두 구현되어 있지 않습니다. 다음 항목을 모두 확인하고 별도 PoC 승인을 받은 경우에만 원장 어댑터를 추가합니다.

1. iM뱅크·소상공인·상담기관 등 서로 독립된 복수 주체의 공동 검증 수요가 존재함.
2. 중앙 DB 감사 기록과 전자서명만으로 해결하기 어려운 분쟁·신뢰 문제가 문서화됨.
3. 참여기관, 노드 운영, 읽기·쓰기 권한, 비용 부담과 장애 대응 책임이 확정됨.
4. 개인정보·금융정보·보존·정정·삭제 관련 검토가 완료됨.
5. 발급키 생성·보관·회전·폐기·복구와 유출 사고 대응 절차가 마련됨.
6. 원장 장애 시 기존 분석·보고서 기능과 검증 기능을 분리해 운영할 수 있음.
7. 중앙 방식 대비 비용·편익과 PoC 성공·중단 기준이 합의됨.

조건을 충족하지 않으면 원장 계층을 추가하지 않고 목표 중앙 운영 구조의 DB 감사 기록을 우선 구현하며, 필요한 전자서명은 별도 검토합니다.

### 10-2. 조건부 발급·검증 흐름

```text
공통 analysis_run·source_ids·data_version·rule_version 확정
                              ↓
월간 경영 브리핑 manifest와 보고서 버전 확정
                              ↓
직접 식별할 수 없는 verification_id 생성
                              ↓
verification_id 검증 URL만 담은 QR 삽입 → PDF 최종 저장
                              ↓
최종 PDF 바이트의 digest 계산
                              ↓
[별도 PoC 승인 시] 승인 발급키로 원장 거래 서명·제출
                              ↓
원장 거래 영수증과 내부 ID 매핑은 접근 통제된 서버에 저장

사용자 QR 요청 → verification_id의 등록 기록·버전·생명주기 조회
               → 파일 미선택 상태는 등록 확인, 일치 여부 미확인

사용자가 PDF를 명시적으로 선택
→ 브라우저가 기기에서 digest 계산, PDF 원본은 기본적으로 전송하지 않음
→ 계산 digest와 verification_id의 원장 기록 비교
→ 일치 / 불일치-확인 필요 / 기록 확인 불가
```

QR과 검증 ID는 해시 계산 전에 최종 PDF에 포함합니다. 원장 거래 ID나 블록 높이를 PDF에 사후 삽입하면 파일 해시가 달라지므로, 등록 결과는 검증 서버가 `verification_id`로 조회하고 PDF를 다시 생성하지 않습니다.

QR은 등록 기록과 버전을 조회하는 진입점입니다. QR만 스캔해서는 사용자가 가진 PDF 바이트를 검증 서버가 알 수 없으므로 파일 일치를 판정하지 않습니다. 전체 파일 검증은 사용자의 명시적 파일 선택 후 브라우저의 로컬 해시 계산으로 수행하며, 서버 보관본을 다시 해시하는 것만으로 사용자 파일이 일치한다고 판정하지 않습니다.

### 10-3. 저장 경계

| 위치 | 정보 후보 | 원칙 |
|---|---|---|
| 접근 통제된 기존 저장소 | PDF 원본, 검증 manifest, `analysis_run_id`, `source_ids`, 사업자·사용자·가게 ID, POS·CCTV·금융 원자료, 상세 지표·산식·주의사항, 동의·권한·정정·삭제 기록 | 현재 보안·소유권·감사 통제를 적용하며 원장에 복제하지 않음 |
| 키 관리 계층 | 발급 개인키, 회전·폐기·복구 정보 | Git·브라우저·PDF·일반 DB 응답에 포함하지 않음 |
| 조건부 원장 | 직접 식별할 수 없는 `verification_id`, `artifact_digest`, digest·표준화 버전, 보고서 버전, UTC 보고서 발급 시각, 거래 서명 주체의 계정·키 식별자, 대체 버전 연결정보 | 개인정보 영향과 재식별 가능성을 검토한 뒤 최소 정보만 기록 |
| 검증 서버 | 검증 ID와 내부 보고서·원장 거래의 매핑, 거래 영수증·제출/확정 시각, 검증 접근 로그, 원장 장애 상태 | 보고서 발급 시각과 원장 처리 시각을 구분하고 사용자의 보고서 접근 권한과 원장 조회 결과를 분리해 처리 |

### 10-4. 상태와 표현 범위

검증 상태는 기존 분석 `data_status`와 분리합니다.

- `not_registered`: 등록된 검증 기록이 없음.
- `pending`: 등록 또는 확정 처리 중.
- `registered_unchecked`: 등록 기록과 버전은 확인했지만 사용자가 가진 파일은 비교하지 않음.
- `matched`: 현재 파일이 발급 당시 등록본과 일치함.
- `mismatched`: 일치하지 않아 변경 여부 확인이 필요함.
- `unavailable`: 원장·네트워크·검증 서비스 문제로 현재 확인할 수 없음.

보고서 생명주기 `active`, `superseded`, `revoked`도 검증 결과와 분리합니다. 수정본은 새 검증 ID·해시·버전으로 발급하고 이전 버전과의 대체 관계를 보존합니다. QR 조회만으로 `matched`를 반환하지 않으며 원장 장애나 기록 누락을 `matched` 또는 `mismatched`로 대신 표시하지 않습니다.

이 구조가 확인하는 범위는 **발급 당시 등록본과 현재 파일의 일치 여부 및 버전 이력**입니다. 원본 POS·금융 자료의 사실성, 누락 여부, AI 분석의 정확성, 경영상태, 공식 인증·소명 효력, 금융상품·대출·지원 심사 결과를 확인하거나 증명하지 않습니다. 해시값만 생성하고 실제 원장 네트워크·기록 주체·거래·검증 절차를 구현하지 않았다면 블록체인 적용 또는 PoC 완료로 설명하지 않습니다.
