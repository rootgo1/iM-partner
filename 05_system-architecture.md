# iM 파트너 시스템 구조 명세서

- 문서 상태: 초안
- 기준 문서: 01_prd.md, 02_function-spec.md, 03_user-flow.md, 04_data-spec.md
- 서비스명: iM 파트너
- 프로토타입 형태: 웹 기반 반응형 서비스
- 개발 언어: Python
- 웹 프레임워크: Django
- API 프레임워크: Django REST Framework
- 데이터베이스: MySQL
- 프로토타입 데이터: AI 생성 더미 데이터
- 본선 데이터: 허용된 실제 분석 결과 및 시각화 자료

---

## 1. 시스템 구축 목적

iM 파트너는 소상공인의 지역·업종 정보를 기준으로 카드소비, 유동인구, 매출·지출, 금융상태 및 정책 데이터를 분석하여 인사이트, 골목상권 회복 플랜 및 AI 리포트를 제공하는 웹 서비스입니다.

프로토타입 단계에서는 AI 생성 더미 데이터를 사용합니다.

---

## 2. 시스템 구조 원칙

- 웹과 모바일에서 사용할 수 있는 반응형 구조로 개발합니다.
- 핵심 기능을 먼저 구현하고 보조 기능은 이후 확장합니다.
- 데이터 분석 로직과 화면 표시 로직을 분리합니다.
- 분석 결과는 숫자, 그래프, 문장 형태로 제공합니다.
- 더미 데이터와 실제 분석 결과를 동일한 구조로 관리합니다.
- 실제 원본 데이터를 무단으로 저장하거나 반출하지 않습니다.
- 데이터가 없는 경우 임의의 분석 결과를 생성하지 않습니다.
- 상권·업종 단위 데이터는 확정적인 원인이 아닌 원인 후보·영향 요인으로 표현합니다.
- 회복 플랜의 소비 기회 규모와 금융 효과는 비교지표 기반 참고용 시나리오로 표시합니다.
- 민감한 설정값과 API 키는 환경변수로 관리합니다.
- 향후 실제 분석 결과로 교체할 수 있도록 구성합니다.
- 실제 금융기관 연동이나 금융상품 가입 기능은 현재 범위에 포함하지 않습니다.

---

## 3. 전체 시스템 구조

```text
사용자 브라우저
        ↓
Django 웹 화면
        ↓
Django View 또는 DRF API
        ↓
서비스 처리 계층
        ├── 사용자 조건 처리
        ├── 상권·매출 분석
        ├── 골목상권 회복 플랜
        ├── 매출·지출 분석
        ├── 금융상태 분석
        ├── 정책·지원사업 추천
        └── AI 리포트 생성
        ↓
데이터 분석 계층
        ├── pandas
        └── numpy
        ↓
데이터 접근 계층
        ├── Django ORM
        └── mysqlclient
        ↓
MySQL 데이터베이스
```

---

## 4. 데이터 입력 및 저장 구조

### 4-1. 프로토타입 더미 데이터

더미 데이터는 파일 형태로 준비한 뒤 초기 입력 과정을 거쳐 MySQL에 저장합니다.

```text
CSV·JSON·엑셀 더미 데이터
        ↓
데이터 형식 및 항목 확인
        ↓
초기 데이터 입력 스크립트 실행
        ↓
MySQL 저장
        ↓
서비스에서 조건별 조회
```

`data/dummy/` 폴더에는 프로토타입용 원본 더미 데이터만 보관합니다.

---

### 4-2. 본선 실제 분석 결과

대구 빅데이터활용센터에서 분석한 실제 원본 데이터는 직접 저장하거나 반출하지 않습니다.

허용된 경우에만 다음 결과를 활용합니다.

- 집계된 분석 결과
- 허용된 표와 그래프
- 발표용 시각화 자료
- 실제 분석 기준 기간·지역·업종 정보

```text
센터 내 실제 데이터 분석
        ↓
허용된 분석 결과 정리
        ↓
더미 데이터와 동일한 구조로 변환
        ↓
프로토타입 결과와 필요한 범위에서 교체
```

실제 데이터의 반출 또는 저장이 허용되지 않은 경우에는 원본 데이터와 원본 파일을 프로젝트 및 GitHub에 포함하지 않습니다.

---

## 5. 전체 시스템 구성 요소

| 구성 요소 | 주요 역할 | 사용 기술 |
|---|---|---|
| 웹 화면 | 사용자 입력 및 분석 결과 표시 | Django Template, HTML, CSS, JavaScript |
| Django View | 웹 화면 요청 처리 | Django |
| API 서버 | 화면과 데이터 처리 기능 연결 | Django REST Framework |
| 사용자 조건 처리 | 지역·업종·매출 범위 입력 처리 | Django |
| 상권분석 모듈 | 카드소비·유동인구 비교 | pandas, numpy |
| 회복 플랜 모듈 | 분석 결과 기반 운영 행동·참고용 금융 효과 연결 | Python |
| 매출·지출 분석 모듈 | 매출·지출 및 자금 흐름 계산 | pandas, numpy |
| 금융상태 모듈 | 업종 평균 대비 금융상태 비교 | Python |
| 정책 추천 모듈 | 지역·업종·매출 범위 조건 비교 | Django, Python |
| AI 리포트 모듈 | 분석 결과 요약 및 대응 방법 제공 | 분석 결과 기반 처리 |
| 데이터 입력 모듈 | 더미 데이터 정리 및 입력 | pandas, openpyxl |
| 데이터 저장소 | 더미 데이터 및 분석 결과 저장 | MySQL |
| 이미지 처리 모듈 | 향후 영수증 업로드 및 OCR 처리 | Pillow |
| 환경 설정 | 데이터베이스 비밀번호·API 키 관리 | django-environ |
| 테스트 | 기능 및 API 동작 확인 | pytest, pytest-django |

---

## 6. 웹 화면과 API의 역할

### 6-1. Django 웹 화면

초기 웹 프로토타입은 Django Template을 중심으로 구현합니다.

주요 역할:

- 지역·업종 입력
- 매출·지출 정보 표시
- 그래프 및 표 표시
- 금융 체온계 표시
- 정책·지원사업 목록 표시
- AI 리포트 표시
- 데이터 없음 및 오류 안내

---

### 6-2. Django REST Framework API

DRF API는 화면과 분석 기능을 분리하고, 향후 모바일 화면에서 재사용할 수 있도록 구성합니다.

주요 역할:

- 사용자 입력값 수신
- 분석 조건 전달
- 분석 결과 반환
- 정책 및 지원사업 검색 결과 반환
- AI 리포트 결과 반환
- 오류 및 데이터 없음 상태 반환

세부 API 주소와 요청·응답 형식은 `08_api-spec.md`에서 정의합니다.

---

### 6-3. 공통 서비스 계층

Django 웹 화면과 DRF API는 동일한 서비스 처리 계층을 사용합니다.

```text
Django 웹 화면 ──┐
                 ├── 서비스 처리 계층 ── 분석 계층 ── 데이터 접근 계층
DRF API ────────┘
```

이를 통해 웹 화면과 API에서 서로 다른 분석 결과가 생성되는 것을 방지합니다.

---

## 7. 주요 기능 연결 구조

```text
F-01 지역·업종 입력
        ↓
F-02 상권 및 매출 현황
        ↓
F-03 상권분석 및 인사이트
        ├──→ F-09 골목상권 회복 플랜
        │          ↓
        │      F-04 매출·지출 분석
        │          ↓
        │      F-05 골목금융 체온계
        │          ↓
        │      F-06 정책·지원사업 추천
        │
        └──→ F-07 AI 리포트
```

F-08 시연용 데이터 처리는 각 기능에서 사용할 더미 데이터를 제공합니다.

---

## 8. 계층별 역할

### 8-1. 웹 화면 계층

사용자가 직접 이용하는 화면을 담당합니다.

주요 역할:

- 지역·업종 입력
- 분석 기간 및 매출 범위 입력
- 분석 결과 표시
- 그래프·표·수치 표시
- 인사이트 문장 표시
- 데이터 없음 및 오류 안내

---

### 8-2. 요청 처리 계층

사용자의 화면 요청과 API 요청을 처리합니다.

주요 역할:

- 입력값 검증
- 서비스 처리 계층 호출
- 처리 결과 전달
- 오류 상태 반환

---

### 8-3. 서비스 처리 계층

사용자의 요청에 따라 각 기능의 처리 순서를 담당합니다.

주요 처리 내용:

- 지역·업종 조건 저장
- 매출 범위 조건 저장
- 조건에 맞는 데이터 조회
- 분석 모듈 호출
- 정책 추천 조건 전달
- 회복 플랜 조건 및 참고 지표 전달
- 여러 분석 결과 통합
- AI 리포트 생성 요청

---

### 8-4. 데이터 분석 계층

데이터를 집계하고 분석 지표를 계산합니다.

주요 분석 내용:

- 최근 2년 카드소비 증감률
- 유동인구 증감률
- 주요 요일·시간대
- 유동인구와 카드소비 차이
- 소비 전환 공백 시간대
- 방문 고객층과 소비 고객층 비교
- 경기일·비경기일 소비 차이
- 총매출·총지출
- 한 달간 예상 자금 흐름
- 업종 평균 대비 금융상태

분석 결과는 상권·업종 단위 데이터와 사용자 입력정보를 비교한 원인 후보이며, 실제 가게 매출 감소의 확정적인 원인으로 해석하지 않습니다.

분석 결과는 화면과 AI 리포트에서 공통으로 사용할 수 있도록 정리합니다.

---

### 8-5. 데이터 접근 계층

데이터베이스에서 데이터를 조회하고 저장하는 역할을 담당합니다.

주요 역할:

- 가게 기본정보 조회
- 카드소비 데이터 조회
- 유동인구 데이터 조회
- 매출·지출 데이터 조회
- 금융상태 비교 데이터 조회
- 정책 및 지원사업 데이터 조회
- 더미 데이터 저장 및 조회

Django ORM을 사용하여 데이터베이스와 연결합니다.

---

### 8-6. 데이터베이스 계층

프로토타입에서 사용하는 데이터를 저장합니다.

주요 저장 대상:

- 가게 기본정보
- 지역·업종 정보
- 카드소비 데이터
- 유동인구 데이터
- 매출·지출 데이터
- 대구로 운영 데이터
- 금융상태 비교 데이터
- 정책 및 지원사업 데이터
- 행사·날씨 데이터
- 허용된 분석 결과

실제 원본 데이터와 반출이 제한된 데이터는 저장하지 않습니다.

---

## 9. 더미 데이터와 실제 결과의 공통 구조

더미 데이터와 실제 분석 결과를 교체할 수 있도록 다음 기준을 공통으로 사용합니다.

| 공통 항목 | 활용 내용 |
|---|---|
| 지역 | 분석 대상 지역 |
| 업종 | 분석 대상 업종 |
| 기간 | 분석 기준 기간 |
| 월 | 월별 추세 분석 |
| 요일 | 요일별 비교 |
| 시간대 | 시간대별 비교 |
| 유동인구 | 유동인구 수 또는 지수 |
| 카드소비 | 카드소비 금액 또는 지수 |
| 연령 구분 | 고객층 분석 |
| 성별 구분 | 고객층 분석 |
| 경기일 여부 | 경기일·비경기일 비교 |

데이터의 실제 컬럼명은 제공기관에 따라 달라질 수 있으므로, 서비스 내부에서 공통 형식으로 변환한 뒤 분석합니다.

---

## 10. 대표 사용자 요청 처리 흐름

```text
1. 사용자가 지역·업종 입력
        ↓
2. Django가 입력값 확인
        ↓
3. 조건에 맞는 더미 데이터 조회
        ↓
4. 분석 모듈이 카드소비·유동인구 분석
        ↓
5. 주요 시간대와 소비 전환 공백 계산
        ↓
6. 회복 플랜과 참고용 금융 효과 계산
        ↓
7. 분석 결과를 화면에 표시
        ↓
8. 분석 결과를 AI 리포트 모듈에 전달
        ↓
9. 대응 방법을 포함한 리포트 표시
```

---

## 11. AI 리포트 처리 구조

프로토타입에서는 별도의 학습모델을 새로 학습시키지 않습니다.

분석 결과와 회복 플랜을 정리하여 AI 리포트에 전달합니다.

```text
원천 데이터
        ↓
pandas·numpy 분석
        ↓
구조화된 분석 결과
        ↓
AI 리포트 처리 모듈
        ↓
상황 요약 및 대응 방법 표시
```

AI 리포트 생성 방식은 다음 원칙을 따릅니다.

- 분석된 수치와 인사이트를 근거로 사용
- 분석되지 않은 내용을 임의로 생성하지 않음
- 매출 감소 요인 후보와 근거를 함께 표시
- 추천 운영 행동과 참고용 금융 효과를 근거 수치와 연결
- 프로토타입에서는 규칙 또는 템플릿 기반 문장 구성을 우선 검토
- 향후 AI API 등을 연결하더라도 분석 모듈과 리포트 모듈은 분리

---

## 12. 금융 기능의 구현 범위

골목금융 체온계는 사용자가 입력한 매출·지출 정보와 업종 평균 데이터를 비교하는 기능입니다.

현재 구현 범위:

- 매출액 비교
- 영업이익률 비교
- 부채비율 비교
- 비용 구조 비교
- 예상 자금 흐름 비교
- 상대적인 금융상태 표시

현재 구현하지 않는 기능:

- 금융기관 계좌 연동
- 실제 신용평가
- 대출 심사
- 대출 실행
- 금융상품 가입
- 금융거래 승인

---

## 13. 데이터 처리 구조

### 프로토타입 단계

```text
AI 생성 더미 데이터
        ↓
CSV·JSON·엑셀 형식 확인
        ↓
초기 데이터 입력
        ↓
MySQL 저장
        ↓
사용자 조건으로 데이터 조회
        ↓
pandas·numpy 분석
        ↓
그래프·수치·인사이트 생성
        ↓
웹 화면 및 AI 리포트 표시
```

### 본선 시연 단계

```text
대구 빅데이터활용센터 분석
        ↓
허용된 분석 결과 및 시각화 자료 정리
        ↓
공통 데이터 구조로 변환
        ↓
프로토타입 더미 결과와 필요한 범위에서 교체
        ↓
웹 화면에서 실제 분석 결과 시연
```

---

## 14. 논리적 기능 모듈

### 14-1. 사용자 조건 모듈

- 사업 지역 입력
- 업종 입력
- 매출 범위 입력
- 분석 기간 관리

### 14-2. 상권분석 모듈

- 카드소비 분석
- 유동인구 분석
- 시간대별 비교
- 고객층 비교
- 소비 전환 공백 분석
- 경기일·비경기일 비교

### 14-3. 매출·지출 모듈

- 매출 입력 및 조회
- 지출 입력 및 조회
- 비용 항목 분류
- 예상 매출·지출 계산
- 자금 흐름 계산

### 14-4. 금융상태 모듈

- 매출액 비교
- 영업이익률 비교
- 부채비율 비교
- 비용 구조 비교
- 금융 체온계 표시

### 14-5. 정책 추천 모듈

- 지역 조건 비교
- 업종 조건 비교
- 매출 범위 조건 비교
- 지원사업 목록 표시

### 14-6. AI 리포트 모듈

- 분석 결과 요약
- 매출 감소 요인 후보 정리
- 주요 근거 수치 표시
- 운영 방향 제시
- 대응 방법 표시

### 14-7. 골목상권 회복 플랜 모듈

- 소비 전환 공백 시간대 확인
- 핵심 소비 고객층 확인
- 추천 운영 행동 연결
- 참고용 소비 기회 지수 계산
- 참고용 자금 흐름 변화 표시
- 관련 정책·지원사업 연결

회복 플랜의 효과는 비교지표 기반 참고용 시나리오로 제공하며, 실제 매출이나 금융 결과를 보장하지 않습니다.

---

## 15. 권장 프로젝트 구조

현재 정적 프로토타입은 `prototype/` 폴더에서 실행합니다. 아래 구조는 이후 Django 백엔드로 확장할 때의 권장 구조입니다.

```text
iM-partner/
├── prototype/
│   ├── main-screen.html
│   └── mock-data.js
├── manage.py
├── config/
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── apps/
│   ├── dashboard/
│   ├── market/
│   ├── finance/
│   ├── policy/
│   └── report/
├── templates/
├── static/
├── data/
│   └── dummy/
├── scripts/
│   └── seed_dummy_data.py
├── tests/
├── requirements.txt
├── .env.example
└── README.md
```

### 폴더별 역할

| 폴더 | 역할 |
|---|---|
| `config/` | Django 프로젝트 설정 |
| `apps/` | 기능별 Django 앱 |
| `templates/` | 웹 화면 템플릿 |
| `static/` | CSS·JavaScript·이미지 |
| `data/dummy/` | 프로토타입용 더미 데이터 |
| `scripts/` | 더미 데이터 초기 입력 등 실행 스크립트 |
| `tests/` | 테스트 코드 |
| `.env.example` | 환경변수 예시 |
| `README.md` | 설치 및 실행 방법 |

실제 분석 원본 데이터는 프로젝트 폴더와 GitHub에 저장하지 않습니다.

---

## 16. 환경변수 및 보안 관리

다음 값은 코드에 직접 작성하지 않고 환경변수로 관리합니다.

- 데이터베이스 이름
- 데이터베이스 사용자명
- 데이터베이스 비밀번호
- 데이터베이스 주소
- 공공데이터 API 키
- AI 리포트 관련 설정값

`.env` 파일은 GitHub에 업로드하지 않습니다.

`.env.example` 파일에는 필요한 환경변수의 이름과 형식만 작성합니다.

---

## 17. 오류 및 예외 처리

### 입력값이 없는 경우

- 필수 입력값 안내
- 분석 시작 제한

### 분석 데이터가 없는 경우

- 분석 결과를 임의로 생성하지 않음
- 데이터 없음 안내 표시

### 일부 데이터가 없는 경우

- 확인 가능한 데이터만 분석
- 제외된 항목 안내

### 데이터 기준이 다른 경우

- 지역·업종·기간·단위 확인
- 비교 가능한 기준으로 변환
- 변환이 불가능하면 해당 비교 제외

### 분석 처리 오류

- 사용자에게 일반적인 오류 안내
- 개발 환경에서는 오류 원인 기록

### 영수증 인식 실패

- 인식 실패 안내
- 항목과 금액 직접 입력 기능 제공

---

## 18. 기능 구현 우선순위

### 1단계: 핵심 시연 기능

- 지역·업종 입력
- 상권 및 매출 현황
- 유동인구·카드소비 비교
- 매출 감소 요인 후보 분석
- 골목상권 회복 플랜
- AI 리포트

### 2단계: 보조 기능

- 골목금융 체온계
- 정책 및 지원사업 추천
- 매출·지출 상세 분석
- 영수증 OCR은 향후 확장 기능

---

## 19. 시스템 구조 완료 기준

- 웹 화면과 API의 역할이 구분되어야 함
- 데이터 분석 로직과 화면 표시 로직이 분리되어야 함
- 더미 데이터 입력 과정을 통해 MySQL에 데이터를 저장할 수 있어야 함
- 더미 데이터와 실제 허용 분석 결과를 공통 구조로 처리할 수 있어야 함
- 카드소비와 유동인구 분석 결과를 화면에 전달할 수 있어야 함
- 분석 결과에서 회복 플랜을 생성하고 화면에 전달할 수 있어야 함
- 분석 결과를 AI 리포트에서 재사용할 수 있어야 함
- 별도 학습모델 없이 프로토타입 리포트를 생성할 수 있어야 함
- 금융기관 연동 없이 금융상태 비교 기능을 제공할 수 있어야 함
- 데이터가 없는 경우 예외 처리가 가능해야 함
- 실제 원본 데이터를 저장하지 않고 허용된 결과만 활용해야 함
- 환경변수와 민감한 설정값을 분리해야 함
- 핵심 기능부터 단계적으로 구현할 수 있어야 함
