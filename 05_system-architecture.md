# iM 파트너 시스템 아키텍처

- 개정일: 2026-09-04 / 정적 HTML 부분 구현 및 목표 설계
- 현재 실행 구조와 목표 구조를 구분합니다.
- 관련: [데이터](04_data-spec.md), [DB](07_db-schema.md), [API](08_api-spec.md)

## 1. 현재 실행 구조

브라우저 → prototype/main-screen.html → meeting-data.js → meeting-ui.js → report-pdf.js.

현재는 DB·백엔드·외부 API·실제 CCTV 분석·지도·사용자 인증이 없는 정적 시안입니다. `meeting-data.js`가 POS 형식 생성 자료와 공통 계산을 제공하고, `meeting-ui.js`가 화면·기간·챗봇·AI 비서를 연결합니다. `report-pdf.js`는 서버 없이 브라우저에서 실제 PDF 파일을 만듭니다. 챗봇과 AI 비서는 외부 AI가 아닌 키워드 규칙으로 동작하며, 프로필은 메모리에만 반영됩니다. 혼합형 회복 플랜은 별도 결과 화면 시안만 있으며 메인 데이터 흐름에는 아직 연결되지 않았습니다.

## 2. 목표 구조

```text
반응형 화면: 공통 메뉴 / 대시보드 / 상권 / 매출·지출 / 회복 / 정책 / AI 비서 / 프로필
                    ↕ 모든 탭의 챗봇
Django 웹 화면 또는 DRF API
                    ↓ 입력·권한 확인
공통 서비스 계층
    ├─ 가게·프로필·분석 조건
    ├─ 데이터 조회·집계·요인 후보
    ├─ CCTV·POS 전환 퍼널·회복 실험
    ├─ 지도·상권 보조 근거·현재 시간 안내
    ├─ 정책 조건 매칭
    ├─ 챗봇: 간단한 조회·설명
    └─ AI 비서: 대화형 분석·PDF 생성
                    ↓
Django ORM / MySQL
                    ↑
생성 시연 CCTV 집계·POS·지출 등(표시 필수) / 권한 있는 실자료 / 허용된 카드·유동인구 결과 / 지도·공공 자료
```

지도 SDK는 화면에 위치를 표시하고, 상권·점포 및 주소 좌표 변환의 실제 연동 방식은 제공처 확인 후 결정합니다. 지도 표시 기능과 경쟁 근거 수집은 별도 역할입니다.

## 3. 기술과 역할

| 영역 | 방향 |
|---|---|
| 화면 | 현재 HTML/CSS/JavaScript 톤 유지, Django Template 중심으로 확장 |
| 요청 | Django Views와 DRF, 같은 공통 서비스를 호출 |
| 분석 | Python, pandas·numpy; 계산은 서버 한 곳에서 수행 |
| 저장 | MySQL, Django ORM, 기능별 소유권·출처 연결 |
| 수집·입력 | httpx, openpyxl 등 기존 도구; 제공처 계약 검증 후 연결 |
| 인증 | Django 인증 기반 비밀번호 해시·세션·가게 권한 |
| 지도 | 카카오맵 활용 요구; 키·도메인·이용 조건 확인 필요 |
| CCTV 집계 | 기존 CCTV 활용 가능성을 검토하며 영상 대신 익명 시간대 집계값을 서비스에 연결; 공급 방식·모델·정확도·보관 기준 미정 |
| 챗봇·AI 비서 | 구조화 결과를 우선 사용; 외부 LLM 선택 미정 |
| PDF | 실제 파일 생성·저장·다운로드 계층 필요, 도구 선택 미정 |
| 품질 | pytest·pytest-django, ruff |
| 영수증 | Pillow 업로드 준비, OCR은 후순위 |

새 프론트 프레임워크, n8n, 특정 유료 AI, 새 학습모델을 이번에 도입하지 않습니다.

## 4. 공통 결과와 분석 실행

가게·기간·자료 버전·계산 규칙 → analysis_runs → 정량 지표·근거·요인 후보 → 모든 표시 영역.

- KPI, 그래프, 회복 플랜, 챗봇, AI 비서가 같은 결과를 사용합니다.
- 정량 계산을 화면과 AI가 각각 수행하지 않습니다.
- 소스 ID·단위·표본·누락과 규칙 버전을 기록합니다.
- 가게·기간 변경 시 오래된 결과를 새 결과처럼 보여주지 않습니다.
- 처리 상태와 자료 충분성 상태를 분리합니다.
- 자동 예측·체온계·정책 % 등 미정 산식은 완료 결과를 만들지 않습니다.

## 5. 기능 모듈

| 모듈 | 책임 |
|---|---|
| 가게·프로필 | 사용자 정보, 사업장 정보, 계정·소유권, 분석 조건 |
| 상권 | 카드소비·유동인구, 요일·시간·고객층·경기일 비교 |
| 매출·지출 | DB POS, 비용 5분류, 품목 매입·재고 조건 확인, 현금흐름 |
| 금융상태 | 정의된 업종 비교 결과를 대시보드 체온계로 제공 |
| 회복 플랜 | CCTV 통행·체류·입장 + POS 결제·매출 퍼널, 요인 후보→운영 행동→7일 비교 |
| 회복 보조 근거 | 지도·동종업종·상권 고객층·행사·대목을 핵심 퍼널과 출처별로 구분해 연결 |
| 정책 | 1차 수집, 원문·자격 구조화, 프로필 매칭·검색 |
| 대시보드 정보 | 뉴스·이슈 배너, 날씨·캘린더, 현재 시간 행동 안내 |
| 챗봇 | 권한 있는 모든 카테고리의 간단한 질의응답 |
| AI 비서 | 질문별 분석 실행·종합 요약·실제 PDF |
| 데이터 처리 | 출처·권한·검증·정규화, 서비스와 테스트 자료 분리 |

대구로 운영지표, 직접 입력·거래내역 보완, 기존 금융 비교는 제거하지 않고 자료 확보 조건으로 유지합니다.

## 6. 주요 처리 흐름

### 데이터 수집

자료 이용 권한 확인 → 공급원 식별·검증 → 중복·취소·갱신 처리 → DB 저장 → 집계 → 출처가 연결된 결과.

POS 가져오기는 DB 조회를 기본으로 설계합니다. 실제 POS 장비와 실시간 연결하는 기능을 이미 확보했다고 가정하지 않습니다.

CCTV는 영상 원본을 일반 서비스로 전달하는 구조를 기본으로 하지 않습니다. 카메라 또는 별도 분석 계층에서 익명 집계한 통행·체류·입장 수와 품질 메타데이터를 제공하는 방향이며, 실제 공급·연결 방식은 아직 미정입니다.

정책은 금융·창업 분류 및 제목 키워드 필터 후 조건을 구조화합니다. 수집 실패·원문 변화·불확실한 추출을 기록합니다. 정기 갱신 요구는 반영하지만 실행 주기·스케줄러는 미정이며 n8n으로 확정하지 않습니다.

### 혼합형 회복 플랜

```text
CCTV 익명 집계: 통행 → 체류 → 입장
                         +
POS: 유효 결제 건수 → 순매출
                         ↓
시간 정렬·품질 확인 → 유입률·추정 구매전환율·객단가
                         ↓
이탈 구간·요인 후보 → 지도·상권 보조 근거
                         ↓
실행 행동 기록 → 7일 후 동일 조건 전후 비교
```

정량 계산과 규칙 판정은 서버 공통 분석 계층에서 수행하고 화면·챗봇·AI 비서·PDF는 같은 analysis_run과 recovery_experiment를 조회합니다. 지도·상권 보조 자료가 실패해도 핵심 CCTV·POS 결과는 유지하며, 핵심 자료가 없으면 보조 자료로 전환율을 채우지 않습니다.

### 챗봇

질문·가게·분석 실행 확인 → 허용된 데이터 조회 → 규칙/선택된 문장 생성기 → 근거 검증 → 짧은 답변 또는 답변 불가·상담 안내.

모든 탭의 데이터 범위는 유지하되 타인 가게 자료와 계정 비밀은 제외합니다. 상담 연계는 채널·응대 주체가 결정되어야 구현할 수 있습니다.

### AI 비서

대화·예시 질문 → 분석 범위 확인 → 허용된 분석 서비스 호출 → 결과 확인 → 최종 요약 → PDF 생성 → 권한 확인 후 다운로드.

챗봇의 간단한 답변과 AI 비서의 분석 작업은 별도 경로입니다. 생성 문장이 DB 쿼리나 임의 코드를 직접 실행하도록 하지 않습니다. 파일이 존재하지 않으면 완료 링크를 반환하지 않습니다.

### 행사·현재 시간 안내

확인된 시간 집계 또는 일정 → 정의된 규칙 → 화면 안내.

현재 시간 안내는 과거 기록의 참고 제안이며 실시간 수요 예측이 아닙니다. 업종만으로 만든 대목 문구는 출처 있는 행사 사실과 구분합니다. 알림 전송 채널은 미정입니다.

## 7. 금융 경계

실제 계좌 연동, 신용평가, 대출 심사·실행, 상품 가입, 금융거래 승인은 범위 밖입니다.

현금흐름·상대 비교·정책 정보는 근거 있는 참고자료로 제공하며, 고정 Mock 62°·+650,000원은 목표 분석에 승계하지 않습니다. 예상 입출금 가정과 점수 정의가 먼저 필요합니다.

## 8. 보안·장애 처리

- 개인·가게 데이터와 PDF에 로그인·소유권 검증.
- 비밀번호는 해시로 관리, 분석·API 응답에서 제외.
- DB 자격정보·서버용 API 키는 환경변수로 관리, Git에 포함하지 않음.
- 브라우저용 지도 키와 서버 비밀키는 노출 방식이 다르므로 제공처의 제한 설정을 확인.
- 외부 AI를 선택해도 원자료·개인정보를 무조건 전송하지 않음.
- CCTV 원본 영상·얼굴·개인 식별정보를 대시보드·챗봇·AI 비서·일반 분석 API에 제공하지 않음. 실제 영상 보관·접근·삭제 기준은 도입 전에 검토·확정.
- 외부 호출 실패 시 다른 분석 화면은 유지하고 누락·재시도 안내.
- 공모전 생성 자료는 synthetic_demo로 명시하여 사용. 실자료 요청 실패 시 Mock으로 자동 대체하지 않음.
- 반출 제한 자료는 승인된 범위만 처리하고, 허용된 POS는 별도 보호 저장.
- 테스트 자료는 서비스 데이터와 분리.

## 9. 구현 순서와 기존 구조 보존

1. CCTV·POS 집계 계약, 익명·중복 기준, 시간 정합성과 기회 시간·7일 비교 규칙 확정 및 현재 정적 MVP 보존.
2. Django·MySQL·인증·소유권 기본 구조.
3. 생성 CCTV·POS 데이터의 일관성·산식·시간 정합성 검증과 공통 분석 결과. 실자료는 권한·출처·익명 처리 별도 검증.
4. CCTV·POS 전환 퍼널과 기존 P0·POS 상세·기간 분석을 같은 데이터셋으로 연결. 실자료 확보 시 교체.
5. 실행 기록·7일 비교를 연결한 뒤 지도·상권 보조 기능을 데이터 준비 순서에 따라 구현.
6. API·화면·챗봇·AI 비서·PDF 일치 검증 후 발표 자료 갱신.

순서는 기술적 의존관계이며 신규 기능의 일괄 P0 확정이 아닙니다. 회복 플랜의 기존 P0 지위는 유지합니다.

예정 구조는 기존 prototype/, Django config/, apps/, templates/, static/, tests/ 방향을 유지합니다. 내부 테스트 fixture는 tests/ 아래에서 격리하고 생성 시연 데이터는 출처가 명시된 별도 데이터셋으로 관리합니다. 실제 사용자 자료는 저장소 밖의 접근 통제된 저장소로 분리합니다. 아직 폴더·DB·서버를 생성한 것은 아닙니다.
